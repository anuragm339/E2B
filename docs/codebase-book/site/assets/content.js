window.CODEBASE_BOOK = {"generatedAt":"2026-06-13T04:56:53.308Z","stats":{"chapters":16,"lines":2735,"features":13,"apiSections":10},"chapters":[{"order":0,"file":"00-index.md","slug":"00-index","title":"Codebase Book Index","html":"<h1 id=\"codebase-book-index\" data-heading=\"Codebase Book Index\">Codebase Book Index<a class=\"heading-link\" href=\"#/chapter/00-index/codebase-book-index\" aria-label=\"Link to Codebase Book Index\">#</a></h1>\n<p>Repository: <code>kafka-like-messaging</code> Verified against the working tree on 2026-06-11.</p>\n<p>This book is a source-backed map of the repository. Start with the chapter that owns the behavior, then follow its links to the API, data, event, state, test, and debugging chapters.</p>\n<h2 id=\"chapters\" data-heading=\"Chapters\">Chapters<a class=\"heading-link\" href=\"#/chapter/00-index/chapters\" aria-label=\"Link to Chapters\">#</a></h2>\n<ol><li><a href=\"#/chapter/01-system-overview\">System overview</a></li><li><a href=\"#/chapter/02-module-map\">Module map</a></li><li><a href=\"#/chapter/03-feature-catalog\">Feature catalog</a></li><li><a href=\"#/chapter/04-api-catalog\">API catalog</a></li><li><a href=\"#/chapter/05-data-model\">Data model</a></li><li><a href=\"#/chapter/06-event-kafka-flow\">Event and Kafka-like flow</a></li><li><a href=\"#/chapter/07-pos-machine-state\">POS machine state</a></li><li><a href=\"#/chapter/08-compaction-handling\">Compaction handling</a></li><li><a href=\"#/chapter/09-error-retry-recovery\">Error, retry, and recovery</a></li><li><a href=\"#/chapter/10-test-map\">Test map</a></li><li><a href=\"#/chapter/11-runtime-config\">Runtime configuration</a></li><li><a href=\"#/chapter/12-debugging-guide\">Debugging guide</a></li><li><a href=\"#/chapter/13-risk-and-edge-cases\">Risks and edge cases</a></li><li><a href=\"#/chapter/14-glossary\">Glossary</a></li><li><a href=\"#/chapter/15-open-questions\">Open questions</a></li></ol>\n<h2 id=\"features\" data-heading=\"Features\">Features<a class=\"heading-link\" href=\"#/chapter/00-index/features\" aria-label=\"Link to Features\">#</a></h2>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Feature</th>\n<th>Primary chapter</th>\n<th>Entry point</th>\n</tr></thead><tbody>\n<tr>\n<td>Broker startup and shutdown</td>\n<td><a href=\"#/chapter/01-system-overview/startup-and-shutdown\">Overview</a></td>\n<td><code>broker/src/main/java/com/messaging/broker/core/BrokerService.java</code></td>\n</tr>\n<tr>\n<td>Producer ingestion</td>\n<td><a href=\"#/chapter/03-feature-catalog/producer-ingestion\">Features</a></td>\n<td><code>broker/src/main/java/com/messaging/broker/handler/DataHandler.java</code></td>\n</tr>\n<tr>\n<td>Parent-to-child pipe replication</td>\n<td><a href=\"#/chapter/03-feature-catalog/topology-and-pipe-replication\">Features</a></td>\n<td><code>broker/src/main/java/com/messaging/broker/core/TopologyManager.java</code></td>\n</tr>\n<tr>\n<td>Segment storage and recovery</td>\n<td><a href=\"#/chapter/05-data-model/segment-storage\">Data model</a></td>\n<td><code>storage/src/main/java/com/messaging/storage/segment/SegmentManager.java</code></td>\n</tr>\n<tr>\n<td>Modern consumer subscription</td>\n<td><a href=\"#/chapter/03-feature-catalog/modern-consumer-subscription-and-readiness\">Features</a></td>\n<td><code>broker/src/main/java/com/messaging/broker/handler/SubscribeHandler.java</code></td>\n</tr>\n<tr>\n<td>Zero-copy batch delivery</td>\n<td><a href=\"#/chapter/06-event-kafka-flow/modern-delivery-flow\">Events</a></td>\n<td><code>broker/src/main/java/com/messaging/broker/consumer/BatchDeliveryService.java</code></td>\n</tr>\n<tr>\n<td>Legacy multi-topic delivery</td>\n<td><a href=\"#/chapter/03-feature-catalog/legacy-consumer-delivery\">Features</a></td>\n<td><code>broker/src/main/java/com/messaging/broker/legacy/LegacyConsumerDeliveryManager.java</code></td>\n</tr>\n<tr>\n<td>Offset and per-record ACK persistence</td>\n<td><a href=\"#/chapter/03-feature-catalog/offset-and-ack-persistence\">Features</a></td>\n<td><code>broker/src/main/java/com/messaging/broker/consumer/BatchAckService.java</code></td>\n</tr>\n<tr>\n<td>POS refresh/reset/replay/ready</td>\n<td><a href=\"#/chapter/07-pos-machine-state\">POS state</a></td>\n<td><code>broker/src/main/java/com/messaging/broker/consumer/RefreshCoordinator.java</code></td>\n</tr>\n<tr>\n<td>Log compaction</td>\n<td><a href=\"#/chapter/08-compaction-handling\">Compaction</a></td>\n<td><code>broker/src/main/java/com/messaging/broker/compaction/CompactionScheduler.java</code></td>\n</tr>\n<tr>\n<td>Embedded annotation consumers</td>\n<td><a href=\"#/chapter/03-feature-catalog/embedded-annotation-consumers\">Features</a></td>\n<td><code>broker/src/main/java/com/messaging/broker/consumer/ConsumerAnnotationProcessor.java</code></td>\n</tr>\n<tr>\n<td>Client auto-discovery and reconnect</td>\n<td><a href=\"#/chapter/03-feature-catalog/modern-client-library\">Features</a></td>\n<td><code>client/src/main/java/com/messaging/client/ClientConsumerManager.java</code></td>\n</tr>\n<tr>\n<td>Monitoring and diagnostics</td>\n<td><a href=\"#/chapter/04-api-catalog/diagnostics-and-management\">API catalog</a></td>\n<td><code>broker/src/main/java/com/messaging/broker/http/ThreadDiagnosticsController.java</code></td>\n</tr>\n<tr>\n<td>Test-data ingestion</td>\n<td><a href=\"#/chapter/04-api-catalog/test-data-api\">API catalog</a></td>\n<td><code>broker/src/main/java/com/messaging/broker/http/TestDataController.java</code></td>\n</tr>\n</tbody></table></div>\n<h2 id=\"apis\" data-heading=\"APIs\">APIs<a class=\"heading-link\" href=\"#/chapter/00-index/apis\" aria-label=\"Link to APIs\">#</a></h2>\n<h3 id=\"http\" data-heading=\"HTTP\">HTTP<a class=\"heading-link\" href=\"#/chapter/00-index/http\" aria-label=\"Link to HTTP\">#</a></h3>\n<ul><li><code>GET /pipe/poll</code>: <a href=\"#/chapter/04-api-catalog/pipe-api\">Pipe API</a></li><li><code>POST /admin/refresh-topic</code>: <a href=\"#/chapter/04-api-catalog/refresh-api\">Refresh API</a></li><li><code>GET /admin/refresh-status</code>: <a href=\"#/chapter/04-api-catalog/refresh-api\">Refresh API</a></li><li><code>GET /admin/refresh-current</code>: <a href=\"#/chapter/04-api-catalog/refresh-api\">Refresh API</a></li><li><code>POST /admin/compaction/trigger</code>: <a href=\"#/chapter/04-api-catalog/compaction-api\">Compaction API</a></li><li><code>GET /admin/compaction/status</code>: <a href=\"#/chapter/04-api-catalog/compaction-api\">Compaction API</a></li><li><code>/admin/logging/**</code>: <a href=\"#/chapter/04-api-catalog/runtime-logging-api\">Runtime logging API</a></li><li><code>/diagnostics/threads/**</code>: <a href=\"#/chapter/04-api-catalog/diagnostics-and-management\">Diagnostics API</a></li><li><code>/test/**</code>: <a href=\"#/chapter/04-api-catalog/test-data-api\">Test-data API</a></li><li><code>/health</code>, <code>/prometheus</code>, <code>/metrics</code>: <a href=\"#/chapter/04-api-catalog/diagnostics-and-management\">Management endpoints</a></li></ul>\n<h3 id=\"tcp-protocol\" data-heading=\"TCP protocol\">TCP protocol<a class=\"heading-link\" href=\"#/chapter/00-index/tcp-protocol\" aria-label=\"Link to TCP protocol\">#</a></h3>\n<ul><li><code>DATA</code>, <code>ACK</code>, <code>SUBSCRIBE</code>, <code>COMMIT_OFFSET</code></li><li><code>RESET</code>, <code>READY</code>, <code>DISCONNECT</code>, <code>HEARTBEAT</code></li><li><code>BATCH_HEADER</code>, <code>BATCH_ACK</code>, <code>RESET_ACK</code>, <code>READY_ACK</code></li></ul>\n<p>See <a href=\"#/chapter/06-event-kafka-flow/modern-wire-protocol\">Event and Kafka-like flow</a>. Definitions are in <code>common/src/main/java/com/messaging/common/model/BrokerMessage.java</code>.</p>\n<h2 id=\"topics-and-events\" data-heading=\"Topics And Events\">Topics And Events<a class=\"heading-link\" href=\"#/chapter/00-index/topics-and-events\" aria-label=\"Link to Topics And Events\">#</a></h2>\n<p>This repository does <strong>not</strong> use Apache Kafka libraries or a Kafka broker. It implements Kafka-like topics, offsets, consumer groups, ACKs, and compaction itself. See <a href=\"#/chapter/06-event-kafka-flow\">Event and Kafka-like flow</a>.</p>\n<ul><li>Record events: <code>MESSAGE</code>, <code>DELETE</code> in <code>common/src/main/java/com/messaging/common/model/EventType.java</code></li><li>Legacy events: <code>REGISTER</code>, <code>MESSAGE</code>, <code>RESET</code>, <code>READY</code>, <code>ACK</code>, <code>EOF</code>, <code>DELETE</code>, <code>BATCH</code> in <code>network/src/main/java/com/messaging/network/legacy/events/EventType.java</code></li><li>Topic source: producer payload, pipe <code>MessageRecord.topic</code>, or legacy service mapping in <code>broker/src/main/resources/application.yml</code></li><li>Known configured POS topics: <a href=\"#/chapter/11-runtime-config/legacy-service-topic-map\">Runtime configuration</a></li></ul>\n<h2 id=\"database-tables-and-persistent-entities\" data-heading=\"Database Tables And Persistent Entities\">Database Tables And Persistent Entities<a class=\"heading-link\" href=\"#/chapter/00-index/database-tables-and-persistent-entities\" aria-label=\"Link to Database Tables And Persistent Entities\">#</a></h2>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Store/entity</th>\n<th>Location</th>\n<th>Details</th>\n</tr></thead><tbody>\n<tr>\n<td><code>segment_metadata</code> SQLite table</td>\n<td>Per topic: <code>&lt;dataDir&gt;/&lt;topic&gt;/segment_metadata.db</code></td>\n<td><a href=\"#/chapter/05-data-model/sqlite-segment-metadata\">Data model</a></td>\n</tr>\n<tr>\n<td>Segment log/index files</td>\n<td><code>&lt;dataDir&gt;/&lt;topic&gt;/partition-0/</code></td>\n<td><a href=\"#/chapter/05-data-model/segment-storage\">Data model</a></td>\n</tr>\n<tr>\n<td>ACK records</td>\n<td>RocksDB default column family</td>\n<td><a href=\"#/chapter/05-data-model/rocksdb\">Data model</a></td>\n</tr>\n<tr>\n<td>Compaction latest-key/checkpoint data</td>\n<td>RocksDB <code>compaction</code> column family</td>\n<td><a href=\"#/chapter/08-compaction-handling/persistent-index-and-checkpoints\">Compaction</a></td>\n</tr>\n<tr>\n<td>Consumer offsets</td>\n<td><code>consumer-offsets.properties</code></td>\n<td><a href=\"#/chapter/05-data-model/properties-state-files\">Data model</a></td>\n</tr>\n<tr>\n<td>Refresh state</td>\n<td><code>data-refresh-state.properties</code></td>\n<td><a href=\"#/chapter/07-pos-machine-state/persisted-refresh-state\">POS state</a></td>\n</tr>\n<tr>\n<td>Pipe offset</td>\n<td><code>pipe-offset.properties</code></td>\n<td><a href=\"#/chapter/05-data-model/properties-state-files\">Data model</a></td>\n</tr>\n<tr>\n<td>Topology</td>\n<td><code>topology.properties</code></td>\n<td><a href=\"#/chapter/11-runtime-config/topology-and-pipe\">Runtime config</a></td>\n</tr>\n</tbody></table></div>\n<p>No SQL migration files were found. The SQLite schema is created at runtime by <code>storage/src/main/java/com/messaging/storage/metadata/SegmentMetadataStore.java</code>.</p>\n<h2 id=\"important-classes\" data-heading=\"Important Classes\">Important Classes<a class=\"heading-link\" href=\"#/chapter/00-index/important-classes\" aria-label=\"Link to Important Classes\">#</a></h2>\n<ul><li>Orchestration: <code>BrokerService</code>, <code>TopologyManager</code>, <code>RefreshCoordinator</code>, <code>ConsumerRegistry</code></li><li>Ingress: <code>DataHandler</code>, <code>HttpPipeConnector</code>, <code>PipeServer</code></li><li>Storage: <code>Segment</code>, <code>SegmentManager</code>, <code>FileChannelStorageEngine</code>, <code>DefaultStorageRecoveryService</code></li><li>Delivery: <code>DeliveryScheduler</code>, <code>TopicFairScheduler</code>, <code>BatchDeliveryService</code>, <code>BatchAckService</code></li><li>State: <code>ConsumerOffsetTracker</code>, <code>InMemoryInFlightDeliveryStore</code>, <code>RefreshStateStore</code></li><li>Network: <code>NettyTcpServer</code>, <code>NettyTcpClient</code>, <code>ZeroCopyBatchDecoder</code>, <code>ProtocolDetectionDecoder</code></li><li>Compaction: <code>CompactionIndex</code>, <code>CompactionPlanner</code>, <code>CompactionRewriter</code>, <code>CompactionScheduler</code></li><li>Client: <code>ClientConsumerManager</code></li></ul>\n<p>Package ownership and paths are in <a href=\"#/chapter/02-module-map\">Module map</a>.</p>\n<h2 id=\"test-suites\" data-heading=\"Test Suites\">Test Suites<a class=\"heading-link\" href=\"#/chapter/00-index/test-suites\" aria-label=\"Link to Test Suites\">#</a></h2>\n<ul><li>Unit: <code>./gradlew test</code></li><li>Integration: <code>./gradlew integrationTest</code></li><li>Journey/end-to-end in process: <code>./gradlew :broker:journeyTest</code></li><li>Separate-process black-box: <code>./gradlew :broker:systemTest</code></li><li>Performance source sets: <code>./gradlew performanceTest</code> (currently no source)</li><li>Build verification: <code>./gradlew build</code></li><li>Static analysis: Qodana configuration exists, but no local Qodana CLI or Gradle lint task was found.</li></ul>\n<p>Results and every suite are indexed in <a href=\"#/chapter/10-test-map\">Test map</a>.</p>\n<h2 id=\"debugging-entries\" data-heading=\"Debugging Entries\">Debugging Entries<a class=\"heading-link\" href=\"#/chapter/00-index/debugging-entries\" aria-label=\"Link to Debugging Entries\">#</a></h2>\n<ul><li>Broker does not start: <a href=\"#/chapter/12-debugging-guide/broker-does-not-start\">Startup failures</a></li><li>Consumer receives no data: <a href=\"#/chapter/12-debugging-guide/consumer-receives-no-data\">Delivery stalls</a></li><li>Pending ACK never clears: <a href=\"#/chapter/12-debugging-guide/pending-ack-never-clears\">ACK stalls</a></li><li>Refresh stuck: <a href=\"#/chapter/12-debugging-guide/refresh-is-stuck\">Refresh debugging</a></li><li>Pipe not advancing: <a href=\"#/chapter/12-debugging-guide/pipe-is-not-advancing\">Pipe debugging</a></li><li>Compaction stalls or loses delivery: <a href=\"#/chapter/12-debugging-guide/compaction-problems\">Compaction debugging</a></li><li>Legacy client not registering: <a href=\"#/chapter/12-debugging-guide/legacy-client-does-not-register\">Legacy registration</a></li><li>Disk/memory/CPU pressure: <a href=\"#/chapter/12-debugging-guide/resource-pressure\">Resource debugging</a></li></ul>\n<p>Related: <a href=\"#/chapter/13-risk-and-edge-cases\">Risks</a>, <a href=\"#/chapter/15-open-questions\">Open questions</a>.</p>","headings":[{"level":2,"title":"Chapters","id":"chapters"},{"level":2,"title":"Features","id":"features"},{"level":2,"title":"APIs","id":"apis"},{"level":3,"title":"HTTP","id":"http"},{"level":3,"title":"TCP protocol","id":"tcp-protocol"},{"level":2,"title":"Topics And Events","id":"topics-and-events"},{"level":2,"title":"Database Tables And Persistent Entities","id":"database-tables-and-persistent-entities"},{"level":2,"title":"Important Classes","id":"important-classes"},{"level":2,"title":"Test Suites","id":"test-suites"},{"level":2,"title":"Debugging Entries","id":"debugging-entries"}],"plainText":"Codebase Book Index Repository: kafka-like-messaging Verified against the working tree on 2026-06-11. This book is a source-backed map of the repository. Start with the chapter that owns the behavior, then follow its links to the API, data, event, state, test, and debugging chapters. Chapters System overview Module map Feature catalog API catalog Data model Event and Kafka-like flow POS machine state Compaction handling Error, retry, and recovery Test map Runtime configuration Debugging guide Risks and edge cases Glossary Open questions Features Feature Primary chapter Entry point --- --- --- Broker startup and shutdown Overview broker/src/main/java/com/messaging/broker/core/BrokerService.java Producer ingestion Features broker/src/main/java/com/messaging/broker/handler/DataHandler.java Parent-to-child pipe replication Features broker/src/main/java/com/messaging/broker/core/TopologyManager.java Segment storage and recovery Data model storage/src/main/java/com/messaging/storage/segment/SegmentManager.java Modern consumer subscription Features broker/src/main/java/com/messaging/broker/handler/SubscribeHandler.java Zero-copy batch delivery Events broker/src/main/java/com/messaging/broker/consumer/BatchDeliveryService.java Legacy multi-topic delivery Features broker/src/main/java/com/messaging/broker/legacy/LegacyConsumerDeliveryManager.java Offset and per-record ACK persistence Features broker/src/main/java/com/messaging/broker/consumer/BatchAckService.java POS refresh/reset/replay/ready POS state broker/src/main/java/com/messaging/broker/consumer/RefreshCoordinator.java Log compaction Compaction broker/src/main/java/com/messaging/broker/compaction/CompactionScheduler.java Embedded annotation consumers Features broker/src/main/java/com/messaging/broker/consumer/ConsumerAnnotationProcessor.java Client auto-discovery and reconnect Features client/src/main/java/com/messaging/client/ClientConsumerManager.java Monitoring and diagnostics API catalog broker/src/main/java/com/messaging/broker/http/ThreadDiagnosticsController.java Test-data ingestion API catalog broker/src/main/java/com/messaging/broker/http/TestDataController.java APIs HTTP GET /pipe/poll: Pipe API POST /admin/refresh-topic: Refresh API GET /admin/refresh-status: Refresh API GET /admin/refresh-current: Refresh API POST /admin/compaction/trigger: Compaction API GET /admin/compaction/status: Compaction API /admin/logging/: Runtime logging API /diagnostics/threads/: Diagnostics API /test/: Test-data API /health, /prometheus, /metrics: Management endpoints TCP protocol DATA, ACK, SUBSCRIBE, COMMITOFFSET RESET, READY, DISCONNECT, HEARTBEAT BATCHHEADER, BATCHACK, RESETACK, READYACK See Event and Kafka-like flow. Definitions are in common/src/main/java/com/messaging/common/model/BrokerMessage.java. Topics And Events This repository does not use Apache Kafka libraries or a Kafka broker. It implements Kafka-like topics, offsets, consumer groups, ACKs, and compaction itself. See Event and Kafka-like flow. Record events: MESSAGE, DELETE in common/src/main/java/com/messaging/common/model/EventType.java Legacy events: REGISTER, MESSAGE, RESET, READY, ACK, EOF, DELETE, BATCH in network/src/main/java/com/messaging/network/legacy/events/EventType.java Topic source: producer payload, pipe MessageRecord.topic, or legacy service mapping in broker/src/main/resources/application.yml Known configured POS topics: Runtime configuration Database Tables And Persistent Entities Store/entity Location Details --- --- --- segmentmetadata SQLite table Per topic: <dataDir>/<topic>/segmentmetadata.db Data model Segment log/index files <dataDir>/<topic>/partition-0/ Data model ACK records RocksDB default column family Data model Compaction latest-key/checkpoint data RocksDB compaction column family Compaction Consumer offsets consumer-offsets.properties Data model Refresh state data-refresh-state.properties POS state Pipe offset pipe-offset.properties Data model Topology topology.properties Runtime config No SQL migration files were found. The SQLite schema is created at runtime by storage/src/main/java/com/messaging/storage/metadata/SegmentMetadataStore.java. Important Classes Orchestration: BrokerService, TopologyManager, RefreshCoordinator, ConsumerRegistry Ingress: DataHandler, HttpPipeConnector, PipeServer Storage: Segment, SegmentManager, FileChannelStorageEngine, DefaultStorageRecoveryService Delivery: DeliveryScheduler, TopicFairScheduler, BatchDeliveryService, BatchAckService State: ConsumerOffsetTracker, InMemoryInFlightDeliveryStore, RefreshStateStore Network: NettyTcpServer, NettyTcpClient, ZeroCopyBatchDecoder, ProtocolDetectionDecoder Compaction: CompactionIndex, CompactionPlanner, CompactionRewriter, CompactionScheduler Client: ClientConsumerManager Package ownership and paths are in Module map. Test Suites Unit: ./gradlew test Integration: ./gradlew integrationTest Journey/end-to-end in process: ./gradlew :broker:journeyTest Separate-process black-box: ./gradlew :broker:systemTest Performance source sets: ./gradlew performanceTest (currently no source) Build verification: ./gradlew build Static analysis: Qodana configuration exists, but no local Qodana CLI or Gradle lint task was found. Results and every suite are indexed in Test map. Debugging Entries Broker does not start: Startup failures Consumer receives no data: Delivery stalls Pending ACK never clears: ACK stalls Refresh stuck: Refresh debugging Pipe not advancing: Pipe debugging Compaction stalls or loses delivery: Compaction debugging Legacy client not registering: Legacy registration Disk/memory/CPU pressure: Resource debugging Related: Risks, Open questions.","lineCount":129},{"order":1,"file":"01-system-overview.md","slug":"01-system-overview","title":"System Overview","html":"<h1 id=\"system-overview\" data-heading=\"System Overview\">System Overview<a class=\"heading-link\" href=\"#/chapter/01-system-overview/system-overview\" aria-label=\"Link to System Overview\">#</a></h1>\n<p>Related: <a href=\"#/chapter/02-module-map\">Module map</a>, <a href=\"#/chapter/03-feature-catalog\">Features</a>, <a href=\"#/chapter/06-event-kafka-flow\">Events</a>, <a href=\"#/chapter/11-runtime-config\">Runtime config</a>, <a href=\"#/chapter/13-risk-and-edge-cases\">Risks</a>.</p>\n<h2 id=\"purpose\" data-heading=\"Purpose\">Purpose<a class=\"heading-link\" href=\"#/chapter/01-system-overview/purpose\" aria-label=\"Link to Purpose\">#</a></h2>\n<p>The repository implements a store-oriented, Kafka-like messaging provider. It owns topic storage, offsets, consumer groups, binary delivery, legacy protocol compatibility, parent-cloud replication, refresh orchestration, ACK auditing, and log compaction. It does not depend on Apache Kafka. The module list is declared in <code>settings.gradle</code>; runtime wiring starts in <code>broker/src/main/java/com/messaging/broker/Application.java</code>.</p>\n<p>The dominant deployment model is a broker running near POS consumers:</p>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>Cloud registry/topology\n        |\n        v\nParent HTTP pipe ----&gt; Local broker storage ----&gt; POS/service consumers\n                             ^                         |\n                             |                         v\n                      TCP producers             ACK / RESET / READY</code></pre></div>\n<p>Sources:</p>\n<ul><li>Parent discovery: <code>broker/src/main/java/com/messaging/broker/core/CloudRegistryClient.java</code></li><li>Parent lifecycle: <code>broker/src/main/java/com/messaging/broker/core/TopologyManager.java</code></li><li>Pipe polling: <code>pipe/src/main/java/com/messaging/pipe/HttpPipeConnector.java</code></li><li>Local storage: <code>storage/src/main/java/com/messaging/storage/filechannel/FileChannelStorageEngine.java</code></li><li>Consumer orchestration: <code>broker/src/main/java/com/messaging/broker/consumer/ConsumerRegistry.java</code></li><li>Client library: <code>client/src/main/java/com/messaging/client/ClientConsumerManager.java</code></li></ul>\n<h2 id=\"architectural-style\" data-heading=\"Architectural Style\">Architectural Style<a class=\"heading-link\" href=\"#/chapter/01-system-overview/architectural-style\" aria-label=\"Link to Architectural Style\">#</a></h2>\n<ul><li>Java 17 source compatibility, Gradle multi-project build, Micronaut 4.2.1: <code>build.gradle</code></li><li>Netty TCP data plane with a custom binary protocol: <code>network/src/main/java/com/messaging/network/tcp/NettyTcpServer.java</code></li><li>Micronaut HTTP control and pipe plane: <code>broker/src/main/java/com/messaging/broker/http/</code>, <code>pipe/src/main/java/com/messaging/pipe/PipeServer.java</code></li><li>Append-only segmented local logs plus dense indexes: <code>storage/src/main/java/com/messaging/storage/segment/Segment.java</code></li><li>SQLite segment metadata: <code>storage/src/main/java/com/messaging/storage/metadata/SegmentMetadataStore.java</code></li><li>RocksDB ACK and compaction state: <code>broker/src/main/java/com/messaging/broker/compaction/SharedRocksDb.java</code></li><li>Properties files for small operational state: <code>broker/src/main/java/com/messaging/broker/consumer/PropertiesFileStore.java</code></li><li>Scheduled polling rather than push notification for delivery: <code>broker/src/main/java/com/messaging/broker/consumer/DeliveryScheduler.java</code></li></ul>\n<h2 id=\"core-data-flow\" data-heading=\"Core Data Flow\">Core Data Flow<a class=\"heading-link\" href=\"#/chapter/01-system-overview/core-data-flow\" aria-label=\"Link to Core Data Flow\">#</a></h2>\n<ol><li>A producer sends <code>DATA</code>, or a parent returns <code>MessageRecord</code> objects from the pipe.</li><li>The broker appends each record to topic partition <code>0</code>.</li><li>The compaction index records the latest offset for non-null message keys.</li><li>Adaptive delivery gates on consumer readiness, refresh state, storage watermark, in-flight state, and pending ACK.</li><li>The storage layer returns a file-backed <code>DeliveryBatch</code>; the network layer sends a <code>BATCH_HEADER</code> followed by raw segment bytes.</li><li>The client decoder parses the batch and automatically emits <code>BATCH_ACK</code>.</li><li>The broker commits the next delivery offset and asynchronously writes per-record ACK entries.</li></ol>\n<p>Sources: <code>broker/src/main/java/com/messaging/broker/handler/DataHandler.java</code>, <code>broker/src/main/java/com/messaging/broker/core/BrokerService.java</code>, <code>broker/src/main/java/com/messaging/broker/consumer/BatchDeliveryService.java</code>, <code>network/src/main/java/com/messaging/network/codec/ZeroCopyBatchDecoder.java</code>, <code>broker/src/main/java/com/messaging/broker/consumer/BatchAckService.java</code>.</p>\n<p>See <a href=\"#/chapter/06-event-kafka-flow\">Event flow</a> and <a href=\"#/chapter/05-data-model\">Data model</a>.</p>\n<h2 id=\"startup-and-shutdown\" data-heading=\"Startup And Shutdown\">Startup And Shutdown<a class=\"heading-link\" href=\"#/chapter/01-system-overview/startup-and-shutdown\" aria-label=\"Link to Startup And Shutdown\">#</a></h2>\n<p><code>BrokerService.onApplicationEvent</code> performs:</p>\n<ol><li><code>storage.recover()</code>; recovery failure aborts startup.</li><li>Optional ACK-store seeding; failure is logged and startup continues.</li><li>Network message and disconnect handler registration.</li><li>TCP server startup.</li><li>Embedded annotation-consumer delivery startup.</li><li>Adaptive remote-consumer delivery startup.</li><li>Topology manager and parent pipe startup.</li></ol>\n<p>Source: <code>broker/src/main/java/com/messaging/broker/core/BrokerService.java</code>.</p>\n<p>Handler registration is separately performed on <code>StartupEvent</code> by <code>broker/src/main/java/com/messaging/broker/config/HandlerInitializer.java</code>. This registers <code>DATA</code>, <code>SUBSCRIBE</code>, <code>COMMIT_OFFSET</code>, <code>RESET_ACK</code>, <code>READY_ACK</code>, and <code>BATCH_ACK</code>.</p>\n<p>Shutdown order is network ingress, adaptive delivery, topology/pipe, embedded delivery, shared executors, and storage. Sources: <code>BrokerService.java</code> and <code>broker/src/main/java/com/messaging/broker/core/ShutdownCoordinator.java</code>.</p>\n<h2 id=\"consistency-model\" data-heading=\"Consistency Model\">Consistency Model<a class=\"heading-link\" href=\"#/chapter/01-system-overview/consistency-model\" aria-label=\"Link to Consistency Model\">#</a></h2>\n<ul><li>Segment append order is protected by a fair append lock in <code>storage/src/main/java/com/messaging/storage/segment/SegmentManager.java</code>.</li><li>Consumer delivery state is keyed by <code>group:topic</code>, not ephemeral socket address: <code>broker/src/main/java/com/messaging/broker/model/DeliveryKey.java</code>.</li><li>Modern offsets use next-to-deliver semantics in <code>broker/src/main/java/com/messaging/broker/consumer/BatchDeliveryService.java</code>.</li><li>Legacy delivery stores the last acknowledged offset per topic in <code>broker/src/main/java/com/messaging/broker/legacy/LegacyConsumerDeliveryManager.java</code>.</li><li>ACK processing is at-least-once oriented: an ACK timeout reverts the in-memory offset; a lost persisted offset causes replay, not omission.</li><li>Pipe progress advances only after the broker handler returns success: <code>pipe/src/main/java/com/messaging/pipe/HttpPipeConnector.java</code>.</li><li>Compaction preserves original offsets and filters stale records before physical rewrite: <code>broker/src/main/java/com/messaging/broker/compaction/CompactionRewriter.java</code>.</li></ul>\n<p>The mixed modern/legacy offset semantics are an important maintenance risk. See <a href=\"#/chapter/13-risk-and-edge-cases/offset-semantics\">Risks</a>.</p>\n<h2 id=\"concurrency-and-resource-model\" data-heading=\"Concurrency And Resource Model\">Concurrency And Resource Model<a class=\"heading-link\" href=\"#/chapter/01-system-overview/concurrency-and-resource-model\" aria-label=\"Link to Concurrency And Resource Model\">#</a></h2>\n<ul><li>Netty boss/worker pools: <code>network/src/main/java/com/messaging/network/tcp/NettyTcpServer.java</code></li><li>Per-topic fair delivery scheduler: <code>broker/src/main/java/com/messaging/broker/consumer/TopicFairScheduler.java</code></li><li>Dedicated ACK, ACK-storage, storage-read, compaction, registry, refresh, consumer, and flush executors: <code>broker/src/main/java/com/messaging/broker/config/ExecutorFactory.java</code></li><li>One shared client-side Netty event loop, but one connection per <code>topic:group</code>: <code>client/src/main/java/com/messaging/client/ClientConsumerManager.java</code></li><li>Pipe and topology each own scheduler/executor lifecycle: <code>pipe/src/main/java/com/messaging/pipe/HttpPipeConnector.java</code>, <code>TopologyManager.java</code></li></ul>\n<p>Performance guardrails are implemented through bounded executor queues, per-topic semaphores, batch byte limits, storage-read/send timeouts, compaction CPU/heap guards, chunked ACK replay, streaming pipe parsing, and page-cache advisory calls. Sources are detailed in <a href=\"#/chapter/11-runtime-config\">Runtime config</a> and <a href=\"#/chapter/08-compaction-handling\">Compaction</a>.</p>\n<h2 id=\"external-integrations\" data-heading=\"External Integrations\">External Integrations<a class=\"heading-link\" href=\"#/chapter/01-system-overview/external-integrations\" aria-label=\"Link to External Integrations\">#</a></h2>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Integration</th>\n<th>Protocol</th>\n<th>Source</th>\n</tr></thead><tbody>\n<tr>\n<td>Cloud registry</td>\n<td>HTTP GET topology</td>\n<td><code>broker/src/main/java/com/messaging/broker/core/CloudRegistryClient.java</code></td>\n</tr>\n<tr>\n<td>Parent/cloud broker</td>\n<td>HTTP streaming poll</td>\n<td><code>pipe/src/main/java/com/messaging/pipe/HttpPipeConnector.java</code></td>\n</tr>\n<tr>\n<td>POS/service consumers</td>\n<td>Modern or legacy TCP</td>\n<td><code>network/src/main/java/com/messaging/network/tcp/NettyTcpServer.java</code></td>\n</tr>\n<tr>\n<td>Embedded modern consumers</td>\n<td>TCP client library</td>\n<td><code>client/src/main/java/com/messaging/client/ClientConsumerManager.java</code></td>\n</tr>\n<tr>\n<td>SQLite test import</td>\n<td>JDBC file URL</td>\n<td><code>broker/src/main/java/com/messaging/broker/http/TestDataController.java</code></td>\n</tr>\n<tr>\n<td>Prometheus</td>\n<td>Micronaut management</td>\n<td><code>broker/src/main/resources/application.yml</code></td>\n</tr>\n<tr>\n<td>Linux page cache advice</td>\n<td>JNA <code>posix_fadvise</code></td>\n<td><code>storage/src/main/java/com/messaging/storage/filechannel/FileChannelStorageEngine.java</code></td>\n</tr>\n</tbody></table></div>\n<h2 id=\"security-boundary\" data-heading=\"Security Boundary\">Security Boundary<a class=\"heading-link\" href=\"#/chapter/01-system-overview/security-boundary\" aria-label=\"Link to Security Boundary\">#</a></h2>\n<p><code>common/src/main/java/com/messaging/common/http/AuthTokenClientFilter.java</code> only adds credentials to outbound Micronaut HTTP requests when <code>broker.http.auth.token</code> is configured. The main YAML instead defines <code>broker.registry.auth.bearer-token</code>; no code mapping between these keys was found.</p>\n<p>No inbound HTTP authentication filter or <code>@Secured</code> controller annotation was found. The repository does configure Micronaut management endpoint sensitivity in <code>broker/src/main/resources/application.yml</code>, but enforcement is <strong>not confirmed from code</strong>. Treat admin, diagnostics, and test endpoints as requiring network-layer protection until verified.</p>\n<p>See <a href=\"#/chapter/11-runtime-config/security-and-authentication\">Runtime config</a> and <a href=\"#/chapter/13-risk-and-edge-cases/security\">Risks</a>.</p>","headings":[{"level":2,"title":"Purpose","id":"purpose"},{"level":2,"title":"Architectural Style","id":"architectural-style"},{"level":2,"title":"Core Data Flow","id":"core-data-flow"},{"level":2,"title":"Startup And Shutdown","id":"startup-and-shutdown"},{"level":2,"title":"Consistency Model","id":"consistency-model"},{"level":2,"title":"Concurrency And Resource Model","id":"concurrency-and-resource-model"},{"level":2,"title":"External Integrations","id":"external-integrations"},{"level":2,"title":"Security Boundary","id":"security-boundary"}],"plainText":"System Overview Related: Module map, Features, Events, Runtime config, Risks. Purpose The repository implements a store-oriented, Kafka-like messaging provider. It owns topic storage, offsets, consumer groups, binary delivery, legacy protocol compatibility, parent-cloud replication, refresh orchestration, ACK auditing, and log compaction. It does not depend on Apache Kafka. The module list is declared in settings.gradle; runtime wiring starts in broker/src/main/java/com/messaging/broker/Application.java. The dominant deployment model is a broker running near POS consumers: Sources: Parent discovery: broker/src/main/java/com/messaging/broker/core/CloudRegistryClient.java Parent lifecycle: broker/src/main/java/com/messaging/broker/core/TopologyManager.java Pipe polling: pipe/src/main/java/com/messaging/pipe/HttpPipeConnector.java Local storage: storage/src/main/java/com/messaging/storage/filechannel/FileChannelStorageEngine.java Consumer orchestration: broker/src/main/java/com/messaging/broker/consumer/ConsumerRegistry.java Client library: client/src/main/java/com/messaging/client/ClientConsumerManager.java Architectural Style Java 17 source compatibility, Gradle multi-project build, Micronaut 4.2.1: build.gradle Netty TCP data plane with a custom binary protocol: network/src/main/java/com/messaging/network/tcp/NettyTcpServer.java Micronaut HTTP control and pipe plane: broker/src/main/java/com/messaging/broker/http/, pipe/src/main/java/com/messaging/pipe/PipeServer.java Append-only segmented local logs plus dense indexes: storage/src/main/java/com/messaging/storage/segment/Segment.java SQLite segment metadata: storage/src/main/java/com/messaging/storage/metadata/SegmentMetadataStore.java RocksDB ACK and compaction state: broker/src/main/java/com/messaging/broker/compaction/SharedRocksDb.java Properties files for small operational state: broker/src/main/java/com/messaging/broker/consumer/PropertiesFileStore.java Scheduled polling rather than push notification for delivery: broker/src/main/java/com/messaging/broker/consumer/DeliveryScheduler.java Core Data Flow A producer sends DATA, or a parent returns MessageRecord objects from the pipe. The broker appends each record to topic partition 0. The compaction index records the latest offset for non-null message keys. Adaptive delivery gates on consumer readiness, refresh state, storage watermark, in-flight state, and pending ACK. The storage layer returns a file-backed DeliveryBatch; the network layer sends a BATCHHEADER followed by raw segment bytes. The client decoder parses the batch and automatically emits BATCHACK. The broker commits the next delivery offset and asynchronously writes per-record ACK entries. Sources: broker/src/main/java/com/messaging/broker/handler/DataHandler.java, broker/src/main/java/com/messaging/broker/core/BrokerService.java, broker/src/main/java/com/messaging/broker/consumer/BatchDeliveryService.java, network/src/main/java/com/messaging/network/codec/ZeroCopyBatchDecoder.java, broker/src/main/java/com/messaging/broker/consumer/BatchAckService.java. See Event flow and Data model. Startup And Shutdown BrokerService.onApplicationEvent performs: storage.recover(); recovery failure aborts startup. Optional ACK-store seeding; failure is logged and startup continues. Network message and disconnect handler registration. TCP server startup. Embedded annotation-consumer delivery startup. Adaptive remote-consumer delivery startup. Topology manager and parent pipe startup. Source: broker/src/main/java/com/messaging/broker/core/BrokerService.java. Handler registration is separately performed on StartupEvent by broker/src/main/java/com/messaging/broker/config/HandlerInitializer.java. This registers DATA, SUBSCRIBE, COMMITOFFSET, RESETACK, READYACK, and BATCHACK. Shutdown order is network ingress, adaptive delivery, topology/pipe, embedded delivery, shared executors, and storage. Sources: BrokerService.java and broker/src/main/java/com/messaging/broker/core/ShutdownCoordinator.java. Consistency Model Segment append order is protected by a fair append lock in storage/src/main/java/com/messaging/storage/segment/SegmentManager.java. Consumer delivery state is keyed by group:topic, not ephemeral socket address: broker/src/main/java/com/messaging/broker/model/DeliveryKey.java. Modern offsets use next-to-deliver semantics in broker/src/main/java/com/messaging/broker/consumer/BatchDeliveryService.java. Legacy delivery stores the last acknowledged offset per topic in broker/src/main/java/com/messaging/broker/legacy/LegacyConsumerDeliveryManager.java. ACK processing is at-least-once oriented: an ACK timeout reverts the in-memory offset; a lost persisted offset causes replay, not omission. Pipe progress advances only after the broker handler returns success: pipe/src/main/java/com/messaging/pipe/HttpPipeConnector.java. Compaction preserves original offsets and filters stale records before physical rewrite: broker/src/main/java/com/messaging/broker/compaction/CompactionRewriter.java. The mixed modern/legacy offset semantics are an important maintenance risk. See Risks. Concurrency And Resource Model Netty boss/worker pools: network/src/main/java/com/messaging/network/tcp/NettyTcpServer.java Per-topic fair delivery scheduler: broker/src/main/java/com/messaging/broker/consumer/TopicFairScheduler.java Dedicated ACK, ACK-storage, storage-read, compaction, registry, refresh, consumer, and flush executors: broker/src/main/java/com/messaging/broker/config/ExecutorFactory.java One shared client-side Netty event loop, but one connection per topic:group: client/src/main/java/com/messaging/client/ClientConsumerManager.java Pipe and topology each own scheduler/executor lifecycle: pipe/src/main/java/com/messaging/pipe/HttpPipeConnector.java, TopologyManager.java Performance guardrails are implemented through bounded executor queues, per-topic semaphores, batch byte limits, storage-read/send timeouts, compaction CPU/heap guards, chunked ACK replay, streaming pipe parsing, and page-cache advisory calls. Sources are detailed in Runtime config and Compaction. External Integrations Integration Protocol Source --- --- --- Cloud registry HTTP GET topology broker/src/main/java/com/messaging/broker/core/CloudRegistryClient.java Parent/cloud broker HTTP streaming poll pipe/src/main/java/com/messaging/pipe/HttpPipeConnector.java POS/service consumers Modern or legacy TCP network/src/main/java/com/messaging/network/tcp/NettyTcpServer.java Embedded modern consumers TCP client library client/src/main/java/com/messaging/client/ClientConsumerManager.java SQLite test import JDBC file URL broker/src/main/java/com/messaging/broker/http/TestDataController.java Prometheus Micronaut management broker/src/main/resources/application.yml Linux page cache advice JNA posixfadvise storage/src/main/java/com/messaging/storage/filechannel/FileChannelStorageEngine.java Security Boundary common/src/main/java/com/messaging/common/http/AuthTokenClientFilter.java only adds credentials to outbound Micronaut HTTP requests when broker.http.auth.token is configured. The main YAML instead defines broker.registry.auth.bearer-token; no code mapping between these keys was found. No inbound HTTP authentication filter or @Secured controller annotation was found. The repository does configure Micronaut management endpoint sensitivity in broker/src/main/resources/application.yml, but enforcement is not confirmed from code. Treat admin, diagnostics, and test endpoints as requiring network-layer protection until verified. See Runtime config and Risks.","lineCount":114},{"order":2,"file":"02-module-map.md","slug":"02-module-map","title":"Module Map","html":"<h1 id=\"module-map\" data-heading=\"Module Map\">Module Map<a class=\"heading-link\" href=\"#/chapter/02-module-map/module-map\" aria-label=\"Link to Module Map\">#</a></h1>\n<p>Related: <a href=\"#/chapter/01-system-overview\">Overview</a>, <a href=\"#/chapter/03-feature-catalog\">Features</a>, <a href=\"#/chapter/10-test-map\">Tests</a>, <a href=\"#/chapter/11-runtime-config\">Runtime config</a>.</p>\n<h2 id=\"build-topology\" data-heading=\"Build Topology\">Build Topology<a class=\"heading-link\" href=\"#/chapter/02-module-map/build-topology\" aria-label=\"Link to Build Topology\">#</a></h2>\n<p>Modules are declared in <code>settings.gradle</code>:</p>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>common &lt;- storage\ncommon &lt;- network\ncommon + storage &lt;- pipe\ncommon + storage + network + pipe &lt;- broker\ncommon + network &lt;- client\ncommon + network + client &lt;- test-consumer\ntest-consumer &lt;- broker journey/system test classpaths</code></pre></div>\n<p>Dependency declarations are in each module's <code>build.gradle</code>.</p>\n<h2 id=\"common\" data-heading=\"common\"><code>common</code><a class=\"heading-link\" href=\"#/chapter/02-module-map/common\" aria-label=\"Link to common\">#</a></h2>\n<p>Purpose: stable contracts, annotations, models, exceptions, and outbound HTTP auth.</p>\n<ul><li>APIs: <code>common/src/main/java/com/messaging/common/api/</code><ul><li><code>StorageEngine</code>, <code>BatchReadableStorage</code>, <code>NetworkServer</code>, <code>NetworkClient</code>, <code>PipeConnector</code></li><li><code>MessageHandler</code>, <code>ErrorHandler</code>, <code>NoOpErrorHandler</code></li></ul></li><li>Models: <code>common/src/main/java/com/messaging/common/model/</code><ul><li><code>BrokerMessage</code>, <code>MessageRecord</code>, <code>ConsumerRecord</code>, <code>DeliveryBatch</code></li><li><code>ByteArrayDeliveryBatch</code>, <code>EventType</code>, <code>HealthStatus</code>, <code>TopologyResponse</code></li></ul></li><li>Consumer annotations: <code>common/src/main/java/com/messaging/common/annotation/</code></li><li>Exceptions: <code>common/src/main/java/com/messaging/common/exception/</code></li><li>Outbound token filter: <code>common/src/main/java/com/messaging/common/http/AuthTokenClientFilter.java</code></li></ul>\n<p>Tests: <code>common/src/unitTest/groovy/</code>, <code>common/src/integrationTest/groovy/</code>. See <a href=\"#/chapter/10-test-map/common\">Test map</a>.</p>\n<h2 id=\"storage\" data-heading=\"storage\"><code>storage</code><a class=\"heading-link\" href=\"#/chapter/02-module-map/storage\" aria-label=\"Link to storage\">#</a></h2>\n<p>Purpose: append-only topic storage, segment/index lifecycle, crash recovery, watermarks, and SQLite metadata.</p>\n<ul><li>File-channel engine: <code>storage/src/main/java/com/messaging/storage/filechannel/FileChannelStorageEngine.java</code></li><li>Alternate DI engine: <code>storage/src/main/java/com/messaging/storage/mmap/MMapStorageEngine.java</code></li><li>Segment core:<ul><li><code>storage/src/main/java/com/messaging/storage/segment/Segment.java</code></li><li><code>storage/src/main/java/com/messaging/storage/segment/SegmentManager.java</code></li><li><code>storage/src/main/java/com/messaging/storage/segment/DefaultStorageRecoveryService.java</code></li><li><code>storage/src/main/java/com/messaging/storage/segment/SegmentAccess.java</code></li></ul></li><li>Metadata:<ul><li><code>storage/src/main/java/com/messaging/storage/metadata/SegmentMetadata.java</code></li><li><code>storage/src/main/java/com/messaging/storage/metadata/SegmentMetadataStore.java</code></li><li><code>storage/src/main/java/com/messaging/storage/metadata/SegmentMetadataStoreFactory.java</code></li></ul></li><li>Watermarks: <code>storage/src/main/java/com/messaging/storage/watermark/StorageWatermarkTracker.java</code></li></ul>\n<p><code>MMapStorageEngine</code> delegates to the same <code>SegmentManager</code>/<code>Segment</code> implementation; the active segment implementation uses <code>FileChannel</code>. The class name does not prove memory mapping.</p>\n<p>Tests: <code>storage/src/unitTest/groovy/</code>, <code>storage/src/integrationTest/groovy/</code>. <code>storage/src/test/java/com/messaging/storage/mmap/MMapStorageEngineTest.java</code> is outside the custom unit source set and is not run by <code>:storage:test</code>. See <a href=\"#/chapter/15-open-questions\">Open questions</a>.</p>\n<h2 id=\"network\" data-heading=\"network\"><code>network</code><a class=\"heading-link\" href=\"#/chapter/02-module-map/network\" aria-label=\"Link to network\">#</a></h2>\n<p>Purpose: TCP server/client, modern codecs, zero-copy batch framing, protocol detection, legacy codecs, and network metrics.</p>\n<ul><li>TCP: <code>network/src/main/java/com/messaging/network/tcp/</code><ul><li><code>NettyTcpServer</code>, <code>NettyTcpClient</code></li></ul></li><li>Modern codecs: <code>network/src/main/java/com/messaging/network/codec/</code><ul><li><code>BinaryMessageDecoder</code>, <code>BinaryMessageEncoder</code></li><li><code>ZeroCopyBatchDecoder</code>, <code>BatchDecodedEvent</code>, client <code>BatchAckHandler</code></li></ul></li><li>Handlers: <code>network/src/main/java/com/messaging/network/handler/</code></li><li>Legacy compatibility: <code>network/src/main/java/com/messaging/network/legacy/</code><ul><li><code>ProtocolDetectionDecoder</code>, <code>DefaultProtocolDetectionService</code></li><li><code>LegacyEventDecoder</code>, <code>LegacyEventEncoder</code>, <code>LegacyConnectionState</code></li></ul></li><li>Legacy event model: <code>network/src/main/java/com/messaging/network/legacy/events/</code></li><li>Metrics: <code>network/src/main/java/com/messaging/network/metrics/</code></li></ul>\n<p>Tests: <code>network/src/unitTest/groovy/</code>, <code>network/src/integrationTest/groovy/</code>.</p>\n<h2 id=\"pipe\" data-heading=\"pipe\"><code>pipe</code><a class=\"heading-link\" href=\"#/chapter/02-module-map/pipe\" aria-label=\"Link to pipe\">#</a></h2>\n<p>Purpose: parent-to-child replication over HTTP and server-side polling.</p>\n<ul><li>Client connector: <code>pipe/src/main/java/com/messaging/pipe/HttpPipeConnector.java</code></li><li>Parent endpoint: <code>pipe/src/main/java/com/messaging/pipe/PipeServer.java</code></li><li>Metrics: <code>pipe/src/main/java/com/messaging/pipe/PipeMetrics.java</code></li></ul>\n<p>Tests: <code>pipe/src/unitTest/groovy/</code>, <code>pipe/src/integrationTest/groovy/</code>.</p>\n<h2 id=\"broker\" data-heading=\"broker\"><code>broker</code><a class=\"heading-link\" href=\"#/chapter/02-module-map/broker\" aria-label=\"Link to broker\">#</a></h2>\n<p>Purpose: application composition and nearly all business workflows.</p>\n<ul><li>Application and lifecycle:<ul><li><code>broker/src/main/java/com/messaging/broker/Application.java</code></li><li><code>broker/src/main/java/com/messaging/broker/core/BrokerService.java</code></li><li><code>broker/src/main/java/com/messaging/broker/core/ShutdownCoordinator.java</code></li></ul></li><li>Topology and replication: <code>broker/src/main/java/com/messaging/broker/core/</code></li><li>Protocol handlers: <code>broker/src/main/java/com/messaging/broker/handler/</code></li><li>Consumer lifecycle/delivery/refresh/state: <code>broker/src/main/java/com/messaging/broker/consumer/</code></li><li>ACK store and reconciliation: <code>broker/src/main/java/com/messaging/broker/ack/</code></li><li>Compaction: <code>broker/src/main/java/com/messaging/broker/compaction/</code></li><li>Legacy merge delivery: <code>broker/src/main/java/com/messaging/broker/legacy/</code></li><li>HTTP controllers: <code>broker/src/main/java/com/messaging/broker/http/</code></li><li>DTO/value types: <code>broker/src/main/java/com/messaging/broker/model/</code></li><li>Metrics/logging/health: <code>broker/src/main/java/com/messaging/broker/monitoring/</code></li><li>DI/executors/handler setup: <code>broker/src/main/java/com/messaging/broker/config/</code></li><li>Runtime configuration: <code>broker/src/main/resources/application.yml</code></li></ul>\n<p>Test source sets:</p>\n<ul><li>Unit: <code>broker/src/unitTest/groovy/</code></li><li>Integration: <code>broker/src/integrationTest/groovy/</code></li><li>In-process journey: <code>broker/src/systemTest/groovy/</code>, mapped to <code>journeyTest</code></li><li>Separate-process black-box: <code>broker/src/blackboxSystemTest/groovy/</code>, mapped to <code>systemTest</code></li></ul>\n<p>The unusual mapping is defined in <code>broker/build.gradle</code>.</p>\n<h2 id=\"client\" data-heading=\"client\"><code>client</code><a class=\"heading-link\" href=\"#/chapter/02-module-map/client\" aria-label=\"Link to client\">#</a></h2>\n<p>Purpose: embedded modern consumer framework.</p>\n<p>Only production class: <code>client/src/main/java/com/messaging/client/ClientConsumerManager.java</code>.</p>\n<p>It discovers <code>@Consumer</code> handlers, creates one TCP connection per <code>topic:group</code>, shares one Netty event loop, reconnects with exponential backoff, handles <code>RESET</code>/<code>READY</code>, and routes decoded batches to all handlers for a topic.</p>\n<p>Tests: <code>client/src/integrationTest/groovy/com/messaging/client/ClientConsumerManagerIntegrationSpec.groovy</code>.</p>\n<h2 id=\"test-consumer\" data-heading=\"test-consumer\"><code>test-consumer</code><a class=\"heading-link\" href=\"#/chapter/02-module-map/test-consumer\" aria-label=\"Link to test-consumer\">#</a></h2>\n<p>Purpose: runnable consumer process and journey/system-test fixture.</p>\n<ul><li>Application: <code>test-consumer/src/main/java/com/example/consumer/ConsumerApplication.java</code></li><li>Modern handler: <code>test-consumer/src/main/java/com/example/consumer/GenericConsumerHandler.java</code></li><li>Legacy direct connection/service: <code>test-consumer/src/main/java/com/example/consumer/service/LegacyConsumerService.java</code></li><li>Legacy protocol mirror: <code>test-consumer/src/main/java/com/example/consumer/legacy/</code></li><li>Consumer logging API: <code>test-consumer/src/main/java/com/example/consumer/http/LoggingController.java</code></li><li>Runtime config: <code>test-consumer/src/main/resources/application.yml</code></li></ul>\n<p>No direct test source is present; it is exercised from broker journey and black-box tests.</p>\n<h2 id=\"repository-level-assets\" data-heading=\"Repository-Level Assets\">Repository-Level Assets<a class=\"heading-link\" href=\"#/chapter/02-module-map/repository-level-assets\" aria-label=\"Link to Repository-Level Assets\">#</a></h2>\n<ul><li>Root build: <code>build.gradle</code>, <code>settings.gradle</code>, <code>gradle/</code>, <code>gradlew</code></li><li>Container: <code>Dockerfile</code></li><li>Static-analysis config: <code>qodana.yaml</code></li><li>Existing operating notes: <code>README.md</code>, <code>BUGS.md</code>, <code>TODO.md</code>, <code>CLAUDE.md</code></li><li>Audit documents: <code>docs/CLAUDE_MD_AUDIT_COMPARISON.md</code>, <code>docs/CONCURRENCY_AUDIT_COMPARISON.md</code></li><li>Monitoring dashboards: <code>html-dashboards/</code> is currently untracked in Git and was not modified.</li></ul>\n<p>Existing prose was used only as a navigation aid; behavior in this book is based on code and executed tests.</p>","headings":[{"level":2,"title":"Build Topology","id":"build-topology"},{"level":2,"title":"common","id":"common"},{"level":2,"title":"storage","id":"storage"},{"level":2,"title":"network","id":"network"},{"level":2,"title":"pipe","id":"pipe"},{"level":2,"title":"broker","id":"broker"},{"level":2,"title":"client","id":"client"},{"level":2,"title":"test-consumer","id":"test-consumer"},{"level":2,"title":"Repository-Level Assets","id":"repository-level-assets"}],"plainText":"Module Map Related: Overview, Features, Tests, Runtime config. Build Topology Modules are declared in settings.gradle: Dependency declarations are in each module's build.gradle. common Purpose: stable contracts, annotations, models, exceptions, and outbound HTTP auth. APIs: common/src/main/java/com/messaging/common/api/ StorageEngine, BatchReadableStorage, NetworkServer, NetworkClient, PipeConnector MessageHandler, ErrorHandler, NoOpErrorHandler Models: common/src/main/java/com/messaging/common/model/ BrokerMessage, MessageRecord, ConsumerRecord, DeliveryBatch ByteArrayDeliveryBatch, EventType, HealthStatus, TopologyResponse Consumer annotations: common/src/main/java/com/messaging/common/annotation/ Exceptions: common/src/main/java/com/messaging/common/exception/ Outbound token filter: common/src/main/java/com/messaging/common/http/AuthTokenClientFilter.java Tests: common/src/unitTest/groovy/, common/src/integrationTest/groovy/. See Test map. storage Purpose: append-only topic storage, segment/index lifecycle, crash recovery, watermarks, and SQLite metadata. File-channel engine: storage/src/main/java/com/messaging/storage/filechannel/FileChannelStorageEngine.java Alternate DI engine: storage/src/main/java/com/messaging/storage/mmap/MMapStorageEngine.java Segment core: storage/src/main/java/com/messaging/storage/segment/Segment.java storage/src/main/java/com/messaging/storage/segment/SegmentManager.java storage/src/main/java/com/messaging/storage/segment/DefaultStorageRecoveryService.java storage/src/main/java/com/messaging/storage/segment/SegmentAccess.java Metadata: storage/src/main/java/com/messaging/storage/metadata/SegmentMetadata.java storage/src/main/java/com/messaging/storage/metadata/SegmentMetadataStore.java storage/src/main/java/com/messaging/storage/metadata/SegmentMetadataStoreFactory.java Watermarks: storage/src/main/java/com/messaging/storage/watermark/StorageWatermarkTracker.java MMapStorageEngine delegates to the same SegmentManager/Segment implementation; the active segment implementation uses FileChannel. The class name does not prove memory mapping. Tests: storage/src/unitTest/groovy/, storage/src/integrationTest/groovy/. storage/src/test/java/com/messaging/storage/mmap/MMapStorageEngineTest.java is outside the custom unit source set and is not run by :storage:test. See Open questions. network Purpose: TCP server/client, modern codecs, zero-copy batch framing, protocol detection, legacy codecs, and network metrics. TCP: network/src/main/java/com/messaging/network/tcp/ NettyTcpServer, NettyTcpClient Modern codecs: network/src/main/java/com/messaging/network/codec/ BinaryMessageDecoder, BinaryMessageEncoder ZeroCopyBatchDecoder, BatchDecodedEvent, client BatchAckHandler Handlers: network/src/main/java/com/messaging/network/handler/ Legacy compatibility: network/src/main/java/com/messaging/network/legacy/ ProtocolDetectionDecoder, DefaultProtocolDetectionService LegacyEventDecoder, LegacyEventEncoder, LegacyConnectionState Legacy event model: network/src/main/java/com/messaging/network/legacy/events/ Metrics: network/src/main/java/com/messaging/network/metrics/ Tests: network/src/unitTest/groovy/, network/src/integrationTest/groovy/. pipe Purpose: parent-to-child replication over HTTP and server-side polling. Client connector: pipe/src/main/java/com/messaging/pipe/HttpPipeConnector.java Parent endpoint: pipe/src/main/java/com/messaging/pipe/PipeServer.java Metrics: pipe/src/main/java/com/messaging/pipe/PipeMetrics.java Tests: pipe/src/unitTest/groovy/, pipe/src/integrationTest/groovy/. broker Purpose: application composition and nearly all business workflows. Application and lifecycle: broker/src/main/java/com/messaging/broker/Application.java broker/src/main/java/com/messaging/broker/core/BrokerService.java broker/src/main/java/com/messaging/broker/core/ShutdownCoordinator.java Topology and replication: broker/src/main/java/com/messaging/broker/core/ Protocol handlers: broker/src/main/java/com/messaging/broker/handler/ Consumer lifecycle/delivery/refresh/state: broker/src/main/java/com/messaging/broker/consumer/ ACK store and reconciliation: broker/src/main/java/com/messaging/broker/ack/ Compaction: broker/src/main/java/com/messaging/broker/compaction/ Legacy merge delivery: broker/src/main/java/com/messaging/broker/legacy/ HTTP controllers: broker/src/main/java/com/messaging/broker/http/ DTO/value types: broker/src/main/java/com/messaging/broker/model/ Metrics/logging/health: broker/src/main/java/com/messaging/broker/monitoring/ DI/executors/handler setup: broker/src/main/java/com/messaging/broker/config/ Runtime configuration: broker/src/main/resources/application.yml Test source sets: Unit: broker/src/unitTest/groovy/ Integration: broker/src/integrationTest/groovy/ In-process journey: broker/src/systemTest/groovy/, mapped to journeyTest Separate-process black-box: broker/src/blackboxSystemTest/groovy/, mapped to systemTest The unusual mapping is defined in broker/build.gradle. client Purpose: embedded modern consumer framework. Only production class: client/src/main/java/com/messaging/client/ClientConsumerManager.java. It discovers @Consumer handlers, creates one TCP connection per topic:group, shares one Netty event loop, reconnects with exponential backoff, handles RESET/READY, and routes decoded batches to all handlers for a topic. Tests: client/src/integrationTest/groovy/com/messaging/client/ClientConsumerManagerIntegrationSpec.groovy. test-consumer Purpose: runnable consumer process and journey/system-test fixture. Application: test-consumer/src/main/java/com/example/consumer/ConsumerApplication.java Modern handler: test-consumer/src/main/java/com/example/consumer/GenericConsumerHandler.java Legacy direct connection/service: test-consumer/src/main/java/com/example/consumer/service/LegacyConsumerService.java Legacy protocol mirror: test-consumer/src/main/java/com/example/consumer/legacy/ Consumer logging API: test-consumer/src/main/java/com/example/consumer/http/LoggingController.java Runtime config: test-consumer/src/main/resources/application.yml No direct test source is present; it is exercised from broker journey and black-box tests. Repository-Level Assets Root build: build.gradle, settings.gradle, gradle/, gradlew Container: Dockerfile Static-analysis config: qodana.yaml Existing operating notes: README.md, BUGS.md, TODO.md, CLAUDE.md Audit documents: docs/CLAUDEMDAUDITCOMPARISON.md, docs/CONCURRENCYAUDITCOMPARISON.md Monitoring dashboards: html-dashboards/ is currently untracked in Git and was not modified. Existing prose was used only as a navigation aid; behavior in this book is based on code and executed tests.","lineCount":148},{"order":3,"file":"03-feature-catalog.md","slug":"03-feature-catalog","title":"Feature Catalog","html":"<h1 id=\"feature-catalog\" data-heading=\"Feature Catalog\">Feature Catalog<a class=\"heading-link\" href=\"#/chapter/03-feature-catalog/feature-catalog\" aria-label=\"Link to Feature Catalog\">#</a></h1>\n<p>Related: <a href=\"#/chapter/04-api-catalog\">APIs</a>, <a href=\"#/chapter/05-data-model\">Data model</a>, <a href=\"#/chapter/06-event-kafka-flow\">Events</a>, <a href=\"#/chapter/07-pos-machine-state\">POS state</a>, <a href=\"#/chapter/08-compaction-handling\">Compaction</a>, <a href=\"#/chapter/10-test-map\">Tests</a>.</p>\n<h2 id=\"producer-ingestion\" data-heading=\"Producer Ingestion\">Producer Ingestion<a class=\"heading-link\" href=\"#/chapter/03-feature-catalog/producer-ingestion\" aria-label=\"Link to Producer Ingestion\">#</a></h2>\n<ul><li><strong>Business purpose:</strong> Accept keyed <code>MESSAGE</code> or <code>DELETE</code> records from TCP producers.</li><li><strong>Entry point:</strong> <code>broker/src/main/java/com/messaging/broker/handler/DataHandler.java</code>.</li><li><strong>Main files:</strong> <code>HandlerInitializer.java</code>, <code>DataHandler.java</code>, <code>StorageEngine.java</code>, <code>CompactionIndex.java</code>.</li><li><strong>Flow:</strong> Decode JSON, require <code>topic</code>, default missing key/type/data, append partition <code>0</code>, update metrics and compaction index, send <code>ACK</code>.</li><li><strong>Data:</strong> <code>MessageRecord</code>; segment log/index; SQLite <code>segment_metadata</code>.</li><li><strong>Events:</strong> inbound <code>DATA</code>, outbound <code>ACK</code>; domain <code>MESSAGE</code>/<code>DELETE</code>.</li><li><strong>External systems:</strong> TCP client only.</li><li><strong>POS dependency:</strong> None directly; produced records feed POS consumers.</li><li><strong>Compaction:</strong> Every append calls <code>CompactionIndex.updateKey</code>.</li><li><strong>Failures:</strong> Invalid JSON/topic or storage error closes the connection; ACK send failure is logged asynchronously.</li><li><strong>Tests:</strong> <code>broker/src/integrationTest/groovy/com/messaging/broker/handler/DataHandlerIntegrationSpec.groovy</code>; parser/model unit specs.</li><li><strong>Coverage gaps:</strong> No explicit authorization or payload-size business validation test at the handler boundary was confirmed.</li><li><strong>Risks:</strong> Generated default keys prevent useful compaction; <code>event_type</code> values other than exact <code>DELETE</code> become <code>MESSAGE</code>.</li></ul>\n<p>See <a href=\"#/chapter/04-api-catalog/tcp-data-plane\">TCP API</a> and <a href=\"#/chapter/05-data-model/segment-storage\">Storage</a>.</p>\n<h2 id=\"topology-and-pipe-replication\" data-heading=\"Topology And Pipe Replication\">Topology And Pipe Replication<a class=\"heading-link\" href=\"#/chapter/03-feature-catalog/topology-and-pipe-replication\" aria-label=\"Link to Topology And Pipe Replication\">#</a></h2>\n<ul><li><strong>Business purpose:</strong> Discover a parent node and replicate cloud records into local storage.</li><li><strong>Entry point:</strong> <code>broker/src/main/java/com/messaging/broker/core/TopologyManager.java</code>.</li><li><strong>Main files:</strong> <code>CloudRegistryClient.java</code>, <code>TopologyManager.java</code>, <code>TopologyPropertiesStore.java</code>, <code>HttpPipeConnector.java</code>, <code>BrokerService.java</code>.</li><li><strong>Flow:</strong> Poll registry, choose the first parent, probe <code>/health</code>, connect pipe, stream <code>/pipe/poll</code>, invoke broker handler per record, persist the last successful offset.</li><li><strong>Data:</strong> <code>TopologyResponse</code>, <code>MessageRecord</code>, <code>topology.properties</code>, <code>pipe-offset.properties</code>.</li><li><strong>Events/topics:</strong> HTTP JSON records with topic and parent offsets; no Kafka client.</li><li><strong>External systems:</strong> Cloud registry and parent HTTP broker.</li><li><strong>POS dependency:</strong> Pipe pauses during refresh so the POS snapshot is replayed against stable local data.</li><li><strong>Compaction:</strong> Pipe records update the same compaction index as producer records.</li><li><strong>Failures:</strong> Registry/probe/poll failures retain or retry the previous state with adaptive delay; failed storage handling prevents offset advancement.</li><li><strong>Tests:</strong> <code>CloudRegistryClientIntegrationSpec</code>, <code>TopologyManagerProbeIntegrationSpec</code>, <code>HttpPipeConnectorIntegrationSpec</code>, <code>PipeOutageJourneySpec</code>.</li><li><strong>Coverage gaps:</strong> Multi-parent failover and topic selection across the pipe are not confirmed.</li><li><strong>Risks:</strong> <code>HttpPipeConnector</code> does not add a <code>topic</code> query parameter, while <code>PipeServer</code> defaults to <code>price-topic</code>; <code>PipeMessageForwarder.getCurrentOffset()</code> always returns <code>0</code>.</li></ul>\n<p>See <a href=\"#/chapter/04-api-catalog/pipe-api\">Pipe API</a>, <a href=\"#/chapter/11-runtime-config/topology-and-pipe\">Runtime config</a>, and <a href=\"#/chapter/13-risk-and-edge-cases/pipe-and-topology\">Risks</a>.</p>\n<h2 id=\"segment-storage-and-recovery\" data-heading=\"Segment Storage And Recovery\">Segment Storage And Recovery<a class=\"heading-link\" href=\"#/chapter/03-feature-catalog/segment-storage-and-recovery\" aria-label=\"Link to Segment Storage And Recovery\">#</a></h2>\n<ul><li><strong>Business purpose:</strong> Persist ordered topic records with bounded segment files and restart recovery.</li><li><strong>Entry point:</strong> <code>storage/src/main/java/com/messaging/storage/filechannel/FileChannelStorageEngine.java</code>.</li><li><strong>Main files:</strong> <code>Segment.java</code>, <code>SegmentManager.java</code>, <code>DefaultStorageRecoveryService.java</code>, <code>SegmentMetadataStore.java</code>.</li><li><strong>Flow:</strong> Create/recover topic manager, append binary record and dense index entry, roll full segments, periodically force active files, read by binary-searching index offsets.</li><li><strong>Data:</strong> <code>.log</code>, <code>.index</code>, <code>.compacted.log</code>, <code>.compacted.index</code>, SQLite <code>segment_metadata</code>.</li><li><strong>Events/topics:</strong> Topic and partition <code>0</code>; record offsets may be sparse when copied from a parent.</li><li><strong>External systems:</strong> Local filesystem, SQLite JDBC, Linux JNA cache advisory.</li><li><strong>POS dependency:</strong> All replay/readiness state ultimately depends on durable segment offsets.</li><li><strong>Compaction:</strong> Exposes <code>SegmentAccess</code> and atomic replacement APIs to the broker compactor.</li><li><strong>Failures:</strong> Recovery validates headers, truncates orphan log/index tails, ignores/deletes staging files, and fails startup for unrecoverable storage errors.</li><li><strong>Tests:</strong> Storage segment, crash recovery, concurrent read, memory-leak, engine, metadata, and recovery specs under <code>storage/src/</code>.</li><li><strong>Coverage gaps:</strong> Power-loss behavior on real POS filesystems and filesystems without atomic move is not confirmed.</li><li><strong>Risks:</strong> No per-record CRC in current v2 format; non-monotonic supplied offsets are warned but appended; <code>StorageEngine.compact()</code> and <code>validateStorage()</code> are placeholders.</li></ul>\n<p>See <a href=\"#/chapter/05-data-model\">Data model</a>.</p>\n<h2 id=\"modern-consumer-subscription-and-readiness\" data-heading=\"Modern Consumer Subscription And Readiness\">Modern Consumer Subscription And Readiness<a class=\"heading-link\" href=\"#/chapter/03-feature-catalog/modern-consumer-subscription-and-readiness\" aria-label=\"Link to Modern Consumer Subscription And Readiness\">#</a></h2>\n<ul><li><strong>Business purpose:</strong> Register one consumer group for one topic and block delivery until the client is ready.</li><li><strong>Entry point:</strong> <code>broker/src/main/java/com/messaging/broker/handler/SubscribeHandler.java</code>.</li><li><strong>Main files:</strong> <code>ConsumerRegistry.java</code>, <code>ConsumerRegistrationManager.java</code>, <code>ConsumerReadinessManager.java</code>, <code>ClientConsumerManager.java</code>.</li><li><strong>Flow:</strong> Parse <code>{topic,group}</code>, restore and validate <code>group:topic</code> offset, atomically register the socket session, ACK subscribe, send <code>READY</code>, wait for <code>READY_ACK</code>.</li><li><strong>Data:</strong> <code>RemoteConsumer</code>, <code>ConsumerKey</code>, <code>DeliveryKey</code>, <code>consumer-offsets.properties</code>, in-memory readiness state.</li><li><strong>Events:</strong> <code>SUBSCRIBE</code>, <code>ACK</code>, <code>READY</code>, <code>READY_ACK</code>.</li><li><strong>External systems:</strong> Modern TCP client.</li><li><strong>POS dependency:</strong> READY is the normal POS startup gate and is reused by refresh completion.</li><li><strong>Compaction:</strong> No direct state change; later delivery filters superseded records.</li><li><strong>Failures:</strong> Duplicate registration is ignored; malformed subscription closes the connection; READY retries three times.</li><li><strong>Tests:</strong> <code>SubscribeHandlerSpec</code>, <code>SubscribeHandlerIntegrationSpec</code>, <code>ConsumerRegistrationManagerSpec</code>, client integration tests.</li><li><strong>Coverage gaps:</strong> Multiple live sockets for the same group/topic and takeover semantics are not specified.</li><li><strong>Risks:</strong> Client IDs are remote socket addresses and change on reconnect; group/topic state must never be keyed only by client ID.</li></ul>\n<p>See <a href=\"#/chapter/07-pos-machine-state/startup-readiness\">POS state</a>.</p>\n<h2 id=\"modern-zero-copy-delivery-and-ack\" data-heading=\"Modern Zero-Copy Delivery And ACK\">Modern Zero-Copy Delivery And ACK<a class=\"heading-link\" href=\"#/chapter/03-feature-catalog/modern-zero-copy-delivery-and-ack\" aria-label=\"Link to Modern Zero-Copy Delivery And ACK\">#</a></h2>\n<ul><li><strong>Business purpose:</strong> Deliver batches efficiently and commit progress only after client processing.</li><li><strong>Entry point:</strong> <code>broker/src/main/java/com/messaging/broker/consumer/DeliveryScheduler.java</code>.</li><li><strong>Main files:</strong> <code>TopicFairScheduler.java</code>, <code>BatchDeliveryService.java</code>, <code>NettyTcpServer.java</code>, <code>ZeroCopyBatchDecoder.java</code>, <code>BatchAckService.java</code>.</li><li><strong>Flow:</strong> Apply gates, read <code>DeliveryBatch</code> on storage executor, filter stale compacted records, reserve generation state, send header plus file region, auto-ACK after decode, persist next offset.</li><li><strong>Data:</strong> In-memory <code>PendingDelivery</code>; <code>consumer-offsets.properties</code>; ACK RocksDB records.</li><li><strong>Events:</strong> <code>BATCH_HEADER</code> plus raw bytes, then <code>BATCH_ACK</code>.</li><li><strong>External systems:</strong> Modern TCP client.</li><li><strong>POS dependency:</strong> Delivery is blocked until startup/refresh readiness and paused in <code>RESET_SENT</code>.</li><li><strong>Compaction:</strong> Delivery-time filter prevents stale versions before physical compaction.</li><li><strong>Failures:</strong> Storage/send timeouts, ACK timeout offset rollback, exponential consumer backoff, unregister after ten consecutive failures.</li><li><strong>Tests:</strong> <code>BatchDeliveryServiceSpec</code>, <code>BatchAckServiceRocksDbSpec</code>, codec tests, modern delivery journey/system tests.</li><li><strong>Coverage gaps:</strong> Slow real networks and POS process pauses are simulated only through timeouts.</li><li><strong>Risks:</strong> A reconnect journey once retained a pending ACK beyond its configured timeout; see <a href=\"#/chapter/10-test-map/observed-failures\">Tests</a>.</li></ul>\n<p>See <a href=\"#/chapter/06-event-kafka-flow/modern-delivery-flow\">Modern event flow</a>.</p>\n<h2 id=\"legacy-consumer-delivery\" data-heading=\"Legacy Consumer Delivery\">Legacy Consumer Delivery<a class=\"heading-link\" href=\"#/chapter/03-feature-catalog/legacy-consumer-delivery\" aria-label=\"Link to Legacy Consumer Delivery\">#</a></h2>\n<ul><li><strong>Business purpose:</strong> Support old service-name registration and merged multi-topic batches.</li><li><strong>Entry point:</strong> <code>network/src/main/java/com/messaging/network/legacy/ProtocolDetectionDecoder.java</code>.</li><li><strong>Main files:</strong> <code>LegacyEventDecoder.java</code>, <code>LegacyConnectionState.java</code>, <code>LegacyClientConfig.java</code>, <code>LegacyConsumerDeliveryManager.java</code>, <code>ConsumerRegistry.java</code>.</li><li><strong>Flow:</strong> Detect leading <code>REGISTER</code>, convert it to legacy <code>SUBSCRIBE</code>, map service name to topics, merge topic cursors by offset, send a JSON <code>BatchEvent</code>, interpret generic ACK by pending FIFO expectation.</li><li><strong>Data:</strong> Per-topic offsets, <code>MergedBatch</code>, <code>TopicCursor</code>, segment indexes.</li><li><strong>Events:</strong> Legacy <code>REGISTER</code>, <code>READY</code>, <code>RESET</code>, <code>BATCH</code>, generic <code>ACK</code>.</li><li><strong>External systems:</strong> Legacy POS/service client.</li><li><strong>POS dependency:</strong> This is the compatibility path for deployed POS services.</li><li><strong>Compaction:</strong> Legacy cursor reads compacted indexes; merged delivery must preserve offset advancement over gaps.</li><li><strong>Failures:</strong> Unknown service names do not register; pending batch timeout frees the slot; disconnect clears readiness and pending state.</li><li><strong>Tests:</strong> Legacy codec tests, <code>LegacyConsumerDeliveryManagerSpec</code>, legacy journey specs, <code>BrokerLegacyConsumerIntegrationSpec</code>.</li><li><strong>Coverage gaps:</strong> Production service-name aliases are not tested as a configuration contract.</li><li><strong>Risks:</strong> Current integration tests repeatedly fail because they register <code>price-quote-service</code>, while YAML maps only <code>price-quote</code>.</li></ul>\n<p>See <a href=\"#/chapter/06-event-kafka-flow/legacy-wire-protocol\">Legacy protocol</a> and <a href=\"#/chapter/10-test-map/observed-failures\">Observed failures</a>.</p>\n<h2 id=\"offset-and-ack-persistence\" data-heading=\"Offset And ACK Persistence\">Offset And ACK Persistence<a class=\"heading-link\" href=\"#/chapter/03-feature-catalog/offset-and-ack-persistence\" aria-label=\"Link to Offset And ACK Persistence\">#</a></h2>\n<ul><li><strong>Business purpose:</strong> Persist group progress and provide per-record ACK audit/reconciliation.</li><li><strong>Entry point:</strong> <code>broker/src/main/java/com/messaging/broker/consumer/BatchAckService.java</code>.</li><li><strong>Main files:</strong> <code>ConsumerOffsetTracker.java</code>, <code>AckStore.java</code>, <code>RocksDbAckStore.java</code>, <code>AckStoreSeeder.java</code>, <code>AckReconciliationScheduler.java</code>.</li><li><strong>Flow:</strong> Claim one pending generation, persist offset, update metrics, re-read acknowledged records in chunks, write ACK records asynchronously; seed/reconcile missing historical entries.</li><li><strong>Data:</strong> <code>consumer-offsets.properties</code>; RocksDB keys <code>{topic}|{group}|{offset}</code>.</li><li><strong>Events:</strong> <code>BATCH_ACK</code>; legacy generic ACK resolved to batch ACK.</li><li><strong>External systems:</strong> RocksDB JNI and local storage.</li><li><strong>POS dependency:</strong> Offset durability controls replay after POS restart.</li><li><strong>Compaction:</strong> ACK and compaction use separate column families in one <code>SharedRocksDb</code>.</li><li><strong>Failures:</strong> Async ACK-store write can fail after offset commit; seeding/reconciliation repair or report gaps.</li><li><strong>Tests:</strong> ACK store/unit/integration specs, seeder and reconciliation journey specs.</li><li><strong>Coverage gaps:</strong> Abrupt power loss between property flush and RocksDB write is not exercised on real hardware.</li><li><strong>Risks:</strong> Property-file synchronous flush is rate-limited to one second, so at-least-once replay after crash is expected.</li></ul>\n<p>See <a href=\"#/chapter/05-data-model/rocksdb\">Data model</a> and <a href=\"#/chapter/09-error-retry-recovery/ack-recovery\">Recovery</a>.</p>\n<h2 id=\"pos-data-refresh\" data-heading=\"POS Data Refresh\">POS Data Refresh<a class=\"heading-link\" href=\"#/chapter/03-feature-catalog/pos-data-refresh\" aria-label=\"Link to POS Data Refresh\">#</a></h2>\n<ul><li><strong>Business purpose:</strong> Reset consumers, replay a stable local snapshot, and resume upstream updates only after all consumers are ready.</li><li><strong>Entry point:</strong> <code>POST /admin/refresh-topic</code> in <code>broker/src/main/java/com/messaging/broker/http/RefreshController.java</code>.</li><li><strong>Main files:</strong> <code>RefreshCoordinator.java</code>, <code>RefreshInitiator.java</code>, <code>RefreshResetService.java</code>, <code>RefreshReplayService.java</code>, <code>RefreshReadyService.java</code>, <code>RefreshRecoveryService.java</code>.</li><li><strong>Flow:</strong> Pause pipe, persist <code>RESET_SENT</code>, clear ACK records, broadcast RESET, reset each ACKing group to offset <code>0</code>, replay, broadcast READY, wait for READY ACKs, resume pipe and reconciliation.</li><li><strong>Data:</strong> <code>RefreshContext</code>, <code>data-refresh-state.properties</code>, consumer offsets, ACK store.</li><li><strong>Events:</strong> <code>RESET</code>, <code>RESET_ACK</code>, batches, <code>READY</code>, <code>READY_ACK</code>.</li><li><strong>External systems:</strong> POS consumers and parent pipe.</li><li><strong>POS dependency:</strong> This feature is the POS state machine.</li><li><strong>Compaction:</strong> Replay reads compacted plus active segments and delivery-time filtering remains active.</li><li><strong>Failures:</strong> Retries RESET every 5s, READY every 10s, abort watchdog at 10 minutes, persisted recovery after restart.</li><li><strong>Tests:</strong> Refresh unit/integration specs and numerous journey tests.</li><li><strong>Coverage gaps:</strong> Operational behavior after <code>ABORTED</code> still requires manual policy definition.</li><li><strong>Risks:</strong> Late joiners and disconnect races are complex; one reconnect journey was flaky in the aggregate run.</li></ul>\n<p>See <a href=\"#/chapter/07-pos-machine-state\">POS state</a>.</p>\n<h2 id=\"log-compaction\" data-heading=\"Log Compaction\">Log Compaction<a class=\"heading-link\" href=\"#/chapter/03-feature-catalog/log-compaction\" aria-label=\"Link to Log Compaction\">#</a></h2>\n<ul><li><strong>Business purpose:</strong> Suppress superseded keyed records and reclaim sealed-segment disk space.</li><li><strong>Entry point:</strong> scheduled <code>CompactionScheduler.compact()</code> or <code>POST /admin/compaction/trigger</code>.</li><li><strong>Main files:</strong> all files under <code>broker/src/main/java/com/messaging/broker/compaction/</code> plus <code>SegmentManager.java</code>.</li><li><strong>Flow:</strong> Select sealed windows after checkpoint, stream records, drop stale versions and expired latest tombstones, atomically install compacted files, replace segment map, update checkpoint/index.</li><li><strong>Data:</strong> Segment files and RocksDB compaction column family.</li><li><strong>Events:</strong> Keyed <code>MESSAGE</code> and <code>DELETE</code>.</li><li><strong>External systems:</strong> Local filesystem and RocksDB.</li><li><strong>POS dependency:</strong> Delivery must remain safe while POS consumers replay or have batches in flight.</li><li><strong>Compaction:</strong> This is the compaction implementation; active segments are not rewritten.</li><li><strong>Failures:</strong> Single-flight guard, CPU/heap skip guards, staging recovery, per-topic error isolation.</li><li><strong>Tests:</strong> Compaction unit/integration and nine journey suites.</li><li><strong>Coverage gaps:</strong> No test confirms that a no-op compaction rewrite is skipped.</li><li><strong>Risks:</strong> Heavy IO/CPU/GC, force-roll race, in-flight file-region lifetime, and all-records-deleted offset gaps.</li></ul>\n<p>See <a href=\"#/chapter/08-compaction-handling\">Compaction handling</a>.</p>\n<h2 id=\"embedded-annotation-consumers\" data-heading=\"Embedded Annotation Consumers\">Embedded Annotation Consumers<a class=\"heading-link\" href=\"#/chapter/03-feature-catalog/embedded-annotation-consumers\" aria-label=\"Link to Embedded Annotation Consumers\">#</a></h2>\n<ul><li><strong>Business purpose:</strong> Run broker-local <code>@Consumer</code> handlers without the remote TCP registry.</li><li><strong>Entry point:</strong> <code>broker/src/main/java/com/messaging/broker/consumer/ConsumerAnnotationProcessor.java</code>.</li><li><strong>Main files:</strong> <code>ConsumerDeliveryManager.java</code>, <code>ConsumerContext.java</code>, shared <code>@Consumer</code> and <code>RetryPolicy</code>.</li><li><strong>Flow:</strong> Discover annotated <code>MessageHandler</code> beans, restore offsets, poll storage, invoke handler, apply retry policy, persist progress.</li><li><strong>Data:</strong> Consumer context and shared offset properties.</li><li><strong>Events:</strong> Internal <code>MessageRecord</code> to handler calls.</li><li><strong>External systems:</strong> None.</li><li><strong>POS dependency:</strong> None confirmed; test consumer uses the separate client module.</li><li><strong>Compaction:</strong> Local delivery checks <code>CompactionIndex</code> before invoking handlers.</li><li><strong>Failures:</strong> <code>EXPONENTIAL_THEN_FIXED</code>, <code>SKIP</code>, or <code>PAUSE</code> policy from annotation.</li><li><strong>Tests:</strong> <code>ConsumerAnnotationProcessorSpec</code>, <code>ConsumerDeliveryManagerSpec</code>, <code>ConsumerContextSpec</code>.</li><li><strong>Coverage gaps:</strong> No production local consumer implementation was found.</li><li><strong>Risks:</strong> It shares storage and offset resources with remote delivery but follows a separate execution path.</li></ul>\n<h2 id=\"modern-client-library\" data-heading=\"Modern Client Library\">Modern Client Library<a class=\"heading-link\" href=\"#/chapter/03-feature-catalog/modern-client-library\" aria-label=\"Link to Modern Client Library\">#</a></h2>\n<ul><li><strong>Business purpose:</strong> Make a Micronaut service a consumer by annotating a <code>MessageHandler</code>.</li><li><strong>Entry point:</strong> <code>client/src/main/java/com/messaging/client/ClientConsumerManager.java</code>.</li><li><strong>Main files:</strong> <code>NettyTcpClient.java</code>, <code>ZeroCopyBatchDecoder.java</code>, common <code>MessageHandler.java</code>.</li><li><strong>Flow:</strong> Discover beans, resolve properties, create one connection per topic/group, subscribe, route batches, call <code>onReset</code>/<code>onReady</code>, reconnect with 5s-to-60s backoff.</li><li><strong>Data:</strong> In-memory connection maps and decoded <code>ConsumerRecord</code> lists.</li><li><strong>Events:</strong> Modern subscribe/readiness/batch/control messages.</li><li><strong>External systems:</strong> Broker TCP endpoint.</li><li><strong>POS dependency:</strong> Implements the modern POS-side reset/ready callbacks.</li><li><strong>Compaction:</strong> Receives already-filtered records.</li><li><strong>Failures:</strong> Per-connection health check every 10s; reconnect on disconnect/dead channel.</li><li><strong>Tests:</strong> <code>client/src/integrationTest/groovy/com/messaging/client/ClientConsumerManagerIntegrationSpec.groovy</code>.</li><li><strong>Coverage gaps:</strong> Multi-handler reset semantics and long reconnect storms are not load tested.</li><li><strong>Risks:</strong> All handlers registered for a topic are invoked for data and control events, even when multiple groups share that topic.</li></ul>\n<h2 id=\"monitoring-and-administration\" data-heading=\"Monitoring And Administration\">Monitoring And Administration<a class=\"heading-link\" href=\"#/chapter/03-feature-catalog/monitoring-and-administration\" aria-label=\"Link to Monitoring And Administration\">#</a></h2>\n<ul><li><strong>Business purpose:</strong> Expose health, metrics, thread/resource diagnostics, runtime log levels, refresh status, and compaction controls.</li><li><strong>Entry point:</strong> controllers under <code>broker/src/main/java/com/messaging/broker/http/</code>.</li><li><strong>Main files:</strong> monitoring package, <code>application.yml</code>, controllers.</li><li><strong>Flow:</strong> Micronaut HTTP requests read process or service state and return maps/text; scheduled monitors update metrics/logs.</li><li><strong>Data:</strong> In-memory metrics and JVM MXBeans.</li><li><strong>Events:</strong> HTTP only.</li><li><strong>External systems:</strong> Prometheus scraper and operators.</li><li><strong>POS dependency:</strong> Used to diagnose deployed store nodes.</li><li><strong>Compaction:</strong> Exposes status/trigger and compaction metrics.</li><li><strong>Failures:</strong> Most endpoints return error maps rather than typed HTTP error statuses.</li><li><strong>Tests:</strong> controller and monitoring integration/unit specs.</li><li><strong>Coverage gaps:</strong> Authentication/authorization behavior is not confirmed.</li><li><strong>Risks:</strong> Diagnostics and test APIs expose powerful operations if reachable.</li></ul>\n<p>See <a href=\"#/chapter/04-api-catalog\">API catalog</a>.</p>\n<h2 id=\"pipe-consistency-detection\" data-heading=\"Pipe Consistency Detection\">Pipe Consistency Detection<a class=\"heading-link\" href=\"#/chapter/03-feature-catalog/pipe-consistency-detection\" aria-label=\"Link to Pipe Consistency Detection\">#</a></h2>\n<ul><li><strong>Business purpose:</strong> Detect (never repair) whether this POS holds everything its parent holds per topic — missed records, stale keys, zombie keys (deletes missed while offline past tombstone retention), and fabricated keys (entries an authoritative verifier — the cloud — never had: corrupted/injected index) — despite both sides compacting independently.</li><li><strong>Entry point:</strong> <code>broker/src/main/java/com/messaging/broker/consistency/PipeConsistencyService.java</code>.</li><li><strong>Main files:</strong> <code>KeyspaceDigest.java</code>, <code>ParentConsistencyClient.java</code>, <code>PipeConsistencyReport.java</code>, <code>http/PipeConsistencyController.java</code>, <code>http/PipeConsistencyAdminController.java</code>, <code>CompactionIndex.forEachEntry</code> (both backends).</li><li><strong>Flow:</strong> admin POST → resolve target (<code>TopologyManager.getCurrentParentUrl()</code> or registry URL) → per topic: child scans its compaction index into 64 XOR-bucket digests at its own watermark → one GET to the parent → equal digests ⇒ CONSISTENT; else fetch mismatched buckets (one request/scan) → classify differences via parent record physical-presence + index state → report + Micrometer gauges.</li><li><strong>Data:</strong> compaction index only (<code>key → latestOffset</code>, compaction-invariant); no new persistent state; bounded in-memory report ring (16).</li><li><strong>Events:</strong> HTTP + <code>PipeConsistencyScheduler</code> (<code>@Scheduled</code>, default 6h interval / 10m initial delay, bean exists only when <code>pipe.consistency.enabled=true</code>); skips with a logged reason when a check runs, the parent is unassigned (offline POS), or heap pressure is high. Admin API remains available regardless.</li><li><strong>External systems:</strong> parent broker; cloud-server must mirror the three served endpoints (pending, separate repo).</li><li><strong>POS dependency:</strong> offline → <code>UNREACHABLE</code> verdict, no retries; reshuffle → target re-resolved per call, watermark clamps to a lagging parent (<code>CONSISTENT_UP_TO</code>).</li><li><strong>Compaction:</strong> verdict is compaction-invariant by construction; expired-tombstone asymmetries resolved by the classify step's physical-presence check.</li><li><strong>Failures:</strong> parent 404 → <code>UNSUPPORTED_PARENT</code>; index/storage error → <code>ERROR</code>; oversized divergence capped by <code>max-drilldown-buckets</code>/<code>max-classify-entries</code>/<code>max-bucket-entries</code>.</li><li><strong>Tests:</strong> <code>KeyspaceDigestSpec</code>, <code>PipeConsistencyServiceSpec</code>, <code>CompactionIndexForEachEntrySpec</code> (unit); <code>PipeConsistencyEndpointsIntegrationSpec</code>, <code>PipeConsistencyDisabledIntegrationSpec</code> (integration).</li><li><strong>Coverage gaps:</strong> no two-broker journey yet; no cloud-side implementation; no index-rebuild tool (a wiped RocksDB reports falsely inconsistent).</li><li><strong>Risks:</strong> trusts the compaction index as source of truth (already load-bearing for delivery filtering); fail-closed default <code>pipe.consistency.enabled=false</code>.</li></ul>\n<p>See <a href=\"#/chapter/04-api-catalog/pipe-consistency-api\">Pipe Consistency API</a> and <code>readme/PIPE_CONSISTENCY_V2.md</code>.</p>","headings":[{"level":2,"title":"Producer Ingestion","id":"producer-ingestion"},{"level":2,"title":"Topology And Pipe Replication","id":"topology-and-pipe-replication"},{"level":2,"title":"Segment Storage And Recovery","id":"segment-storage-and-recovery"},{"level":2,"title":"Modern Consumer Subscription And Readiness","id":"modern-consumer-subscription-and-readiness"},{"level":2,"title":"Modern Zero-Copy Delivery And ACK","id":"modern-zero-copy-delivery-and-ack"},{"level":2,"title":"Legacy Consumer Delivery","id":"legacy-consumer-delivery"},{"level":2,"title":"Offset And ACK Persistence","id":"offset-and-ack-persistence"},{"level":2,"title":"POS Data Refresh","id":"pos-data-refresh"},{"level":2,"title":"Log Compaction","id":"log-compaction"},{"level":2,"title":"Embedded Annotation Consumers","id":"embedded-annotation-consumers"},{"level":2,"title":"Modern Client Library","id":"modern-client-library"},{"level":2,"title":"Monitoring And Administration","id":"monitoring-and-administration"},{"level":2,"title":"Pipe Consistency Detection","id":"pipe-consistency-detection"}],"plainText":"Feature Catalog Related: APIs, Data model, Events, POS state, Compaction, Tests. Producer Ingestion Business purpose: Accept keyed MESSAGE or DELETE records from TCP producers. Entry point: broker/src/main/java/com/messaging/broker/handler/DataHandler.java. Main files: HandlerInitializer.java, DataHandler.java, StorageEngine.java, CompactionIndex.java. Flow: Decode JSON, require topic, default missing key/type/data, append partition 0, update metrics and compaction index, send ACK. Data: MessageRecord; segment log/index; SQLite segmentmetadata. Events: inbound DATA, outbound ACK; domain MESSAGE/DELETE. External systems: TCP client only. POS dependency: None directly; produced records feed POS consumers. Compaction: Every append calls CompactionIndex.updateKey. Failures: Invalid JSON/topic or storage error closes the connection; ACK send failure is logged asynchronously. Tests: broker/src/integrationTest/groovy/com/messaging/broker/handler/DataHandlerIntegrationSpec.groovy; parser/model unit specs. Coverage gaps: No explicit authorization or payload-size business validation test at the handler boundary was confirmed. Risks: Generated default keys prevent useful compaction; eventtype values other than exact DELETE become MESSAGE. See TCP API and Storage. Topology And Pipe Replication Business purpose: Discover a parent node and replicate cloud records into local storage. Entry point: broker/src/main/java/com/messaging/broker/core/TopologyManager.java. Main files: CloudRegistryClient.java, TopologyManager.java, TopologyPropertiesStore.java, HttpPipeConnector.java, BrokerService.java. Flow: Poll registry, choose the first parent, probe /health, connect pipe, stream /pipe/poll, invoke broker handler per record, persist the last successful offset. Data: TopologyResponse, MessageRecord, topology.properties, pipe-offset.properties. Events/topics: HTTP JSON records with topic and parent offsets; no Kafka client. External systems: Cloud registry and parent HTTP broker. POS dependency: Pipe pauses during refresh so the POS snapshot is replayed against stable local data. Compaction: Pipe records update the same compaction index as producer records. Failures: Registry/probe/poll failures retain or retry the previous state with adaptive delay; failed storage handling prevents offset advancement. Tests: CloudRegistryClientIntegrationSpec, TopologyManagerProbeIntegrationSpec, HttpPipeConnectorIntegrationSpec, PipeOutageJourneySpec. Coverage gaps: Multi-parent failover and topic selection across the pipe are not confirmed. Risks: HttpPipeConnector does not add a topic query parameter, while PipeServer defaults to price-topic; PipeMessageForwarder.getCurrentOffset() always returns 0. See Pipe API, Runtime config, and Risks. Segment Storage And Recovery Business purpose: Persist ordered topic records with bounded segment files and restart recovery. Entry point: storage/src/main/java/com/messaging/storage/filechannel/FileChannelStorageEngine.java. Main files: Segment.java, SegmentManager.java, DefaultStorageRecoveryService.java, SegmentMetadataStore.java. Flow: Create/recover topic manager, append binary record and dense index entry, roll full segments, periodically force active files, read by binary-searching index offsets. Data: .log, .index, .compacted.log, .compacted.index, SQLite segmentmetadata. Events/topics: Topic and partition 0; record offsets may be sparse when copied from a parent. External systems: Local filesystem, SQLite JDBC, Linux JNA cache advisory. POS dependency: All replay/readiness state ultimately depends on durable segment offsets. Compaction: Exposes SegmentAccess and atomic replacement APIs to the broker compactor. Failures: Recovery validates headers, truncates orphan log/index tails, ignores/deletes staging files, and fails startup for unrecoverable storage errors. Tests: Storage segment, crash recovery, concurrent read, memory-leak, engine, metadata, and recovery specs under storage/src/. Coverage gaps: Power-loss behavior on real POS filesystems and filesystems without atomic move is not confirmed. Risks: No per-record CRC in current v2 format; non-monotonic supplied offsets are warned but appended; StorageEngine.compact() and validateStorage() are placeholders. See Data model. Modern Consumer Subscription And Readiness Business purpose: Register one consumer group for one topic and block delivery until the client is ready. Entry point: broker/src/main/java/com/messaging/broker/handler/SubscribeHandler.java. Main files: ConsumerRegistry.java, ConsumerRegistrationManager.java, ConsumerReadinessManager.java, ClientConsumerManager.java. Flow: Parse {topic,group}, restore and validate group:topic offset, atomically register the socket session, ACK subscribe, send READY, wait for READYACK. Data: RemoteConsumer, ConsumerKey, DeliveryKey, consumer-offsets.properties, in-memory readiness state. Events: SUBSCRIBE, ACK, READY, READYACK. External systems: Modern TCP client. POS dependency: READY is the normal POS startup gate and is reused by refresh completion. Compaction: No direct state change; later delivery filters superseded records. Failures: Duplicate registration is ignored; malformed subscription closes the connection; READY retries three times. Tests: SubscribeHandlerSpec, SubscribeHandlerIntegrationSpec, ConsumerRegistrationManagerSpec, client integration tests. Coverage gaps: Multiple live sockets for the same group/topic and takeover semantics are not specified. Risks: Client IDs are remote socket addresses and change on reconnect; group/topic state must never be keyed only by client ID. See POS state. Modern Zero-Copy Delivery And ACK Business purpose: Deliver batches efficiently and commit progress only after client processing. Entry point: broker/src/main/java/com/messaging/broker/consumer/DeliveryScheduler.java. Main files: TopicFairScheduler.java, BatchDeliveryService.java, NettyTcpServer.java, ZeroCopyBatchDecoder.java, BatchAckService.java. Flow: Apply gates, read DeliveryBatch on storage executor, filter stale compacted records, reserve generation state, send header plus file region, auto-ACK after decode, persist next offset. Data: In-memory PendingDelivery; consumer-offsets.properties; ACK RocksDB records. Events: BATCHHEADER plus raw bytes, then BATCHACK. External systems: Modern TCP client. POS dependency: Delivery is blocked until startup/refresh readiness and paused in RESETSENT. Compaction: Delivery-time filter prevents stale versions before physical compaction. Failures: Storage/send timeouts, ACK timeout offset rollback, exponential consumer backoff, unregister after ten consecutive failures. Tests: BatchDeliveryServiceSpec, BatchAckServiceRocksDbSpec, codec tests, modern delivery journey/system tests. Coverage gaps: Slow real networks and POS process pauses are simulated only through timeouts. Risks: A reconnect journey once retained a pending ACK beyond its configured timeout; see Tests. See Modern event flow. Legacy Consumer Delivery Business purpose: Support old service-name registration and merged multi-topic batches. Entry point: network/src/main/java/com/messaging/network/legacy/ProtocolDetectionDecoder.java. Main files: LegacyEventDecoder.java, LegacyConnectionState.java, LegacyClientConfig.java, LegacyConsumerDeliveryManager.java, ConsumerRegistry.java. Flow: Detect leading REGISTER, convert it to legacy SUBSCRIBE, map service name to topics, merge topic cursors by offset, send a JSON BatchEvent, interpret generic ACK by pending FIFO expectation. Data: Per-topic offsets, MergedBatch, TopicCursor, segment indexes. Events: Legacy REGISTER, READY, RESET, BATCH, generic ACK. External systems: Legacy POS/service client. POS dependency: This is the compatibility path for deployed POS services. Compaction: Legacy cursor reads compacted indexes; merged delivery must preserve offset advancement over gaps. Failures: Unknown service names do not register; pending batch timeout frees the slot; disconnect clears readiness and pending state. Tests: Legacy codec tests, LegacyConsumerDeliveryManagerSpec, legacy journey specs, BrokerLegacyConsumerIntegrationSpec. Coverage gaps: Production service-name aliases are not tested as a configuration contract. Risks: Current integration tests repeatedly fail because they register price-quote-service, while YAML maps only price-quote. See Legacy protocol and Observed failures. Offset And ACK Persistence Business purpose: Persist group progress and provide per-record ACK audit/reconciliation. Entry point: broker/src/main/java/com/messaging/broker/consumer/BatchAckService.java. Main files: ConsumerOffsetTracker.java, AckStore.java, RocksDbAckStore.java, AckStoreSeeder.java, AckReconciliationScheduler.java. Flow: Claim one pending generation, persist offset, update metrics, re-read acknowledged records in chunks, write ACK records asynchronously; seed/reconcile missing historical entries. Data: consumer-offsets.properties; RocksDB keys {topic} {group} {offset}. Events: BATCHACK; legacy generic ACK resolved to batch ACK. External systems: RocksDB JNI and local storage. POS dependency: Offset durability controls replay after POS restart. Compaction: ACK and compaction use separate column families in one SharedRocksDb. Failures: Async ACK-store write can fail after offset commit; seeding/reconciliation repair or report gaps. Tests: ACK store/unit/integration specs, seeder and reconciliation journey specs. Coverage gaps: Abrupt power loss between property flush and RocksDB write is not exercised on real hardware. Risks: Property-file synchronous flush is rate-limited to one second, so at-least-once replay after crash is expected. See Data model and Recovery. POS Data Refresh Business purpose: Reset consumers, replay a stable local snapshot, and resume upstream updates only after all consumers are ready. Entry point: POST /admin/refresh-topic in broker/src/main/java/com/messaging/broker/http/RefreshController.java. Main files: RefreshCoordinator.java, RefreshInitiator.java, RefreshResetService.java, RefreshReplayService.java, RefreshReadyService.java, RefreshRecoveryService.java. Flow: Pause pipe, persist RESETSENT, clear ACK records, broadcast RESET, reset each ACKing group to offset 0, replay, broadcast READY, wait for READY ACKs, resume pipe and reconciliation. Data: RefreshContext, data-refresh-state.properties, consumer offsets, ACK store. Events: RESET, RESETACK, batches, READY, READYACK. External systems: POS consumers and parent pipe. POS dependency: This feature is the POS state machine. Compaction: Replay reads compacted plus active segments and delivery-time filtering remains active. Failures: Retries RESET every 5s, READY every 10s, abort watchdog at 10 minutes, persisted recovery after restart. Tests: Refresh unit/integration specs and numerous journey tests. Coverage gaps: Operational behavior after ABORTED still requires manual policy definition. Risks: Late joiners and disconnect races are complex; one reconnect journey was flaky in the aggregate run. See POS state. Log Compaction Business purpose: Suppress superseded keyed records and reclaim sealed-segment disk space. Entry point: scheduled CompactionScheduler.compact() or POST /admin/compaction/trigger. Main files: all files under broker/src/main/java/com/messaging/broker/compaction/ plus SegmentManager.java. Flow: Select sealed windows after checkpoint, stream records, drop stale versions and expired latest tombstones, atomically install compacted files, replace segment map, update checkpoint/index. Data: Segment files and RocksDB compaction column family. Events: Keyed MESSAGE and DELETE. External systems: Local filesystem and RocksDB. POS dependency: Delivery must remain safe while POS consumers replay or have batches in flight. Compaction: This is the compaction implementation; active segments are not rewritten. Failures: Single-flight guard, CPU/heap skip guards, staging recovery, per-topic error isolation. Tests: Compaction unit/integration and nine journey suites. Coverage gaps: No test confirms that a no-op compaction rewrite is skipped. Risks: Heavy IO/CPU/GC, force-roll race, in-flight file-region lifetime, and all-records-deleted offset gaps. See Compaction handling. Embedded Annotation Consumers Business purpose: Run broker-local @Consumer handlers without the remote TCP registry. Entry point: broker/src/main/java/com/messaging/broker/consumer/ConsumerAnnotationProcessor.java. Main files: ConsumerDeliveryManager.java, ConsumerContext.java, shared @Consumer and RetryPolicy. Flow: Discover annotated MessageHandler beans, restore offsets, poll storage, invoke handler, apply retry policy, persist progress. Data: Consumer context and shared offset properties. Events: Internal MessageRecord to handler calls. External systems: None. POS dependency: None confirmed; test consumer uses the separate client module. Compaction: Local delivery checks CompactionIndex before invoking handlers. Failures: EXPONENTIALTHENFIXED, SKIP, or PAUSE policy from annotation. Tests: ConsumerAnnotationProcessorSpec, ConsumerDeliveryManagerSpec, ConsumerContextSpec. Coverage gaps: No production local consumer implementation was found. Risks: It shares storage and offset resources with remote delivery but follows a separate execution path. Modern Client Library Business purpose: Make a Micronaut service a consumer by annotating a MessageHandler. Entry point: client/src/main/java/com/messaging/client/ClientConsumerManager.java. Main files: NettyTcpClient.java, ZeroCopyBatchDecoder.java, common MessageHandler.java. Flow: Discover beans, resolve properties, create one connection per topic/group, subscribe, route batches, call onReset/onReady, reconnect with 5s-to-60s backoff. Data: In-memory connection maps and decoded ConsumerRecord lists. Events: Modern subscribe/readiness/batch/control messages. External systems: Broker TCP endpoint. POS dependency: Implements the modern POS-side reset/ready callbacks. Compaction: Receives already-filtered records. Failures: Per-connection health check every 10s; reconnect on disconnect/dead channel. Tests: client/src/integrationTest/groovy/com/messaging/client/ClientConsumerManagerIntegrationSpec.groovy. Coverage gaps: Multi-handler reset semantics and long reconnect storms are not load tested. Risks: All handlers registered for a topic are invoked for data and control events, even when multiple groups share that topic. Monitoring And Administration Business purpose: Expose health, metrics, thread/resource diagnostics, runtime log levels, refresh status, and compaction controls. Entry point: controllers under broker/src/main/java/com/messaging/broker/http/. Main files: monitoring package, application.yml, controllers. Flow: Micronaut HTTP requests read process or service state and return maps/text; scheduled monitors update metrics/logs. Data: In-memory metrics and JVM MXBeans. Events: HTTP only. External systems: Prometheus scraper and operators. POS dependency: Used to diagnose deployed store nodes. Compaction: Exposes status/trigger and compaction metrics. Failures: Most endpoints return error maps rather than typed HTTP error statuses. Tests: controller and monitoring integration/unit specs. Coverage gaps: Authentication/authorization behavior is not confirmed. Risks: Diagnostics and test APIs expose powerful operations if reachable. See API catalog. Pipe Consistency Detection Business purpose: Detect (never repair) whether this POS holds everything its parent holds per topic — missed records, stale keys, zombie keys (deletes missed while offline past tombstone retention), and fabricated keys (entries an authoritative verifier — the cloud — never had: corrupted/injected index) — despite both sides compacting independently. Entry point: broker/src/main/java/com/messaging/broker/consistency/PipeConsistencyService.java. Main files: KeyspaceDigest.java, ParentConsistencyClient.java, PipeConsistencyReport.java, http/PipeConsistencyController.java, http/PipeConsistencyAdminController.java, CompactionIndex.forEachEntry (both backends). Flow: admin POST → resolve target (TopologyManager.getCurrentParentUrl() or registry URL) → per topic: child scans its compaction index into 64 XOR-bucket digests at its own watermark → one GET to the parent → equal digests ⇒ CONSISTENT; else fetch mismatched buckets (one request/scan) → classify differences via parent record physical-presence + index state → report + Micrometer gauges. Data: compaction index only (key → latestOffset, compaction-invariant); no new persistent state; bounded in-memory report ring (16). Events: HTTP + PipeConsistencyScheduler (@Scheduled, default 6h interval / 10m initial delay, bean exists only when pipe.consistency.enabled=true); skips with a logged reason when a check runs, the parent is unassigned (offline POS), or heap pressure is high. Admin API remains available regardless. External systems: parent broker; cloud-server must mirror the three served endpoints (pending, separate repo). POS dependency: offline → UNREACHABLE verdict, no retries; reshuffle → target re-resolved per call, watermark clamps to a lagging parent (CONSISTENTUPTO). Compaction: verdict is compaction-invariant by construction; expired-tombstone asymmetries resolved by the classify step's physical-presence check. Failures: parent 404 → UNSUPPORTEDPARENT; index/storage error → ERROR; oversized divergence capped by max-drilldown-buckets/max-classify-entries/max-bucket-entries. Tests: KeyspaceDigestSpec, PipeConsistencyServiceSpec, CompactionIndexForEachEntrySpec (unit); PipeConsistencyEndpointsIntegrationSpec, PipeConsistencyDisabledIntegrationSpec (integration). Coverage gaps: no two-broker journey yet; no cloud-side implementation; no index-rebuild tool (a wiped RocksDB reports falsely inconsistent). Risks: trusts the compaction index as source of truth (already load-bearing for delivery filtering); fail-closed default pipe.consistency.enabled=false. See Pipe Consistency API and readme/PIPECONSISTENCYV2.md.","lineCount":234},{"order":4,"file":"04-api-catalog.md","slug":"04-api-catalog","title":"API Catalog","html":"<h1 id=\"api-catalog\" data-heading=\"API Catalog\">API Catalog<a class=\"heading-link\" href=\"#/chapter/04-api-catalog/api-catalog\" aria-label=\"Link to API Catalog\">#</a></h1>\n<p>Related: <a href=\"#/chapter/03-feature-catalog\">Features</a>, <a href=\"#/chapter/06-event-kafka-flow\">Events</a>, <a href=\"#/chapter/07-pos-machine-state\">POS state</a>, <a href=\"#/chapter/11-runtime-config\">Runtime config</a>, <a href=\"#/chapter/13-risk-and-edge-cases\">Risks</a>.</p>\n<h2 id=\"pipe-api\" data-heading=\"Pipe API\">Pipe API<a class=\"heading-link\" href=\"#/chapter/04-api-catalog/pipe-api\" aria-label=\"Link to Pipe API\">#</a></h2>\n<h3 id=\"get-pipepoll\" data-heading=\"GET /pipe/poll\"><code>GET /pipe/poll</code><a class=\"heading-link\" href=\"#/chapter/04-api-catalog/get-pipepoll\" aria-label=\"Link to GET /pipe/poll\">#</a></h3>\n<ul><li>Controller: <code>pipe/src/main/java/com/messaging/pipe/PipeServer.java</code></li><li>Query: <code>offset</code> default <code>0</code>, <code>limit</code> default <code>100</code>, <code>topic</code> default <code>price-topic</code></li><li>Success: JSON array of <code>MessageRecord</code></li><li>No data: HTTP <code>204</code></li><li>Failure: HTTP <code>500</code> with text</li><li>Service flow: <code>StorageEngine.read(topic, 0, offset, limit)</code></li><li>Data model: <a href=\"#/chapter/05-data-model/record-models\">MessageRecord</a></li><li>Consumer: <code>pipe/src/main/java/com/messaging/pipe/HttpPipeConnector.java</code></li></ul>\n<p>Important: the connector builds <code>/pipe/poll?offset=...&amp;limit=...</code> without a <code>topic</code> query. Which topic an external parent returns is <strong>not confirmed from code</strong>.</p>\n<h2 id=\"pipe-consistency-api\" data-heading=\"Pipe Consistency API\">Pipe Consistency API<a class=\"heading-link\" href=\"#/chapter/04-api-catalog/pipe-consistency-api\" aria-label=\"Link to Pipe Consistency API\">#</a></h2>\n<p>Detect-only audit of this node against its parent/the cloud. Fail-closed: every endpoint answers <code>404</code> when <code>pipe.consistency.enabled=false</code> (default); the cloud must mirror the three served endpoints. Design: <code>readme/PIPE_CONSISTENCY_V2.md</code>.</p>\n<h3 id=\"get-pipeconsistencyhead\" data-heading=\"GET /pipe/consistency/head\"><code>GET /pipe/consistency/head</code><a class=\"heading-link\" href=\"#/chapter/04-api-catalog/get-pipeconsistencyhead\" aria-label=\"Link to GET /pipe/consistency/head\">#</a></h3>\n<ul><li>O(1) head probe (in-memory storage head, no scan, no semaphore) — lets a child filter</li></ul>\n<p>verifier candidates before asking anyone to digest-scan. <code>{head: N}</code>, <code>404</code> when disabled.</p>\n<h3 id=\"get-pipeconsistencydigest\" data-heading=\"GET /pipe/consistency/digest\"><code>GET /pipe/consistency/digest</code><a class=\"heading-link\" href=\"#/chapter/04-api-catalog/get-pipeconsistencydigest\" aria-label=\"Link to GET /pipe/consistency/digest\">#</a></h3>\n<ul><li>Controller: <code>broker/src/main/java/com/messaging/broker/http/PipeConsistencyController.java</code></li><li>Query: <code>topic</code>, <code>watermark</code> (child head), <code>buckets</code> default <code>64</code></li><li>Success: <code>{parentHead, effectiveWatermark, digests[], counts[]}</code> (~1 KB)</li><li>Guards: <code>400</code> invalid params, <code>429</code> above <code>max-concurrent-scans</code></li><li>Service flow: one streaming <code>CompactionIndex.forEachEntry</code> scan folded by <code>KeyspaceDigest</code></li></ul>\n<h3 id=\"get-pipeconsistencybucket\" data-heading=\"GET /pipe/consistency/bucket\"><code>GET /pipe/consistency/bucket</code><a class=\"heading-link\" href=\"#/chapter/04-api-catalog/get-pipeconsistencybucket\" aria-label=\"Link to GET /pipe/consistency/bucket\">#</a></h3>\n<ul><li>Query: <code>topic</code>, <code>watermark</code>, <code>buckets</code>, <code>bucket</code> = comma-separated mismatched bucket ids</li><li>Success: <code>{entries: {bucketId: [{h: keyHash64, o: latestOffset}, ...]}}</code> — one scan serves all requested buckets</li><li>Guards: <code>413</code> above <code>max-bucket-entries</code></li></ul>\n<h3 id=\"post-pipeconsistencyclassify\" data-heading=\"POST /pipe/consistency/classify\"><code>POST /pipe/consistency/classify</code><a class=\"heading-link\" href=\"#/chapter/04-api-catalog/post-pipeconsistencyclassify\" aria-label=\"Link to POST /pipe/consistency/classify\">#</a></h3>\n<ul><li>Body: <code>{topic, watermark, offsets[], keys[]}</code></li><li>Success: <code>offsets</code> → record-at-exact-offset physically readable (distinguishes missed data</li></ul>\n<p>from expired tombstones); <code>keys</code> → <code>PRESENT_BEYOND_WATERMARK | PRESENT_AT_OR_BELOW_WATERMARK | ABSENT</code>; <code>authoritative: true</code> only when the answering node's keyspace is complete and never expires (the cloud sets it; POS brokers never do). An authoritative <code>ABSENT</code> proves the child key was fabricated/corrupted (<code>fabricatedKeys</code>, INCONSISTENT); a non-authoritative <code>ABSENT</code> stays a benign <code>extraKeys</code> warning (a freshly provisioned parent may simply lack history).</p>\n<h3 id=\"escalation-clamped-parent-in-store-verifiers\" data-heading=\"Escalation (clamped parent → in-store verifiers)\">Escalation (clamped parent → in-store verifiers)<a class=\"heading-link\" href=\"#/chapter/04-api-catalog/escalation-clamped-parent-in-store-verifiers\" aria-label=\"Link to Escalation (clamped parent → in-store verifiers)\">#</a></h3>\n<p>A clamped watermark (parent behind this node — only possible after a reshuffle) marks the report <code>verificationPending</code>. When the clamp persists <code>escalation.after-clamped-checks</code> consecutive checks, the broker probes the registry-provided <code>verifierCandidates</code> (cached from the existing topology poll; dead/behind candidates skipped via the head probe) and obtains the FULL verdict from the first one whose head covers the watermark (<code>escalatedFrom</code> records the original parent). LAN-only; the cloud is never called. The registry's topology response carries <code>verifierCandidates</code> (omitted when empty — brokers fall back to <code>requestToFollow[1..]</code>).</p>\n<h3 id=\"post-adminpipe-consistencychecktopicallt-and-targetparentcloud\" data-heading=\"POST /admin/pipe-consistency/check?topic=all|T&amp;target=parent|cloud\"><code>POST /admin/pipe-consistency/check?topic=all|T&amp;target=parent|cloud</code><a class=\"heading-link\" href=\"#/chapter/04-api-catalog/post-adminpipe-consistencychecktopicallt-and-targetparentcloud\" aria-label=\"Link to POST /admin/pipe-consistency/check?topic=all|T&amp;target=parent|cloud\">#</a></h3>\n<ul><li>Controller: <code>broker/src/main/java/com/messaging/broker/http/PipeConsistencyAdminController.java</code></li><li><code>202</code> started (async on <code>compactionExecutor</code>), <code>409</code> already running, <code>503</code> disabled</li><li>Service: <code>broker/src/main/java/com/messaging/broker/consistency/PipeConsistencyService.java</code> (single-flight)</li></ul>\n<h3 id=\"get-adminpipe-consistencyreport\" data-heading=\"GET /admin/pipe-consistency/report\"><code>GET /admin/pipe-consistency/report</code><a class=\"heading-link\" href=\"#/chapter/04-api-catalog/get-adminpipe-consistencyreport\" aria-label=\"Link to GET /admin/pipe-consistency/report\">#</a></h3>\n<ul><li>Latest verdict per topic + bounded history; states `CONSISTENT | CONSISTENT<em>UP</em>TO |</li></ul>\n<p>INCONSISTENT | INCONCLUSIVE | UNREACHABLE | UNSUPPORTED_PARENT | ERROR<code> (</code>INCONCLUSIVE<code> = drill-down cap exceeded at a clamped watermark — divergence may be benign child-ahead, e.g. the dev cloud's loopback replay); counts for missing/stale/ zombie/fabricated/lagging/extra/child-newer keys and </code>refreshRecommended`</p>\n<h3 id=\"post-testconsistencytamper-index-temporary-delete-after-validation\" data-heading=\"POST /test/consistency/tamper-index — TEMPORARY, delete after validation\"><code>POST /test/consistency/tamper-index</code> — TEMPORARY, delete after validation<a class=\"heading-link\" href=\"#/chapter/04-api-catalog/post-testconsistencytamper-index-temporary-delete-after-validation\" aria-label=\"Link to POST /test/consistency/tamper-index — TEMPORARY, delete after validation\">#</a></h3>\n<ul><li>Controller: <code>broker/src/main/java/com/messaging/broker/http/ConsistencyTamperTestController.java</code></li><li>Query: <code>topic</code>, <code>removeCount</code> (delete first N compaction-index entries → next check vs</li></ul>\n<p>cloud shows <code>missingKeys</code>), <code>addCount</code> (insert fake keys at the topic head → <code>fabricatedKeys</code>)</p>\n<ul><li>Fault-injection tool for live INCONSISTENT validation only; no specs by design.</li></ul>\n<h2 id=\"refresh-api\" data-heading=\"Refresh API\">Refresh API<a class=\"heading-link\" href=\"#/chapter/04-api-catalog/refresh-api\" aria-label=\"Link to Refresh API\">#</a></h2>\n<p>Controller: <code>broker/src/main/java/com/messaging/broker/http/RefreshController.java</code>.</p>\n<h3 id=\"post-adminrefresh-topic\" data-heading=\"POST /admin/refresh-topic\"><code>POST /admin/refresh-topic</code><a class=\"heading-link\" href=\"#/chapter/04-api-catalog/post-adminrefresh-topic\" aria-label=\"Link to POST /admin/refresh-topic\">#</a></h3>\n<ul><li>Body forms:<ul><li><code>{&quot;topic&quot;:&quot;prices-v1,reference-data-v5&quot;}</code></li><li><code>{&quot;topics&quot;:[&quot;prices-v1&quot;,&quot;reference-data-v5&quot;]}</code></li></ul></li><li>Returns immediately with <code>success</code>, <code>message</code>, <code>topics</code>, and <code>status: INITIATED</code>.</li><li>Empty/malformed input returns a map with <code>success:false</code>; explicit HTTP error status is not set.</li><li>Service: <code>RefreshCoordinator.startRefresh</code>.</li><li>State flow: <a href=\"#/chapter/07-pos-machine-state/refresh-state-machine\">POS state</a>.</li></ul>\n<h3 id=\"get-adminrefresh-statustopic\" data-heading=\"GET /admin/refresh-status?topic=...\"><code>GET /admin/refresh-status?topic=...</code><a class=\"heading-link\" href=\"#/chapter/04-api-catalog/get-adminrefresh-statustopic\" aria-label=\"Link to GET /admin/refresh-status?topic=...\">#</a></h3>\n<p>Returns state, counts, timestamps, and per-consumer RESET/READY/replay/offset details from <code>RefreshContext</code>.</p>\n<h3 id=\"get-adminrefresh-current\" data-heading=\"GET /admin/refresh-current\"><code>GET /admin/refresh-current</code><a class=\"heading-link\" href=\"#/chapter/04-api-catalog/get-adminrefresh-current\" aria-label=\"Link to GET /admin/refresh-current\">#</a></h3>\n<p>Returns one arbitrary current context via <code>activeRefreshes.values().stream().findFirst()</code>. With concurrent topic refreshes it is not a complete batch view.</p>\n<h2 id=\"compaction-api\" data-heading=\"Compaction API\">Compaction API<a class=\"heading-link\" href=\"#/chapter/04-api-catalog/compaction-api\" aria-label=\"Link to Compaction API\">#</a></h2>\n<p>Controller: <code>broker/src/main/java/com/messaging/broker/http/CompactionController.java</code>.</p>\n<h3 id=\"post-admincompactiontrigger\" data-heading=\"POST /admin/compaction/trigger\"><code>POST /admin/compaction/trigger</code><a class=\"heading-link\" href=\"#/chapter/04-api-catalog/post-admincompactiontrigger\" aria-label=\"Link to POST /admin/compaction/trigger\">#</a></h3>\n<p>Submits an asynchronous compaction run. Its preparation callback force-rolls every active topic segment before compaction. If another run is active, <code>triggered:false</code>.</p>\n<p>Response: <code>triggered</code>, <code>sealedTopics</code>, <code>sealErrors</code>, <code>message</code>.</p>\n<p>Service/data links: <a href=\"#/chapter/08-compaction-handling\">Compaction</a>, <a href=\"#/chapter/05-data-model/segment-storage\">Segment storage</a>.</p>\n<h3 id=\"get-admincompactionstatus\" data-heading=\"GET /admin/compaction/status\"><code>GET /admin/compaction/status</code><a class=\"heading-link\" href=\"#/chapter/04-api-catalog/get-admincompactionstatus\" aria-label=\"Link to GET /admin/compaction/status\">#</a></h3>\n<p>Returns per-topic sealed segment count/bytes, total sealed count, and an operator tip.</p>\n<h2 id=\"runtime-logging-api\" data-heading=\"Runtime Logging API\">Runtime Logging API<a class=\"heading-link\" href=\"#/chapter/04-api-catalog/runtime-logging-api\" aria-label=\"Link to Runtime Logging API\">#</a></h2>\n<p>Controller: <code>broker/src/main/java/com/messaging/broker/http/LoggingController.java</code>.</p>\n<ul><li><code>GET /admin/logging/features</code></li><li><code>GET /admin/logging/feature/{feature}</code></li><li><code>POST /admin/logging/feature/{feature}?level=DEBUG|INFO|...|RESET</code></li></ul>\n<p>Service: <code>broker/src/main/java/com/messaging/broker/monitoring/RuntimeLogLevelService.java</code>.</p>\n<p>The test consumer exposes the same paths from <code>test-consumer/src/main/java/com/example/consumer/http/LoggingController.java</code>.</p>\n<h2 id=\"diagnostics-and-management\" data-heading=\"Diagnostics And Management\">Diagnostics And Management<a class=\"heading-link\" href=\"#/chapter/04-api-catalog/diagnostics-and-management\" aria-label=\"Link to Diagnostics And Management\">#</a></h2>\n<p>Controller: <code>broker/src/main/java/com/messaging/broker/http/ThreadDiagnosticsController.java</code>.</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Method/path</th>\n<th>Purpose</th>\n</tr></thead><tbody>\n<tr>\n<td><code>GET /diagnostics/threads/summary</code></td>\n<td>Thread counts, states, deadlock/problem counts</td>\n</tr>\n<tr>\n<td><code>GET /diagnostics/threads/top-cpu?limit=N</code></td>\n<td>Highest cumulative CPU-time threads</td>\n</tr>\n<tr>\n<td><code>GET /diagnostics/threads/problematic</code></td>\n<td>Monitor-classified blocked/waiting threads</td>\n</tr>\n<tr>\n<td><code>GET /diagnostics/threads/dump</code></td>\n<td>Plain-text thread dump</td>\n</tr>\n<tr>\n<td><code>GET /diagnostics/threads/{threadId}</code></td>\n<td>Thread details and stack</td>\n</tr>\n<tr>\n<td><code>GET /diagnostics/threads/deadlocks</code></td>\n<td>Deadlock report</td>\n</tr>\n<tr>\n<td><code>GET /diagnostics/threads/top-memory?limit=N</code></td>\n<td>Per-thread allocation estimates</td>\n</tr>\n<tr>\n<td><code>GET /diagnostics/threads/resources?category=...</code></td>\n<td>Resource details</td>\n</tr>\n<tr>\n<td><code>GET /diagnostics/threads/by-category</code></td>\n<td>Aggregated resource categories</td>\n</tr>\n</tbody></table></div>\n<p>Micronaut management enables <code>/health</code>, <code>/prometheus</code>, and <code>/metrics</code> in <code>broker/src/main/resources/application.yml</code>.</p>\n<h2 id=\"test-data-api\" data-heading=\"Test Data API\">Test Data API<a class=\"heading-link\" href=\"#/chapter/04-api-catalog/test-data-api\" aria-label=\"Link to Test Data API\">#</a></h2>\n<p>Controller: <code>broker/src/main/java/com/messaging/broker/http/TestDataController.java</code>.</p>\n<h3 id=\"post-testload-from-sqlite\" data-heading=\"POST /test/load-from-sqlite\"><code>POST /test/load-from-sqlite</code><a class=\"heading-link\" href=\"#/chapter/04-api-catalog/post-testload-from-sqlite\" aria-label=\"Link to POST /test/load-from-sqlite\">#</a></h3>\n<p>Body: <code>sqliteFilePath</code>, optional <code>tableName</code> default <code>messages</code>, optional <code>topic</code> default <code>price-topic</code>.</p>\n<p>The controller opens <code>jdbc:sqlite:&lt;path&gt;</code>, executes <code>SELECT * FROM &lt;tableName&gt;</code>, converts each row to JSON text, and appends it. The table name is concatenated directly; see <a href=\"#/chapter/13-risk-and-edge-cases/security\">Security risks</a>.</p>\n<h3 id=\"post-testinject-messages\" data-heading=\"POST /test/inject-messages\"><code>POST /test/inject-messages</code><a class=\"heading-link\" href=\"#/chapter/04-api-catalog/post-testinject-messages\" aria-label=\"Link to POST /test/inject-messages\">#</a></h3>\n<p>Body: optional <code>count</code>, <code>topic</code>, <code>prefix</code>; appends generated records directly to storage.</p>\n<h3 id=\"get-teststats\" data-heading=\"GET /test/stats\"><code>GET /test/stats</code><a class=\"heading-link\" href=\"#/chapter/04-api-catalog/get-teststats\" aria-label=\"Link to GET /test/stats\">#</a></h3>\n<p>Returns placeholder/fake statistics. This is marked TODO in <code>TestDataController.java</code> and must not be used as an operational source of truth.</p>\n<h2 id=\"tcp-data-plane\" data-heading=\"TCP Data Plane\">TCP Data Plane<a class=\"heading-link\" href=\"#/chapter/04-api-catalog/tcp-data-plane\" aria-label=\"Link to TCP Data Plane\">#</a></h2>\n<p>Message envelope: <code>[type:1][messageId:8][payloadLength:4][payload]</code>, implemented by <code>BinaryMessageEncoder.java</code> and <code>BinaryMessageDecoder.java</code>.</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Type</th>\n<th>Direction</th>\n<th>Payload/behavior</th>\n<th>Handler</th>\n</tr></thead><tbody>\n<tr>\n<td><code>DATA</code></td>\n<td>producer -&gt; broker</td>\n<td>JSON producer record</td>\n<td><code>DataHandler.java</code></td>\n</tr>\n<tr>\n<td><code>ACK</code></td>\n<td>broker -&gt; producer/client</td>\n<td>Empty or generic legacy ACK</td>\n<td>client connection / legacy state</td>\n</tr>\n<tr>\n<td><code>SUBSCRIBE</code></td>\n<td>consumer -&gt; broker</td>\n<td>Modern <code>{topic,group}</code> or legacy adapter JSON</td>\n<td><code>SubscribeHandler.java</code></td>\n</tr>\n<tr>\n<td><code>COMMIT_OFFSET</code></td>\n<td>client -&gt; broker</td>\n<td>JSON <code>{topic,group,offset}</code></td>\n<td><code>CommitOffsetHandler.java</code></td>\n</tr>\n<tr>\n<td><code>RESET</code></td>\n<td>broker -&gt; consumer</td>\n<td>Topic bytes</td>\n<td>client <code>onReset</code></td>\n</tr>\n<tr>\n<td><code>READY</code></td>\n<td>broker -&gt; consumer</td>\n<td>Topic bytes or empty startup payload</td>\n<td>client <code>onReady</code></td>\n</tr>\n<tr>\n<td><code>DISCONNECT</code></td>\n<td>either/control</td>\n<td>Empty</td>\n<td>client reconnect logic</td>\n</tr>\n<tr>\n<td><code>HEARTBEAT</code></td>\n<td>defined</td>\n<td>No registered broker handler found</td>\n<td></td>\n</tr>\n<tr>\n<td><code>BATCH_HEADER</code></td>\n<td>broker -&gt; consumer</td>\n<td>count, bytes, topic, group; raw bytes follow</td>\n<td><code>ZeroCopyBatchDecoder.java</code></td>\n</tr>\n<tr>\n<td><code>BATCH_ACK</code></td>\n<td>consumer -&gt; broker</td>\n<td>topic/group lengths and bytes</td>\n<td><code>BatchAckHandler.java</code></td>\n</tr>\n<tr>\n<td><code>RESET_ACK</code></td>\n<td>consumer -&gt; broker</td>\n<td>topic/group lengths and bytes</td>\n<td><code>ResetAckHandler.java</code></td>\n</tr>\n<tr>\n<td><code>READY_ACK</code></td>\n<td>consumer -&gt; broker</td>\n<td>topic/group lengths and bytes</td>\n<td><code>ReadyAckHandler.java</code></td>\n</tr>\n</tbody></table></div>\n<p>Definitions: <code>common/src/main/java/com/messaging/common/model/BrokerMessage.java</code>.</p>\n<p>Detailed flow: <a href=\"#/chapter/06-event-kafka-flow\">Event chapter</a>.</p>\n<h2 id=\"api-error-behavior\" data-heading=\"API Error Behavior\">API Error Behavior<a class=\"heading-link\" href=\"#/chapter/04-api-catalog/api-error-behavior\" aria-label=\"Link to API Error Behavior\">#</a></h2>\n<ul><li>TCP validation failures generally close the connection.</li><li>ACK handler malformed length fields close the connection; unexpected legacy group lookup soft-fails.</li><li>HTTP controllers generally return maps with error fields rather than <code>HttpResponse</code> error status.</li><li>Pipe server is the exception: it explicitly returns <code>204</code>, <code>200</code>, or <code>500</code>.</li></ul>\n<p>Sources: handlers under <code>broker/src/main/java/com/messaging/broker/handler/</code> and controllers under <code>broker/src/main/java/com/messaging/broker/http/</code>.</p>\n<h2 id=\"authentication\" data-heading=\"Authentication\">Authentication<a class=\"heading-link\" href=\"#/chapter/04-api-catalog/authentication\" aria-label=\"Link to Authentication\">#</a></h2>\n<p>No inbound API authentication implementation was found. <code>AuthTokenClientFilter.java</code> is outbound only. See <a href=\"#/chapter/11-runtime-config/security-and-authentication\">Runtime security</a> before exposing these endpoints.</p>\n<h3 id=\"cloud-server-mirror-separate-repo\" data-heading=\"Cloud-server mirror (separate repo)\">Cloud-server mirror (separate repo)<a class=\"heading-link\" href=\"#/chapter/04-api-catalog/cloud-server-mirror-separate-repo\" aria-label=\"Link to Cloud-server mirror (separate repo)\">#</a></h3>\n<p><code>cloud-server</code> implements the same three served endpoints over its SQLite <code>event</code> table (<code>cloud-server/src/main/java/com/messaging/cloudserver/controller/PipeConsistencyController.java</code>), applying the identical <code>event_size &lt;= 19990</code> serving filter as <code>/pipe/poll</code> and always clamping the watermark to <code>dbMaxOffset</code> (loopback virtual offsets are unrecorded and unverifiable). Hash functions are duplicated and pinned bit-identical via shared test vectors in both repos. Enabled via <code>PIPE_CONSISTENCY_ENABLED=true</code> (fail-closed default false, same as the broker).</p>","headings":[{"level":2,"title":"Pipe API","id":"pipe-api"},{"level":3,"title":"GET /pipe/poll","id":"get-pipepoll"},{"level":2,"title":"Pipe Consistency API","id":"pipe-consistency-api"},{"level":3,"title":"GET /pipe/consistency/head","id":"get-pipeconsistencyhead"},{"level":3,"title":"GET /pipe/consistency/digest","id":"get-pipeconsistencydigest"},{"level":3,"title":"GET /pipe/consistency/bucket","id":"get-pipeconsistencybucket"},{"level":3,"title":"POST /pipe/consistency/classify","id":"post-pipeconsistencyclassify"},{"level":3,"title":"Escalation (clamped parent → in-store verifiers)","id":"escalation-clamped-parent-in-store-verifiers"},{"level":3,"title":"POST /admin/pipe-consistency/check?topic=all|T&target=parent|cloud","id":"post-adminpipe-consistencychecktopicallt-and-targetparentcloud"},{"level":3,"title":"GET /admin/pipe-consistency/report","id":"get-adminpipe-consistencyreport"},{"level":3,"title":"POST /test/consistency/tamper-index — TEMPORARY, delete after validation","id":"post-testconsistencytamper-index-temporary-delete-after-validation"},{"level":2,"title":"Refresh API","id":"refresh-api"},{"level":3,"title":"POST /admin/refresh-topic","id":"post-adminrefresh-topic"},{"level":3,"title":"GET /admin/refresh-status?topic=...","id":"get-adminrefresh-statustopic"},{"level":3,"title":"GET /admin/refresh-current","id":"get-adminrefresh-current"},{"level":2,"title":"Compaction API","id":"compaction-api"},{"level":3,"title":"POST /admin/compaction/trigger","id":"post-admincompactiontrigger"},{"level":3,"title":"GET /admin/compaction/status","id":"get-admincompactionstatus"},{"level":2,"title":"Runtime Logging API","id":"runtime-logging-api"},{"level":2,"title":"Diagnostics And Management","id":"diagnostics-and-management"},{"level":2,"title":"Test Data API","id":"test-data-api"},{"level":3,"title":"POST /test/load-from-sqlite","id":"post-testload-from-sqlite"},{"level":3,"title":"POST /test/inject-messages","id":"post-testinject-messages"},{"level":3,"title":"GET /test/stats","id":"get-teststats"},{"level":2,"title":"TCP Data Plane","id":"tcp-data-plane"},{"level":2,"title":"API Error Behavior","id":"api-error-behavior"},{"level":2,"title":"Authentication","id":"authentication"},{"level":3,"title":"Cloud-server mirror (separate repo)","id":"cloud-server-mirror-separate-repo"}],"plainText":"API Catalog Related: Features, Events, POS state, Runtime config, Risks. Pipe API GET /pipe/poll Controller: pipe/src/main/java/com/messaging/pipe/PipeServer.java Query: offset default 0, limit default 100, topic default price-topic Success: JSON array of MessageRecord No data: HTTP 204 Failure: HTTP 500 with text Service flow: StorageEngine.read(topic, 0, offset, limit) Data model: MessageRecord Consumer: pipe/src/main/java/com/messaging/pipe/HttpPipeConnector.java Important: the connector builds /pipe/poll?offset=...&limit=... without a topic query. Which topic an external parent returns is not confirmed from code. Pipe Consistency API Detect-only audit of this node against its parent/the cloud. Fail-closed: every endpoint answers 404 when pipe.consistency.enabled=false (default); the cloud must mirror the three served endpoints. Design: readme/PIPECONSISTENCYV2.md. GET /pipe/consistency/head O(1) head probe (in-memory storage head, no scan, no semaphore) — lets a child filter verifier candidates before asking anyone to digest-scan. {head: N}, 404 when disabled. GET /pipe/consistency/digest Controller: broker/src/main/java/com/messaging/broker/http/PipeConsistencyController.java Query: topic, watermark (child head), buckets default 64 Success: {parentHead, effectiveWatermark, digests[], counts[]} (1 KB) Guards: 400 invalid params, 429 above max-concurrent-scans Service flow: one streaming CompactionIndex.forEachEntry scan folded by KeyspaceDigest GET /pipe/consistency/bucket Query: topic, watermark, buckets, bucket = comma-separated mismatched bucket ids Success: {entries: {bucketId: [{h: keyHash64, o: latestOffset}, ...]}} — one scan serves all requested buckets Guards: 413 above max-bucket-entries POST /pipe/consistency/classify Body: {topic, watermark, offsets[], keys[]} Success: offsets → record-at-exact-offset physically readable (distinguishes missed data from expired tombstones); keys → PRESENTBEYONDWATERMARK PRESENTATORBELOWWATERMARK ABSENT; authoritative: true only when the answering node's keyspace is complete and never expires (the cloud sets it; POS brokers never do). An authoritative ABSENT proves the child key was fabricated/corrupted (fabricatedKeys, INCONSISTENT); a non-authoritative ABSENT stays a benign extraKeys warning (a freshly provisioned parent may simply lack history). Escalation (clamped parent → in-store verifiers) A clamped watermark (parent behind this node — only possible after a reshuffle) marks the report verificationPending. When the clamp persists escalation.after-clamped-checks consecutive checks, the broker probes the registry-provided verifierCandidates (cached from the existing topology poll; dead/behind candidates skipped via the head probe) and obtains the FULL verdict from the first one whose head covers the watermark (escalatedFrom records the original parent). LAN-only; the cloud is never called. The registry's topology response carries verifierCandidates (omitted when empty — brokers fall back to requestToFollow[1..]). POST /admin/pipe-consistency/check?topic=all T&target=parent cloud Controller: broker/src/main/java/com/messaging/broker/http/PipeConsistencyAdminController.java 202 started (async on compactionExecutor), 409 already running, 503 disabled Service: broker/src/main/java/com/messaging/broker/consistency/PipeConsistencyService.java (single-flight) GET /admin/pipe-consistency/report Latest verdict per topic + bounded history; states CONSISTENT CONSISTENTUPTO INCONSISTENT INCONCLUSIVE UNREACHABLE UNSUPPORTEDPARENT ERROR (INCONCLUSIVE = drill-down cap exceeded at a clamped watermark — divergence may be benign child-ahead, e.g. the dev cloud's loopback replay); counts for missing/stale/ zombie/fabricated/lagging/extra/child-newer keys and refreshRecommended POST /test/consistency/tamper-index — TEMPORARY, delete after validation Controller: broker/src/main/java/com/messaging/broker/http/ConsistencyTamperTestController.java Query: topic, removeCount (delete first N compaction-index entries → next check vs cloud shows missingKeys), addCount (insert fake keys at the topic head → fabricatedKeys) Fault-injection tool for live INCONSISTENT validation only; no specs by design. Refresh API Controller: broker/src/main/java/com/messaging/broker/http/RefreshController.java. POST /admin/refresh-topic Body forms: {\"topic\":\"prices-v1,reference-data-v5\"} {\"topics\":[\"prices-v1\",\"reference-data-v5\"]} Returns immediately with success, message, topics, and status: INITIATED. Empty/malformed input returns a map with success:false; explicit HTTP error status is not set. Service: RefreshCoordinator.startRefresh. State flow: POS state. GET /admin/refresh-status?topic=... Returns state, counts, timestamps, and per-consumer RESET/READY/replay/offset details from RefreshContext. GET /admin/refresh-current Returns one arbitrary current context via activeRefreshes.values().stream().findFirst(). With concurrent topic refreshes it is not a complete batch view. Compaction API Controller: broker/src/main/java/com/messaging/broker/http/CompactionController.java. POST /admin/compaction/trigger Submits an asynchronous compaction run. Its preparation callback force-rolls every active topic segment before compaction. If another run is active, triggered:false. Response: triggered, sealedTopics, sealErrors, message. Service/data links: Compaction, Segment storage. GET /admin/compaction/status Returns per-topic sealed segment count/bytes, total sealed count, and an operator tip. Runtime Logging API Controller: broker/src/main/java/com/messaging/broker/http/LoggingController.java. GET /admin/logging/features GET /admin/logging/feature/{feature} POST /admin/logging/feature/{feature}?level=DEBUG INFO ... RESET Service: broker/src/main/java/com/messaging/broker/monitoring/RuntimeLogLevelService.java. The test consumer exposes the same paths from test-consumer/src/main/java/com/example/consumer/http/LoggingController.java. Diagnostics And Management Controller: broker/src/main/java/com/messaging/broker/http/ThreadDiagnosticsController.java. Method/path Purpose --- --- GET /diagnostics/threads/summary Thread counts, states, deadlock/problem counts GET /diagnostics/threads/top-cpu?limit=N Highest cumulative CPU-time threads GET /diagnostics/threads/problematic Monitor-classified blocked/waiting threads GET /diagnostics/threads/dump Plain-text thread dump GET /diagnostics/threads/{threadId} Thread details and stack GET /diagnostics/threads/deadlocks Deadlock report GET /diagnostics/threads/top-memory?limit=N Per-thread allocation estimates GET /diagnostics/threads/resources?category=... Resource details GET /diagnostics/threads/by-category Aggregated resource categories Micronaut management enables /health, /prometheus, and /metrics in broker/src/main/resources/application.yml. Test Data API Controller: broker/src/main/java/com/messaging/broker/http/TestDataController.java. POST /test/load-from-sqlite Body: sqliteFilePath, optional tableName default messages, optional topic default price-topic. The controller opens jdbc:sqlite:<path>, executes SELECT  FROM <tableName>, converts each row to JSON text, and appends it. The table name is concatenated directly; see Security risks. POST /test/inject-messages Body: optional count, topic, prefix; appends generated records directly to storage. GET /test/stats Returns placeholder/fake statistics. This is marked TODO in TestDataController.java and must not be used as an operational source of truth. TCP Data Plane Message envelope: [type:1][messageId:8][payloadLength:4][payload], implemented by BinaryMessageEncoder.java and BinaryMessageDecoder.java. Type Direction Payload/behavior Handler --- --- --- --- DATA producer -> broker JSON producer record DataHandler.java ACK broker -> producer/client Empty or generic legacy ACK client connection / legacy state SUBSCRIBE consumer -> broker Modern {topic,group} or legacy adapter JSON SubscribeHandler.java COMMITOFFSET client -> broker JSON {topic,group,offset} CommitOffsetHandler.java RESET broker -> consumer Topic bytes client onReset READY broker -> consumer Topic bytes or empty startup payload client onReady DISCONNECT either/control Empty client reconnect logic HEARTBEAT defined No registered broker handler found BATCHHEADER broker -> consumer count, bytes, topic, group; raw bytes follow ZeroCopyBatchDecoder.java BATCHACK consumer -> broker topic/group lengths and bytes BatchAckHandler.java RESETACK consumer -> broker topic/group lengths and bytes ResetAckHandler.java READYACK consumer -> broker topic/group lengths and bytes ReadyAckHandler.java Definitions: common/src/main/java/com/messaging/common/model/BrokerMessage.java. Detailed flow: Event chapter. API Error Behavior TCP validation failures generally close the connection. ACK handler malformed length fields close the connection; unexpected legacy group lookup soft-fails. HTTP controllers generally return maps with error fields rather than HttpResponse error status. Pipe server is the exception: it explicitly returns 204, 200, or 500. Sources: handlers under broker/src/main/java/com/messaging/broker/handler/ and controllers under broker/src/main/java/com/messaging/broker/http/. Authentication No inbound API authentication implementation was found. AuthTokenClientFilter.java is outbound only. See Runtime security before exposing these endpoints. Cloud-server mirror (separate repo) cloud-server implements the same three served endpoints over its SQLite event table (cloud-server/src/main/java/com/messaging/cloudserver/controller/PipeConsistencyController.java), applying the identical eventsize <= 19990 serving filter as /pipe/poll and always clamping the watermark to dbMaxOffset (loopback virtual offsets are unrecorded and unverifiable). Hash functions are duplicated and pinned bit-identical via shared test vectors in both repos. Enabled via PIPECONSISTENCYENABLED=true (fail-closed default false, same as the broker).","lineCount":217},{"order":5,"file":"05-data-model.md","slug":"05-data-model","title":"Data Model","html":"<h1 id=\"data-model\" data-heading=\"Data Model\">Data Model<a class=\"heading-link\" href=\"#/chapter/05-data-model/data-model\" aria-label=\"Link to Data Model\">#</a></h1>\n<p>Related: <a href=\"#/chapter/03-feature-catalog\">Features</a>, <a href=\"#/chapter/06-event-kafka-flow\">Events</a>, <a href=\"#/chapter/07-pos-machine-state\">POS state</a>, <a href=\"#/chapter/08-compaction-handling\">Compaction</a>, <a href=\"#/chapter/09-error-retry-recovery\">Recovery</a>.</p>\n<h2 id=\"record-models\" data-heading=\"Record Models\">Record Models<a class=\"heading-link\" href=\"#/chapter/05-data-model/record-models\" aria-label=\"Link to Record Models\">#</a></h2>\n<h3 id=\"messagerecord\" data-heading=\"MessageRecord\"><code>MessageRecord</code><a class=\"heading-link\" href=\"#/chapter/05-data-model/messagerecord\" aria-label=\"Link to MessageRecord\">#</a></h3>\n<p>Source: <code>common/src/main/java/com/messaging/common/model/MessageRecord.java</code>.</p>\n<p>Fields used by the system:</p>\n<ul><li><code>offset</code></li><li><code>topic</code></li><li><code>partition</code></li><li><code>msgKey</code></li><li><code>eventType</code></li><li>nullable <code>data</code></li><li><code>createdAt</code></li><li><code>contentType</code></li></ul>\n<p>Producer construction is in <code>broker/src/main/java/com/messaging/broker/handler/DataHandler.java</code>; pipe JSON binding is in <code>pipe/src/main/java/com/messaging/pipe/HttpPipeConnector.java</code>.</p>\n<h3 id=\"consumerrecord\" data-heading=\"ConsumerRecord\"><code>ConsumerRecord</code><a class=\"heading-link\" href=\"#/chapter/05-data-model/consumerrecord\" aria-label=\"Link to ConsumerRecord\">#</a></h3>\n<p>Source: <code>common/src/main/java/com/messaging/common/model/ConsumerRecord.java</code>.</p>\n<p>The modern client receives key, event type, data, and timestamp. Offset is tracked by the broker rather than included in each decoded client record. The current zero-copy decoder confirms this in <code>network/src/main/java/com/messaging/network/codec/ZeroCopyBatchDecoder.java</code>.</p>\n<h3 id=\"event-types\" data-heading=\"Event Types\">Event Types<a class=\"heading-link\" href=\"#/chapter/05-data-model/event-types\" aria-label=\"Link to Event Types\">#</a></h3>\n<p><code>common/src/main/java/com/messaging/common/model/EventType.java</code> defines:</p>\n<ul><li><code>MESSAGE</code>, wire code <code>'M'</code></li><li><code>DELETE</code>, wire code <code>'D'</code></li></ul>\n<p>DELETE records have null data in producer ingestion and act as tombstones for compaction.</p>\n<h2 id=\"segment-storage\" data-heading=\"Segment Storage\">Segment Storage<a class=\"heading-link\" href=\"#/chapter/05-data-model/segment-storage\" aria-label=\"Link to Segment Storage\">#</a></h2>\n<p>Sources:</p>\n<ul><li><code>storage/src/main/java/com/messaging/storage/segment/Segment.java</code></li><li><code>storage/src/main/java/com/messaging/storage/segment/SegmentManager.java</code></li><li><code>storage/src/main/java/com/messaging/storage/segment/DefaultStorageRecoveryService.java</code></li></ul>\n<p>Directory layout:</p>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>&lt;dataDir&gt;/\n  &lt;topic&gt;/\n    segment_metadata.db\n    partition-0/\n      00000000000000000000.log\n      00000000000000000000.index\n      00000000000000000000.compacted.log\n      00000000000000000000.compacted.index\n      *.compacting.*</code></pre></div>\n<p>Only partition <code>0</code> is used by broker handlers and pipe code.</p>\n<h3 id=\"log-format\" data-heading=\"Log Format\">Log Format<a class=\"heading-link\" href=\"#/chapter/05-data-model/log-format\" aria-label=\"Link to Log Format\">#</a></h3>\n<p>The file starts with a <code>MLOG</code> header and version. Each current-format record is:</p>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>[keyLen:4][key:keyLen][eventType:1][dataLen:4][data:dataLen][timestampMillis:8]</code></pre></div>\n<p>Offsets are not stored in the log payload. Source: <code>Segment.java</code>.</p>\n<h3 id=\"index-format\" data-heading=\"Index Format\">Index Format<a class=\"heading-link\" href=\"#/chapter/05-data-model/index-format\" aria-label=\"Link to Index Format\">#</a></h3>\n<p>The file starts with <code>MIDX</code> and a version.</p>\n<p>Current v2 entry:</p>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>[offset:8][logPosition:4][recordSize:4]</code></pre></div>\n<p>Recovery also recognizes the older 20-byte v1 entry with a CRC field that is ignored by current reads. Source: <code>Segment.java</code>.</p>\n<p>The index is dense: one entry per log record. <code>Segment.findPositionForOffset</code> binary-searches for the first offset greater than or equal to the requested offset, so sparse parent offsets are supported.</p>\n<h3 id=\"segment-lifecycle\" data-heading=\"Segment Lifecycle\">Segment Lifecycle<a class=\"heading-link\" href=\"#/chapter/05-data-model/segment-lifecycle\" aria-label=\"Link to Segment Lifecycle\">#</a></h3>\n<ul><li>Active segment accepts appends.</li><li>Full or explicitly rolled segment becomes sealed/inactive.</li><li>Reads may walk sealed and active segments.</li><li><code>getBatch</code> returns data from one segment and does not cross a segment boundary.</li><li>File-backed <code>DeliveryBatch.transferTo</code> uses the segment's channel.</li><li>Compaction atomically replaces sealed segment ranges.</li></ul>\n<p>Source: <code>SegmentManager.java</code>.</p>\n<h3 id=\"durability\" data-heading=\"Durability\">Durability<a class=\"heading-link\" href=\"#/chapter/05-data-model/durability\" aria-label=\"Link to Durability\">#</a></h3>\n<p>The active segment is periodically forced by <code>FileChannelStorageEngine</code>. Segment seal also forces files. There is no force per record. Configuration is in <code>broker/src/main/resources/application.yml</code>.</p>\n<p>Recovery:</p>\n<ul><li>validates file headers;</li><li>truncates orphan log bytes when the index is missing or shorter;</li><li>truncates index entries that point beyond the log;</li><li>ignores/removes staging files;</li><li>reopens the last recovered segment as active where applicable.</li></ul>\n<p>Tests: <code>storage/src/unitTest/groovy/com/messaging/storage/segment/SegmentCrashRecoverySpec.groovy</code> and <code>storage/src/integrationTest/groovy/com/messaging/storage/segment/DefaultStorageRecoveryServiceIntegrationSpec.groovy</code>.</p>\n<h2 id=\"sqlite-segment-metadata\" data-heading=\"SQLite Segment Metadata\">SQLite Segment Metadata<a class=\"heading-link\" href=\"#/chapter/05-data-model/sqlite-segment-metadata\" aria-label=\"Link to SQLite Segment Metadata\">#</a></h2>\n<p>No migration directory or <code>.sql</code> migration file exists. Schema creation is runtime code in <code>storage/src/main/java/com/messaging/storage/metadata/SegmentMetadataStore.java</code>.</p>\n<p>Table <code>segment_metadata</code>:</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Column</th>\n<th>Meaning</th>\n</tr></thead><tbody>\n<tr>\n<td><code>id</code></td>\n<td>SQLite primary key</td>\n</tr>\n<tr>\n<td><code>topic</code></td>\n<td>Topic name</td>\n</tr>\n<tr>\n<td><code>partition</code></td>\n<td>Partition number</td>\n</tr>\n<tr>\n<td><code>base_offset</code></td>\n<td>First segment offset</td>\n</tr>\n<tr>\n<td><code>max_offset</code></td>\n<td>Highest indexed offset</td>\n</tr>\n<tr>\n<td><code>log_file_path</code></td>\n<td>Log path</td>\n</tr>\n<tr>\n<td><code>index_file_path</code></td>\n<td>Index path</td>\n</tr>\n<tr>\n<td><code>size_bytes</code></td>\n<td>Segment size</td>\n</tr>\n<tr>\n<td><code>message_count</code></td>\n<td>Record count</td>\n</tr>\n<tr>\n<td><code>created_at</code></td>\n<td>Creation time</td>\n</tr>\n<tr>\n<td><code>updated_at</code></td>\n<td>Update time</td>\n</tr>\n</tbody></table></div>\n<p>Indexes:</p>\n<ul><li><code>idx_topic_partition(topic, partition)</code></li><li><code>idx_base_offset(base_offset)</code></li></ul>\n<p>The metadata store is updated periodically and on roll/close by <code>SegmentManager</code>.</p>\n<h2 id=\"rocksdb\" data-heading=\"RocksDB\">RocksDB<a class=\"heading-link\" href=\"#/chapter/05-data-model/rocksdb\" aria-label=\"Link to RocksDB\">#</a></h2>\n<p>Source: <code>broker/src/main/java/com/messaging/broker/compaction/SharedRocksDb.java</code>.</p>\n<p>One RocksDB instance is shared:</p>\n<ul><li>default column family: per-record ACK data</li><li><code>compaction</code> column family: latest-key index and compaction checkpoints</li></ul>\n<p>The configured default path is <code>${DATA_DIR}/ack-store</code> in <code>broker/src/main/resources/application.yml</code>. A single configured block cache is shared across column families.</p>\n<h3 id=\"ack-keyvalue\" data-heading=\"ACK Key/Value\">ACK Key/Value<a class=\"heading-link\" href=\"#/chapter/05-data-model/ack-keyvalue\" aria-label=\"Link to ACK Key/Value\">#</a></h3>\n<p>Source: <code>broker/src/main/java/com/messaging/broker/ack/RocksDbAckStore.java</code>.</p>\n<ul><li>Key: topic, group, and a zero-padded 20-digit offset</li><li>Value: 16 bytes containing offset and ACK timestamp</li><li>Operations: single/batch put, get, prefix clear, range scan</li></ul>\n<h3 id=\"compaction-data\" data-heading=\"Compaction Data\">Compaction Data<a class=\"heading-link\" href=\"#/chapter/05-data-model/compaction-data\" aria-label=\"Link to Compaction Data\">#</a></h3>\n<p>Sources: <code>RocksDbCompactionIndex.java</code>, <code>CompactionCheckpointStore.java</code>.</p>\n<p>The latest key state records the latest offset/timestamp per <code>topic + msgKey</code>; checkpoints record how far a topic's sealed segments were compacted.</p>\n<h2 id=\"properties-state-files\" data-heading=\"Properties State Files\">Properties State Files<a class=\"heading-link\" href=\"#/chapter/05-data-model/properties-state-files\" aria-label=\"Link to Properties State Files\">#</a></h2>\n<p>All files live under <code>broker.storage.data-dir</code>. Atomic write behavior is implemented by <code>broker/src/main/java/com/messaging/broker/consumer/PropertiesFileStore.java</code>: write temporary file, force it, then rename.</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>File</th>\n<th>Owner</th>\n<th>Key semantics</th>\n</tr></thead><tbody>\n<tr>\n<td><code>consumer-offsets.properties</code></td>\n<td><code>ConsumerOffsetTracker.java</code></td>\n<td><code>group:topic -&gt; offset</code>; local annotation consumers use their consumer ID</td>\n</tr>\n<tr>\n<td><code>delivery-state.properties</code></td>\n<td><code>DeliveryStateStore.java</code></td>\n<td><code>&lt;group:topic&gt;.offset</code> and <code>.until</code></td>\n</tr>\n<tr>\n<td><code>data-refresh-state.properties</code></td>\n<td><code>RefreshStateStore.java</code></td>\n<td>Active topic contexts, ACK sets, timestamps, replay state</td>\n</tr>\n<tr>\n<td><code>pipe-offset.properties</code></td>\n<td><code>HttpPipeConnector.java</code></td>\n<td><code>pipe.current.offset</code></td>\n</tr>\n<tr>\n<td><code>topology.properties</code></td>\n<td><code>TopologyPropertiesStore.java</code></td>\n<td>Last role and parent</td>\n</tr>\n</tbody></table></div>\n<p><code>FlushingPropertiesStore.java</code> adds periodic flushing. <code>ConsumerOffsetTracker</code> also rate-limits synchronous flushes to one per second.</p>\n<h2 id=\"consumer-state-models\" data-heading=\"Consumer State Models\">Consumer State Models<a class=\"heading-link\" href=\"#/chapter/05-data-model/consumer-state-models\" aria-label=\"Link to Consumer State Models\">#</a></h2>\n<ul><li><code>ConsumerKey(clientId, topic, group)</code>: socket-session registration.</li><li><code>DeliveryKey(group, topic)</code>: durable/logical delivery state.</li><li><code>RemoteConsumer</code>: current in-memory offset, legacy flag, failure/backoff state, scheduled task.</li><li><code>PendingDelivery</code>: immutable generation snapshot for ACK/timeout arbitration.</li><li><code>RefreshContext</code>: state, expected consumers, RESET/READY ACKs, replay offsets/times, downtime.</li></ul>\n<p>Sources: <code>broker/src/main/java/com/messaging/broker/model/</code> and <code>broker/src/main/java/com/messaging/broker/consumer/</code>.</p>\n<h2 id=\"offset-semantics\" data-heading=\"Offset Semantics\">Offset Semantics<a class=\"heading-link\" href=\"#/chapter/05-data-model/offset-semantics\" aria-label=\"Link to Offset Semantics\">#</a></h2>\n<ul><li>Modern batch delivery sets pending and persisted offsets to <code>lastOriginalBatchOffset + 1</code>: <code>BatchDeliveryService.java</code>.</li><li>Legacy merged delivery commits the highest acknowledged record offset per topic: <code>LegacyConsumerDeliveryManager.java</code>.</li><li>Pipe state is the last successfully handled parent offset: <code>HttpPipeConnector.java</code>.</li><li>Segment head is the last stored offset: <code>StorageEngine.getCurrentOffset</code>.</li></ul>\n<p>These meanings are not interchangeable. See <a href=\"#/chapter/13-risk-and-edge-cases/offset-semantics\">Offset risks</a>.</p>\n<h2 id=\"data-ownership-and-cleanup\" data-heading=\"Data Ownership And Cleanup\">Data Ownership And Cleanup<a class=\"heading-link\" href=\"#/chapter/05-data-model/data-ownership-and-cleanup\" aria-label=\"Link to Data Ownership And Cleanup\">#</a></h2>\n<ul><li><code>DeliveryBatch</code> ownership transfers to <code>NettyTcpServer.sendBatch</code>, which closes it from <code>BatchPayloadFileRegion.deallocate</code>.</li><li>Compaction closes/deletes replaced segments after swapping the segment map.</li><li><code>SharedRocksDb</code>, storage engines, property flushers, and executors have shutdown hooks.</li><li>Test-created temp directories are managed by test harnesses; production data retention outside tombstone compaction is not implemented as a general time/size retention policy.</li></ul>\n<p>General message retention behavior is <strong>not confirmed from code</strong> beyond compaction of superseded/tombstone records.</p>","headings":[{"level":2,"title":"Record Models","id":"record-models"},{"level":3,"title":"MessageRecord","id":"messagerecord"},{"level":3,"title":"ConsumerRecord","id":"consumerrecord"},{"level":3,"title":"Event Types","id":"event-types"},{"level":2,"title":"Segment Storage","id":"segment-storage"},{"level":3,"title":"Log Format","id":"log-format"},{"level":3,"title":"Index Format","id":"index-format"},{"level":3,"title":"Segment Lifecycle","id":"segment-lifecycle"},{"level":3,"title":"Durability","id":"durability"},{"level":2,"title":"SQLite Segment Metadata","id":"sqlite-segment-metadata"},{"level":2,"title":"RocksDB","id":"rocksdb"},{"level":3,"title":"ACK Key/Value","id":"ack-keyvalue"},{"level":3,"title":"Compaction Data","id":"compaction-data"},{"level":2,"title":"Properties State Files","id":"properties-state-files"},{"level":2,"title":"Consumer State Models","id":"consumer-state-models"},{"level":2,"title":"Offset Semantics","id":"offset-semantics"},{"level":2,"title":"Data Ownership And Cleanup","id":"data-ownership-and-cleanup"}],"plainText":"Data Model Related: Features, Events, POS state, Compaction, Recovery. Record Models MessageRecord Source: common/src/main/java/com/messaging/common/model/MessageRecord.java. Fields used by the system: offset topic partition msgKey eventType nullable data createdAt contentType Producer construction is in broker/src/main/java/com/messaging/broker/handler/DataHandler.java; pipe JSON binding is in pipe/src/main/java/com/messaging/pipe/HttpPipeConnector.java. ConsumerRecord Source: common/src/main/java/com/messaging/common/model/ConsumerRecord.java. The modern client receives key, event type, data, and timestamp. Offset is tracked by the broker rather than included in each decoded client record. The current zero-copy decoder confirms this in network/src/main/java/com/messaging/network/codec/ZeroCopyBatchDecoder.java. Event Types common/src/main/java/com/messaging/common/model/EventType.java defines: MESSAGE, wire code 'M' DELETE, wire code 'D' DELETE records have null data in producer ingestion and act as tombstones for compaction. Segment Storage Sources: storage/src/main/java/com/messaging/storage/segment/Segment.java storage/src/main/java/com/messaging/storage/segment/SegmentManager.java storage/src/main/java/com/messaging/storage/segment/DefaultStorageRecoveryService.java Directory layout: Only partition 0 is used by broker handlers and pipe code. Log Format The file starts with a MLOG header and version. Each current-format record is: Offsets are not stored in the log payload. Source: Segment.java. Index Format The file starts with MIDX and a version. Current v2 entry: Recovery also recognizes the older 20-byte v1 entry with a CRC field that is ignored by current reads. Source: Segment.java. The index is dense: one entry per log record. Segment.findPositionForOffset binary-searches for the first offset greater than or equal to the requested offset, so sparse parent offsets are supported. Segment Lifecycle Active segment accepts appends. Full or explicitly rolled segment becomes sealed/inactive. Reads may walk sealed and active segments. getBatch returns data from one segment and does not cross a segment boundary. File-backed DeliveryBatch.transferTo uses the segment's channel. Compaction atomically replaces sealed segment ranges. Source: SegmentManager.java. Durability The active segment is periodically forced by FileChannelStorageEngine. Segment seal also forces files. There is no force per record. Configuration is in broker/src/main/resources/application.yml. Recovery: validates file headers; truncates orphan log bytes when the index is missing or shorter; truncates index entries that point beyond the log; ignores/removes staging files; reopens the last recovered segment as active where applicable. Tests: storage/src/unitTest/groovy/com/messaging/storage/segment/SegmentCrashRecoverySpec.groovy and storage/src/integrationTest/groovy/com/messaging/storage/segment/DefaultStorageRecoveryServiceIntegrationSpec.groovy. SQLite Segment Metadata No migration directory or .sql migration file exists. Schema creation is runtime code in storage/src/main/java/com/messaging/storage/metadata/SegmentMetadataStore.java. Table segmentmetadata: Column Meaning --- --- id SQLite primary key topic Topic name partition Partition number baseoffset First segment offset maxoffset Highest indexed offset logfilepath Log path indexfilepath Index path sizebytes Segment size messagecount Record count createdat Creation time updatedat Update time Indexes: idxtopicpartition(topic, partition) idxbaseoffset(baseoffset) The metadata store is updated periodically and on roll/close by SegmentManager. RocksDB Source: broker/src/main/java/com/messaging/broker/compaction/SharedRocksDb.java. One RocksDB instance is shared: default column family: per-record ACK data compaction column family: latest-key index and compaction checkpoints The configured default path is ${DATADIR}/ack-store in broker/src/main/resources/application.yml. A single configured block cache is shared across column families. ACK Key/Value Source: broker/src/main/java/com/messaging/broker/ack/RocksDbAckStore.java. Key: topic, group, and a zero-padded 20-digit offset Value: 16 bytes containing offset and ACK timestamp Operations: single/batch put, get, prefix clear, range scan Compaction Data Sources: RocksDbCompactionIndex.java, CompactionCheckpointStore.java. The latest key state records the latest offset/timestamp per topic + msgKey; checkpoints record how far a topic's sealed segments were compacted. Properties State Files All files live under broker.storage.data-dir. Atomic write behavior is implemented by broker/src/main/java/com/messaging/broker/consumer/PropertiesFileStore.java: write temporary file, force it, then rename. File Owner Key semantics --- --- --- consumer-offsets.properties ConsumerOffsetTracker.java group:topic -> offset; local annotation consumers use their consumer ID delivery-state.properties DeliveryStateStore.java <group:topic>.offset and .until data-refresh-state.properties RefreshStateStore.java Active topic contexts, ACK sets, timestamps, replay state pipe-offset.properties HttpPipeConnector.java pipe.current.offset topology.properties TopologyPropertiesStore.java Last role and parent FlushingPropertiesStore.java adds periodic flushing. ConsumerOffsetTracker also rate-limits synchronous flushes to one per second. Consumer State Models ConsumerKey(clientId, topic, group): socket-session registration. DeliveryKey(group, topic): durable/logical delivery state. RemoteConsumer: current in-memory offset, legacy flag, failure/backoff state, scheduled task. PendingDelivery: immutable generation snapshot for ACK/timeout arbitration. RefreshContext: state, expected consumers, RESET/READY ACKs, replay offsets/times, downtime. Sources: broker/src/main/java/com/messaging/broker/model/ and broker/src/main/java/com/messaging/broker/consumer/. Offset Semantics Modern batch delivery sets pending and persisted offsets to lastOriginalBatchOffset + 1: BatchDeliveryService.java. Legacy merged delivery commits the highest acknowledged record offset per topic: LegacyConsumerDeliveryManager.java. Pipe state is the last successfully handled parent offset: HttpPipeConnector.java. Segment head is the last stored offset: StorageEngine.getCurrentOffset. These meanings are not interchangeable. See Offset risks. Data Ownership And Cleanup DeliveryBatch ownership transfers to NettyTcpServer.sendBatch, which closes it from BatchPayloadFileRegion.deallocate. Compaction closes/deletes replaced segments after swapping the segment map. SharedRocksDb, storage engines, property flushers, and executors have shutdown hooks. Test-created temp directories are managed by test harnesses; production data retention outside tombstone compaction is not implemented as a general time/size retention policy. General message retention behavior is not confirmed from code beyond compaction of superseded/tombstone records.","lineCount":205},{"order":6,"file":"06-event-kafka-flow.md","slug":"06-event-kafka-flow","title":"Event And Kafka-Like Flow","html":"<h1 id=\"event-and-kafka-like-flow\" data-heading=\"Event And Kafka-Like Flow\">Event And Kafka-Like Flow<a class=\"heading-link\" href=\"#/chapter/06-event-kafka-flow/event-and-kafka-like-flow\" aria-label=\"Link to Event And Kafka-Like Flow\">#</a></h1>\n<p>Related: <a href=\"#/chapter/01-system-overview\">Overview</a>, <a href=\"#/chapter/03-feature-catalog\">Features</a>, <a href=\"#/chapter/05-data-model\">Data model</a>, <a href=\"#/chapter/07-pos-machine-state\">POS state</a>, <a href=\"#/chapter/08-compaction-handling\">Compaction</a>.</p>\n<h2 id=\"kafka-clarification\" data-heading=\"Kafka Clarification\">Kafka Clarification<a class=\"heading-link\" href=\"#/chapter/06-event-kafka-flow/kafka-clarification\" aria-label=\"Link to Kafka Clarification\">#</a></h2>\n<p>There is no Apache Kafka dependency, producer, consumer, broker protocol, or Kafka topic configuration in this repository. &quot;Kafka-like&quot; means the code implements:</p>\n<ul><li>named topics;</li><li>partition <code>0</code>;</li><li>monotonic/sparse offsets;</li><li>consumer groups;</li><li>committed offsets;</li><li>batched delivery and ACK;</li><li>keyed compaction and tombstones.</li></ul>\n<p>Sources: <code>settings.gradle</code>, module build files, <code>StorageEngine.java</code>, <code>ConsumerOffsetTracker.java</code>, and compaction package.</p>\n<h2 id=\"modern-wire-protocol\" data-heading=\"Modern Wire Protocol\">Modern Wire Protocol<a class=\"heading-link\" href=\"#/chapter/06-event-kafka-flow/modern-wire-protocol\" aria-label=\"Link to Modern Wire Protocol\">#</a></h2>\n<p>Envelope:</p>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>[messageType:1][messageId:8][payloadLength:4][payload]</code></pre></div>\n<p>Definitions: <code>common/src/main/java/com/messaging/common/model/BrokerMessage.java</code>. Codec: <code>network/src/main/java/com/messaging/network/codec/BinaryMessageEncoder.java</code> and <code>BinaryMessageDecoder.java</code>.</p>\n<p>Codes:</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Code</th>\n<th>Type</th>\n</tr></thead><tbody>\n<tr>\n<td><code>0x01</code></td>\n<td><code>DATA</code></td>\n</tr>\n<tr>\n<td><code>0x02</code></td>\n<td><code>ACK</code></td>\n</tr>\n<tr>\n<td><code>0x03</code></td>\n<td><code>SUBSCRIBE</code></td>\n</tr>\n<tr>\n<td><code>0x04</code></td>\n<td><code>COMMIT_OFFSET</code></td>\n</tr>\n<tr>\n<td><code>0x05</code></td>\n<td><code>RESET</code></td>\n</tr>\n<tr>\n<td><code>0x06</code></td>\n<td><code>READY</code></td>\n</tr>\n<tr>\n<td><code>0x07</code></td>\n<td><code>DISCONNECT</code></td>\n</tr>\n<tr>\n<td><code>0x08</code></td>\n<td><code>HEARTBEAT</code></td>\n</tr>\n<tr>\n<td><code>0x09</code></td>\n<td><code>BATCH_HEADER</code></td>\n</tr>\n<tr>\n<td><code>0x0A</code></td>\n<td><code>BATCH_ACK</code></td>\n</tr>\n<tr>\n<td><code>0x0B</code></td>\n<td><code>RESET_ACK</code></td>\n</tr>\n<tr>\n<td><code>0x0C</code></td>\n<td><code>READY_ACK</code></td>\n</tr>\n</tbody></table></div>\n<p>No <code>HEARTBEAT</code> handler is registered by <code>HandlerInitializer.java</code>.</p>\n<h2 id=\"producer-flow\" data-heading=\"Producer Flow\">Producer Flow<a class=\"heading-link\" href=\"#/chapter/06-event-kafka-flow/producer-flow\" aria-label=\"Link to Producer Flow\">#</a></h2>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>Producer DATA JSON\n  -&gt; BinaryMessageDecoder\n  -&gt; ServerMessageHandler\n  -&gt; BrokerService handler registry\n  -&gt; DataHandler\n  -&gt; StorageEngine.append(topic, 0, record)\n  -&gt; CompactionIndex.updateKey\n  -&gt; ACK(messageId)</code></pre></div>\n<p>Sources: <code>NettyTcpServer.java</code>, <code>ServerMessageHandler.java</code>, <code>BrokerService.java</code>, <code>DataHandler.java</code>.</p>\n<p>Producer JSON fields:</p>\n<ul><li>required: <code>topic</code></li><li>optional: <code>msg_key</code>, <code>event_type</code>, <code>data</code></li></ul>\n<p><code>DELETE</code> creates a null-data tombstone. Other event-type text becomes <code>MESSAGE</code>.</p>\n<h2 id=\"pipe-replication-flow\" data-heading=\"Pipe Replication Flow\">Pipe Replication Flow<a class=\"heading-link\" href=\"#/chapter/06-event-kafka-flow/pipe-replication-flow\" aria-label=\"Link to Pipe Replication Flow\">#</a></h2>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>TopologyManager\n  -&gt; CloudRegistryClient GET /registry/topology?nodeId=...\n  -&gt; select/probe parent\n  -&gt; HttpPipeConnector GET /pipe/poll?offset=...&amp;limit=...\n  -&gt; streaming JSON MessageRecord\n  -&gt; BrokerService.handlePipeMessage\n  -&gt; duplicate-offset guard\n  -&gt; StorageEngine.append\n  -&gt; CompactionIndex.updateKey\n  -&gt; persist pipe.current.offset</code></pre></div>\n<p>Sources: <code>CloudRegistryClient.java</code>, <code>TopologyManager.java</code>, <code>HttpPipeConnector.java</code>, <code>BrokerService.java</code>.</p>\n<p>The handler returns a boolean. The connector advances only through the last successful record and retries failures.</p>\n<h2 id=\"modern-subscription-flow\" data-heading=\"Modern Subscription Flow\">Modern Subscription Flow<a class=\"heading-link\" href=\"#/chapter/06-event-kafka-flow/modern-subscription-flow\" aria-label=\"Link to Modern Subscription Flow\">#</a></h2>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>SUBSCRIBE {topic, group}\n  -&gt; SubscribeHandler\n  -&gt; restore group:topic offset\n  -&gt; register RemoteConsumer\n  -&gt; ACK subscribe\n  -&gt; READY(topic)\n  &lt;- READY_ACK(topic, group)\n  -&gt; mark readiness</code></pre></div>\n<p>During active refresh, <code>SubscribeHandler</code> follows late-joiner rules instead of sending an ordinary startup READY. Source: <code>broker/src/main/java/com/messaging/broker/handler/SubscribeHandler.java</code>.</p>\n<h2 id=\"modern-delivery-flow\" data-heading=\"Modern Delivery Flow\">Modern Delivery Flow<a class=\"heading-link\" href=\"#/chapter/06-event-kafka-flow/modern-delivery-flow\" aria-label=\"Link to Modern Delivery Flow\">#</a></h2>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>DeliveryScheduler\n  -&gt; RefreshGatePolicy\n  -&gt; WatermarkGatePolicy\n  -&gt; BatchDeliveryService gates\n  -&gt; BatchReadableStorage.getBatch\n  -&gt; optional compaction delivery filter\n  -&gt; BATCH_HEADER(count, bytes, topic, group)\n  -&gt; raw segment bytes via FileRegion\n  -&gt; ZeroCopyBatchDecoder\n  -&gt; BatchDecodedEvent\n  -&gt; client BatchAckHandler sends BATCH_ACK(topic, group)\n  -&gt; broker BatchAckHandler on ackExecutor\n  -&gt; BatchAckService commits next offset\n  -&gt; async per-record RocksDB ACK write</code></pre></div>\n<p>Sources:</p>\n<ul><li>Scheduling: <code>DeliveryScheduler.java</code>, <code>TopicFairScheduler.java</code></li><li>Broker delivery: <code>BatchDeliveryService.java</code></li><li>Transport: <code>NettyTcpServer.java</code></li><li>Client decode/ACK: <code>ZeroCopyBatchDecoder.java</code>, network <code>BatchAckHandler.java</code></li><li>Broker ACK: broker <code>BatchAckHandler.java</code>, <code>BatchAckService.java</code></li></ul>\n<h3 id=\"batch-header\" data-heading=\"Batch Header\">Batch Header<a class=\"heading-link\" href=\"#/chapter/06-event-kafka-flow/batch-header\" aria-label=\"Link to Batch Header\">#</a></h3>\n<p>Payload:</p>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>[recordCount:4][totalBytes:8]\n[topicLen:4][topic]\n[groupLen:4][group]</code></pre></div>\n<p>Raw records use the current segment log format without offsets or CRC. <code>ZeroCopyBatchDecoder.decodeZeroCopyBatch</code> is the authoritative client parser.</p>\n<p>If the decoder parses fewer records than advertised, it does not ACK and closes the channel to force replay.</p>\n<h2 id=\"legacy-wire-protocol\" data-heading=\"Legacy Wire Protocol\">Legacy Wire Protocol<a class=\"heading-link\" href=\"#/chapter/06-event-kafka-flow/legacy-wire-protocol\" aria-label=\"Link to Legacy Wire Protocol\">#</a></h2>\n<p>Protocol detection is based on the first byte. A legacy connection must begin with <code>REGISTER</code> ordinal <code>0</code>. Sources: <code>DefaultProtocolDetectionService.java</code>, <code>ProtocolDetectionDecoder.java</code>.</p>\n<p>Legacy event ordinals from <code>network/src/main/java/com/messaging/network/legacy/events/EventType.java</code>:</p>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>REGISTER, MESSAGE, RESET, READY, ACK, EOF, DELETE, BATCH</code></pre></div>\n<p><code>LegacyEventDecoder</code> converts legacy events to internal <code>BrokerMessage</code> types. <code>LegacyEventEncoder</code> performs the reverse.</p>\n<p><code>LegacyConnectionState</code> tracks a FIFO of outbound expectations:</p>\n<ul><li>outbound RESET -&gt; generic ACK becomes <code>RESET_ACK</code>;</li><li>outbound READY -&gt; generic ACK becomes <code>READY_ACK</code>;</li><li>outbound DATA/BATCH -&gt; generic ACK becomes <code>BATCH_ACK</code>.</li></ul>\n<p>For refresh ACKs, it creates the structured topic payload with an empty group; broker handlers resolve the group from the consumer registry.</p>\n<h2 id=\"legacy-delivery-flow\" data-heading=\"Legacy Delivery Flow\">Legacy Delivery Flow<a class=\"heading-link\" href=\"#/chapter/06-event-kafka-flow/legacy-delivery-flow\" aria-label=\"Link to Legacy Delivery Flow\">#</a></h2>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>REGISTER(serviceName)\n  -&gt; SUBSCRIBE legacy adapter\n  -&gt; service-to-topics map\n  -&gt; one RemoteConsumer per topic\n  -&gt; startup READY / ACK\n  -&gt; LegacyConsumerDeliveryManager creates TopicCursor per topic\n  -&gt; priority-queue k-way merge by offset\n  -&gt; JSON BatchEvent\n  &lt;- generic ACK\n  -&gt; commit max offset for every topic in batch\n  -&gt; per-record RocksDB ACKs</code></pre></div>\n<p>Sources: <code>SubscribeHandler.java</code>, <code>LegacyClientConfig.java</code>, <code>LegacyConsumerDeliveryManager.java</code>, <code>ConsumerRegistry.java</code>, <code>BatchAckService.java</code>.</p>\n<h2 id=\"refresh-event-flow\" data-heading=\"Refresh Event Flow\">Refresh Event Flow<a class=\"heading-link\" href=\"#/chapter/06-event-kafka-flow/refresh-event-flow\" aria-label=\"Link to Refresh Event Flow\">#</a></h2>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>pause pipe\nRESET(topic) -&gt; RESET_ACK(topic, group)\nreset group offset to 0\nreplay BATCH_HEADER/raw/BATCH_ACK until caught up\nREADY(topic) -&gt; READY_ACK(topic, group)\nresume pipe and ACK reconciliation</code></pre></div>\n<p>Full state and race handling: <a href=\"#/chapter/07-pos-machine-state\">POS machine state</a>.</p>\n<h2 id=\"compaction-event-semantics\" data-heading=\"Compaction Event Semantics\">Compaction Event Semantics<a class=\"heading-link\" href=\"#/chapter/06-event-kafka-flow/compaction-event-semantics\" aria-label=\"Link to Compaction Event Semantics\">#</a></h2>\n<ul><li>Same topic/key later offset supersedes earlier offset.</li><li>Null key is not compactable.</li><li>A latest <code>DELETE</code> remains until tombstone retention expires.</li><li>Physical compaction keeps original offsets, creating gaps.</li><li>Delivery-time filtering prevents stale values before rewrite.</li></ul>\n<p>Sources: <code>CompactionIndex.java</code>, <code>CompactionRewriter.java</code>, <code>BatchDeliveryService.java</code>. See <a href=\"#/chapter/08-compaction-handling\">Compaction</a>.</p>\n<h2 id=\"topic-inventory\" data-heading=\"Topic Inventory\">Topic Inventory<a class=\"heading-link\" href=\"#/chapter/06-event-kafka-flow/topic-inventory\" aria-label=\"Link to Topic Inventory\">#</a></h2>\n<p>Topics are dynamic; no enum or central complete topic registry exists. Sources are:</p>\n<ul><li>producer <code>topic</code> JSON;</li><li>pipe <code>MessageRecord.topic</code>;</li><li><code>legacy-clients.service-topics</code> in <code>broker/src/main/resources/application.yml</code>;</li><li>test fixtures.</li></ul>\n<p>Configured legacy topic names are indexed in <a href=\"#/chapter/11-runtime-config/legacy-service-topic-map\">Runtime config</a>.</p>","headings":[{"level":2,"title":"Kafka Clarification","id":"kafka-clarification"},{"level":2,"title":"Modern Wire Protocol","id":"modern-wire-protocol"},{"level":2,"title":"Producer Flow","id":"producer-flow"},{"level":2,"title":"Pipe Replication Flow","id":"pipe-replication-flow"},{"level":2,"title":"Modern Subscription Flow","id":"modern-subscription-flow"},{"level":2,"title":"Modern Delivery Flow","id":"modern-delivery-flow"},{"level":3,"title":"Batch Header","id":"batch-header"},{"level":2,"title":"Legacy Wire Protocol","id":"legacy-wire-protocol"},{"level":2,"title":"Legacy Delivery Flow","id":"legacy-delivery-flow"},{"level":2,"title":"Refresh Event Flow","id":"refresh-event-flow"},{"level":2,"title":"Compaction Event Semantics","id":"compaction-event-semantics"},{"level":2,"title":"Topic Inventory","id":"topic-inventory"}],"plainText":"Event And Kafka-Like Flow Related: Overview, Features, Data model, POS state, Compaction. Kafka Clarification There is no Apache Kafka dependency, producer, consumer, broker protocol, or Kafka topic configuration in this repository. \"Kafka-like\" means the code implements: named topics; partition 0; monotonic/sparse offsets; consumer groups; committed offsets; batched delivery and ACK; keyed compaction and tombstones. Sources: settings.gradle, module build files, StorageEngine.java, ConsumerOffsetTracker.java, and compaction package. Modern Wire Protocol Envelope: Definitions: common/src/main/java/com/messaging/common/model/BrokerMessage.java. Codec: network/src/main/java/com/messaging/network/codec/BinaryMessageEncoder.java and BinaryMessageDecoder.java. Codes: Code Type ---: --- 0x01 DATA 0x02 ACK 0x03 SUBSCRIBE 0x04 COMMITOFFSET 0x05 RESET 0x06 READY 0x07 DISCONNECT 0x08 HEARTBEAT 0x09 BATCHHEADER 0x0A BATCHACK 0x0B RESETACK 0x0C READYACK No HEARTBEAT handler is registered by HandlerInitializer.java. Producer Flow Sources: NettyTcpServer.java, ServerMessageHandler.java, BrokerService.java, DataHandler.java. Producer JSON fields: required: topic optional: msgkey, eventtype, data DELETE creates a null-data tombstone. Other event-type text becomes MESSAGE. Pipe Replication Flow Sources: CloudRegistryClient.java, TopologyManager.java, HttpPipeConnector.java, BrokerService.java. The handler returns a boolean. The connector advances only through the last successful record and retries failures. Modern Subscription Flow During active refresh, SubscribeHandler follows late-joiner rules instead of sending an ordinary startup READY. Source: broker/src/main/java/com/messaging/broker/handler/SubscribeHandler.java. Modern Delivery Flow Sources: Scheduling: DeliveryScheduler.java, TopicFairScheduler.java Broker delivery: BatchDeliveryService.java Transport: NettyTcpServer.java Client decode/ACK: ZeroCopyBatchDecoder.java, network BatchAckHandler.java Broker ACK: broker BatchAckHandler.java, BatchAckService.java Batch Header Payload: Raw records use the current segment log format without offsets or CRC. ZeroCopyBatchDecoder.decodeZeroCopyBatch is the authoritative client parser. If the decoder parses fewer records than advertised, it does not ACK and closes the channel to force replay. Legacy Wire Protocol Protocol detection is based on the first byte. A legacy connection must begin with REGISTER ordinal 0. Sources: DefaultProtocolDetectionService.java, ProtocolDetectionDecoder.java. Legacy event ordinals from network/src/main/java/com/messaging/network/legacy/events/EventType.java: LegacyEventDecoder converts legacy events to internal BrokerMessage types. LegacyEventEncoder performs the reverse. LegacyConnectionState tracks a FIFO of outbound expectations: outbound RESET -> generic ACK becomes RESETACK; outbound READY -> generic ACK becomes READYACK; outbound DATA/BATCH -> generic ACK becomes BATCHACK. For refresh ACKs, it creates the structured topic payload with an empty group; broker handlers resolve the group from the consumer registry. Legacy Delivery Flow Sources: SubscribeHandler.java, LegacyClientConfig.java, LegacyConsumerDeliveryManager.java, ConsumerRegistry.java, BatchAckService.java. Refresh Event Flow Full state and race handling: POS machine state. Compaction Event Semantics Same topic/key later offset supersedes earlier offset. Null key is not compactable. A latest DELETE remains until tombstone retention expires. Physical compaction keeps original offsets, creating gaps. Delivery-time filtering prevents stale values before rewrite. Sources: CompactionIndex.java, CompactionRewriter.java, BatchDeliveryService.java. See Compaction. Topic Inventory Topics are dynamic; no enum or central complete topic registry exists. Sources are: producer topic JSON; pipe MessageRecord.topic; legacy-clients.service-topics in broker/src/main/resources/application.yml; test fixtures. Configured legacy topic names are indexed in Runtime config.","lineCount":216},{"order":7,"file":"07-pos-machine-state.md","slug":"07-pos-machine-state","title":"POS Machine State","html":"<h1 id=\"pos-machine-state\" data-heading=\"POS Machine State\">POS Machine State<a class=\"heading-link\" href=\"#/chapter/07-pos-machine-state/pos-machine-state\" aria-label=\"Link to POS Machine State\">#</a></h1>\n<p>Related: <a href=\"#/chapter/03-feature-catalog/pos-data-refresh\">Features</a>, <a href=\"#/chapter/06-event-kafka-flow/refresh-event-flow\">Events</a>, <a href=\"#/chapter/05-data-model/consumer-state-models\">Data model</a>, <a href=\"#/chapter/09-error-retry-recovery\">Recovery</a>, <a href=\"#/chapter/10-test-map\">Tests</a>.</p>\n<h2 id=\"scope\" data-heading=\"Scope\">Scope<a class=\"heading-link\" href=\"#/chapter/07-pos-machine-state/scope\" aria-label=\"Link to Scope\">#</a></h2>\n<p>There is no class literally named <code>PosMachineState</code>. POS state is represented by:</p>\n<ul><li>TCP connection and registration state;</li><li>startup READY/ACK state;</li><li>per-group/topic delivery and committed offset;</li><li>refresh RESET/replay/READY state;</li><li>reconnect behavior in the client library.</li></ul>\n<p>Sources: <code>ConsumerRegistry.java</code>, <code>ConsumerReadinessManager.java</code>, <code>RefreshCoordinator.java</code>, and <code>ClientConsumerManager.java</code>.</p>\n<h2 id=\"startup-readiness\" data-heading=\"Startup Readiness\">Startup Readiness<a class=\"heading-link\" href=\"#/chapter/07-pos-machine-state/startup-readiness\" aria-label=\"Link to Startup Readiness\">#</a></h2>\n<p>Modern:</p>\n<ol><li>Client creates one connection per <code>topic:group</code>.</li><li>Client sends <code>SUBSCRIBE</code>.</li><li>Broker restores <code>group:topic</code> offset.</li><li>Broker sends <code>READY(topic)</code>.</li><li>Client invokes every handler's <code>onReady(topic)</code>.</li><li>Client sends structured <code>READY_ACK(topic,group)</code>.</li><li>Broker opens the readiness gate.</li></ol>\n<p>Legacy:</p>\n<ol><li>Client sends <code>REGISTER(serviceName)</code>.</li><li>Broker maps service name to topics.</li><li>Broker sends empty-payload READY.</li><li>Generic ACK is resolved to READY_ACK by <code>LegacyConnectionState</code>.</li></ol>\n<p>Sources: <code>SubscribeHandler.java</code>, <code>ConsumerReadinessManager.java</code>, <code>ClientConsumerManager.java</code>, <code>LegacyConnectionState.java</code>.</p>\n<p>READY retries occur after 5 seconds, up to three retries. Source: <code>ConsumerReadinessManager.java</code>.</p>\n<h2 id=\"refresh-state-machine\" data-heading=\"Refresh State Machine\">Refresh State Machine<a class=\"heading-link\" href=\"#/chapter/07-pos-machine-state/refresh-state-machine\" aria-label=\"Link to Refresh State Machine\">#</a></h2>\n<p>States are defined in <code>broker/src/main/java/com/messaging/broker/consumer/RefreshState.java</code>.</p>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>RESET_SENT -&gt; REPLAYING -&gt; READY_SENT -&gt; COMPLETED\n      \\           \\            \\\n       +-----------+-------------&gt; ABORTED</code></pre></div>\n<p>Transition validation: <code>RefreshStateMachine.java</code>.</p>\n<h3 id=\"initiation\" data-heading=\"Initiation\">Initiation<a class=\"heading-link\" href=\"#/chapter/07-pos-machine-state/initiation\" aria-label=\"Link to Initiation\">#</a></h3>\n<p><code>RefreshInitiator.startRefresh</code>:</p>\n<ul><li>force-cancels any existing refresh for the same topic;</li><li>snapshots currently registered <code>group:topic</code> consumers;</li><li>returns <code>COMPLETED</code> immediately if none exist;</li><li>creates a UUID refresh ID shared by concurrent topics in the same batch;</li><li>pauses pipe calls;</li><li>persists <code>RESET_SENT</code>.</li></ul>\n<p>Sources: <code>RefreshInitiator.java</code>, <code>RefreshController.java</code>.</p>\n<h3 id=\"reset-phase\" data-heading=\"RESET Phase\">RESET Phase<a class=\"heading-link\" href=\"#/chapter/07-pos-machine-state/reset-phase\" aria-label=\"Link to RESET Phase\">#</a></h3>\n<p><code>RefreshResetService.sendReset</code>:</p>\n<ul><li>pauses ACK reconciliation for the topic;</li><li>clears ACK RocksDB entries for expected groups;</li><li>broadcasts RESET;</li><li>records metrics.</li></ul>\n<p>On RESET_ACK:</p>\n<ul><li>validate expected consumer and duplicate;</li><li>reset that consumer's offset to <code>0</code>;</li><li>initialize replay metrics;</li><li>persist context;</li><li>the first accepted ACK atomically claims transition to <code>REPLAYING</code>.</li></ul>\n<p>Sources: <code>RefreshResetService.java</code>, <code>ResetAckHandler.java</code>, <code>RefreshCoordinator.java</code>.</p>\n<p>The coordinator retries RESET every 5 seconds while missing ACKs.</p>\n<h3 id=\"replay-phase\" data-heading=\"Replay Phase\">Replay Phase<a class=\"heading-link\" href=\"#/chapter/07-pos-machine-state/replay-phase\" aria-label=\"Link to Replay Phase\">#</a></h3>\n<p>Normal adaptive delivery resumes because <code>RefreshGatePolicy</code> blocks only <code>RESET_SENT</code>. <code>RefreshReplayService</code> checks committed offsets for consumers that ACKed RESET and compares them with storage head.</p>\n<p>Replay completion requires:</p>\n<ul><li>every expected RESET ACK received; and</li><li>every ACKed consumer considered caught up.</li></ul>\n<p>Source: <code>RefreshReplayService.java</code>.</p>\n<h3 id=\"ready-phase\" data-heading=\"READY Phase\">READY Phase<a class=\"heading-link\" href=\"#/chapter/07-pos-machine-state/ready-phase\" aria-label=\"Link to READY Phase\">#</a></h3>\n<p><code>RefreshReadyService.sendReady</code>:</p>\n<ul><li>snapshots RESET ACKs;</li><li>publishes <code>READY_SENT</code>;</li><li>sends READY to the snapshot;</li><li>sends a supplemental READY to consumers that ACKed in the snapshot/state visibility window;</li><li>persists state.</li></ul>\n<p>READY ACK timeout checks run every 10 seconds and re-send to the live ACK set. All READY ACKs atomically claim completion.</p>\n<p>On completion:</p>\n<ul><li>persist <code>COMPLETED</code>;</li><li>resume pipe only when no sibling topic in the same refresh batch remains active;</li><li>clear the topic's refresh state;</li><li>resume ACK reconciliation;</li><li>retain in-memory context for 60 seconds before guarded cleanup.</li></ul>\n<p>Sources: <code>RefreshReadyService.java</code>, <code>RefreshCoordinator.java</code>.</p>\n<h2 id=\"late-joiners-and-reconnects\" data-heading=\"Late Joiners And Reconnects\">Late Joiners And Reconnects<a class=\"heading-link\" href=\"#/chapter/07-pos-machine-state/late-joiners-and-reconnects\" aria-label=\"Link to Late Joiners And Reconnects\">#</a></h2>\n<p><code>SubscribeHandler</code> examines active refresh state:</p>\n<ul><li><code>RESET_SENT</code> or <code>REPLAYING</code>: register the group as a late RESET ACK before opening delivery.</li><li><code>READY_SENT</code>: add it to the live READY audience and send refresh READY directly.</li><li>terminal/cleaned state: use startup READY.</li></ul>\n<p>Race guards and atomic completion markers are in <code>RefreshCoordinator.registerLateJoiningConsumer</code> and <code>RefreshContext</code>.</p>\n<p>Client reconnect uses exponential delay from 5 seconds to 60 seconds and one connection per <code>topic:group</code>: <code>client/src/main/java/com/messaging/client/ClientConsumerManager.java</code>.</p>\n<h2 id=\"disconnect-state-cleanup\" data-heading=\"Disconnect State Cleanup\">Disconnect State Cleanup<a class=\"heading-link\" href=\"#/chapter/07-pos-machine-state/disconnect-state-cleanup\" aria-label=\"Link to Disconnect State Cleanup\">#</a></h2>\n<p><code>ConsumerRegistry.unregisterConsumer</code>:</p>\n<ul><li>cancels each scheduled delivery task;</li><li>removes <code>DeliveryKey</code> in-flight/pending state;</li><li>completes pending metrics;</li><li>removes readiness;</li><li>clears pending ACKs;</li><li>removes registration and eligible metrics.</li></ul>\n<p>Sources: <code>ConsumerRegistry.java</code>, <code>ClientDisconnectHandler.java</code>.</p>\n<p>Durable <code>group:topic</code> offsets are retained, so reconnect resumes from the last persisted position.</p>\n<h2 id=\"persisted-refresh-state\" data-heading=\"Persisted Refresh State\">Persisted Refresh State<a class=\"heading-link\" href=\"#/chapter/07-pos-machine-state/persisted-refresh-state\" aria-label=\"Link to Persisted Refresh State\">#</a></h2>\n<p>File: <code>&lt;dataDir&gt;/data-refresh-state.properties</code>.</p>\n<p>Owner: <code>broker/src/main/java/com/messaging/broker/consumer/RefreshStateStore.java</code>.</p>\n<p>Persisted content includes active topics, refresh ID/type/state, expected consumers, ACK sets, timestamps, replay progress, and downtime. The store includes backward-compatible loading for an older single-active-refresh format.</p>\n<p>At broker startup, <code>RefreshRecoveryService</code>:</p>\n<ul><li>loads all contexts;</li><li>pauses pipe before resuming;</li><li>fills missing old refresh IDs with UUIDs;</li><li>groups topics by refresh ID;</li><li>records startup/downtime;</li><li>resumes RESET, replay, READY, or completed handling;</li><li>re-arms watchdogs and timeout tasks.</li></ul>\n<h2 id=\"timeouts-and-abort\" data-heading=\"Timeouts And Abort\">Timeouts And Abort<a class=\"heading-link\" href=\"#/chapter/07-pos-machine-state/timeouts-and-abort\" aria-label=\"Link to Timeouts And Abort\">#</a></h2>\n<p>Constants in <code>RefreshCoordinator.java</code>:</p>\n<ul><li>replay check: 1 second;</li><li>RESET retry: 5 seconds;</li><li>READY ACK timeout: 10 seconds;</li><li>abort watchdog: 10 minutes.</li></ul>\n<p>REPLAYING watchdog re-arms if replay recently progressed. READY_SENT gets one additional full watchdog window before abort. ABORTED cleanup removes active maps and tasks, but automatic pipe/reconciliation recovery after every abort scenario is not fully confirmed; see <a href=\"#/chapter/15-open-questions\">Open questions</a>.</p>\n<h2 id=\"pos-safety-invariants\" data-heading=\"POS Safety Invariants\">POS Safety Invariants<a class=\"heading-link\" href=\"#/chapter/07-pos-machine-state/pos-safety-invariants\" aria-label=\"Link to POS Safety Invariants\">#</a></h2>\n<ol><li>Delivery must not occur before startup READY_ACK.</li><li>Delivery is blocked while refresh is waiting for RESET ACK.</li><li>Pipe must remain paused until every active topic in a refresh batch is terminal.</li><li>A decoded partial batch must not be ACKed.</li><li>ACK and timeout may complete only the matching delivery generation.</li><li>Reconnect must preserve durable group/topic offset and remove stale socket state.</li><li>Compaction must preserve delivery offset progress across gaps.</li></ol>\n<p>Source implementations: <code>BatchDeliveryService.java</code>, <code>InMemoryInFlightDeliveryStore.java</code>, <code>ZeroCopyBatchDecoder.java</code>, <code>RefreshReadyService.java</code>, <code>SegmentManager.java</code>.</p>\n<h2 id=\"confirmed-test-signal\" data-heading=\"Confirmed Test Signal\">Confirmed Test Signal<a class=\"heading-link\" href=\"#/chapter/07-pos-machine-state/confirmed-test-signal\" aria-label=\"Link to Confirmed Test Signal\">#</a></h2>\n<p>The full journey run had one failure in <code>ConsumerCrashDuringReplayJourneySpec</code>: after group B reconnected, its pending ACK remained for more than 25 seconds despite a 5-second timeout and READY was never sent. The exact spec passed on an immediate clean rerun. Treat this as a flaky concurrency signal, not a dismissed failure. See <a href=\"#/chapter/10-test-map/observed-failures\">Test map</a> and <a href=\"#/chapter/13-risk-and-edge-cases/pos-refresh-and-reconnect\">Risks</a>.</p>","headings":[{"level":2,"title":"Scope","id":"scope"},{"level":2,"title":"Startup Readiness","id":"startup-readiness"},{"level":2,"title":"Refresh State Machine","id":"refresh-state-machine"},{"level":3,"title":"Initiation","id":"initiation"},{"level":3,"title":"RESET Phase","id":"reset-phase"},{"level":3,"title":"Replay Phase","id":"replay-phase"},{"level":3,"title":"READY Phase","id":"ready-phase"},{"level":2,"title":"Late Joiners And Reconnects","id":"late-joiners-and-reconnects"},{"level":2,"title":"Disconnect State Cleanup","id":"disconnect-state-cleanup"},{"level":2,"title":"Persisted Refresh State","id":"persisted-refresh-state"},{"level":2,"title":"Timeouts And Abort","id":"timeouts-and-abort"},{"level":2,"title":"POS Safety Invariants","id":"pos-safety-invariants"},{"level":2,"title":"Confirmed Test Signal","id":"confirmed-test-signal"}],"plainText":"POS Machine State Related: Features, Events, Data model, Recovery, Tests. Scope There is no class literally named PosMachineState. POS state is represented by: TCP connection and registration state; startup READY/ACK state; per-group/topic delivery and committed offset; refresh RESET/replay/READY state; reconnect behavior in the client library. Sources: ConsumerRegistry.java, ConsumerReadinessManager.java, RefreshCoordinator.java, and ClientConsumerManager.java. Startup Readiness Modern: Client creates one connection per topic:group. Client sends SUBSCRIBE. Broker restores group:topic offset. Broker sends READY(topic). Client invokes every handler's onReady(topic). Client sends structured READYACK(topic,group). Broker opens the readiness gate. Legacy: Client sends REGISTER(serviceName). Broker maps service name to topics. Broker sends empty-payload READY. Generic ACK is resolved to READYACK by LegacyConnectionState. Sources: SubscribeHandler.java, ConsumerReadinessManager.java, ClientConsumerManager.java, LegacyConnectionState.java. READY retries occur after 5 seconds, up to three retries. Source: ConsumerReadinessManager.java. Refresh State Machine States are defined in broker/src/main/java/com/messaging/broker/consumer/RefreshState.java. Transition validation: RefreshStateMachine.java. Initiation RefreshInitiator.startRefresh: force-cancels any existing refresh for the same topic; snapshots currently registered group:topic consumers; returns COMPLETED immediately if none exist; creates a UUID refresh ID shared by concurrent topics in the same batch; pauses pipe calls; persists RESETSENT. Sources: RefreshInitiator.java, RefreshController.java. RESET Phase RefreshResetService.sendReset: pauses ACK reconciliation for the topic; clears ACK RocksDB entries for expected groups; broadcasts RESET; records metrics. On RESETACK: validate expected consumer and duplicate; reset that consumer's offset to 0; initialize replay metrics; persist context; the first accepted ACK atomically claims transition to REPLAYING. Sources: RefreshResetService.java, ResetAckHandler.java, RefreshCoordinator.java. The coordinator retries RESET every 5 seconds while missing ACKs. Replay Phase Normal adaptive delivery resumes because RefreshGatePolicy blocks only RESETSENT. RefreshReplayService checks committed offsets for consumers that ACKed RESET and compares them with storage head. Replay completion requires: every expected RESET ACK received; and every ACKed consumer considered caught up. Source: RefreshReplayService.java. READY Phase RefreshReadyService.sendReady: snapshots RESET ACKs; publishes READYSENT; sends READY to the snapshot; sends a supplemental READY to consumers that ACKed in the snapshot/state visibility window; persists state. READY ACK timeout checks run every 10 seconds and re-send to the live ACK set. All READY ACKs atomically claim completion. On completion: persist COMPLETED; resume pipe only when no sibling topic in the same refresh batch remains active; clear the topic's refresh state; resume ACK reconciliation; retain in-memory context for 60 seconds before guarded cleanup. Sources: RefreshReadyService.java, RefreshCoordinator.java. Late Joiners And Reconnects SubscribeHandler examines active refresh state: RESETSENT or REPLAYING: register the group as a late RESET ACK before opening delivery. READYSENT: add it to the live READY audience and send refresh READY directly. terminal/cleaned state: use startup READY. Race guards and atomic completion markers are in RefreshCoordinator.registerLateJoiningConsumer and RefreshContext. Client reconnect uses exponential delay from 5 seconds to 60 seconds and one connection per topic:group: client/src/main/java/com/messaging/client/ClientConsumerManager.java. Disconnect State Cleanup ConsumerRegistry.unregisterConsumer: cancels each scheduled delivery task; removes DeliveryKey in-flight/pending state; completes pending metrics; removes readiness; clears pending ACKs; removes registration and eligible metrics. Sources: ConsumerRegistry.java, ClientDisconnectHandler.java. Durable group:topic offsets are retained, so reconnect resumes from the last persisted position. Persisted Refresh State File: <dataDir>/data-refresh-state.properties. Owner: broker/src/main/java/com/messaging/broker/consumer/RefreshStateStore.java. Persisted content includes active topics, refresh ID/type/state, expected consumers, ACK sets, timestamps, replay progress, and downtime. The store includes backward-compatible loading for an older single-active-refresh format. At broker startup, RefreshRecoveryService: loads all contexts; pauses pipe before resuming; fills missing old refresh IDs with UUIDs; groups topics by refresh ID; records startup/downtime; resumes RESET, replay, READY, or completed handling; re-arms watchdogs and timeout tasks. Timeouts And Abort Constants in RefreshCoordinator.java: replay check: 1 second; RESET retry: 5 seconds; READY ACK timeout: 10 seconds; abort watchdog: 10 minutes. REPLAYING watchdog re-arms if replay recently progressed. READYSENT gets one additional full watchdog window before abort. ABORTED cleanup removes active maps and tasks, but automatic pipe/reconciliation recovery after every abort scenario is not fully confirmed; see Open questions. POS Safety Invariants Delivery must not occur before startup READYACK. Delivery is blocked while refresh is waiting for RESET ACK. Pipe must remain paused until every active topic in a refresh batch is terminal. A decoded partial batch must not be ACKed. ACK and timeout may complete only the matching delivery generation. Reconnect must preserve durable group/topic offset and remove stale socket state. Compaction must preserve delivery offset progress across gaps. Source implementations: BatchDeliveryService.java, InMemoryInFlightDeliveryStore.java, ZeroCopyBatchDecoder.java, RefreshReadyService.java, SegmentManager.java. Confirmed Test Signal The full journey run had one failure in ConsumerCrashDuringReplayJourneySpec: after group B reconnected, its pending ACK remained for more than 25 seconds despite a 5-second timeout and READY was never sent. The exact spec passed on an immediate clean rerun. Treat this as a flaky concurrency signal, not a dismissed failure. See Test map and Risks.","lineCount":190},{"order":8,"file":"08-compaction-handling.md","slug":"08-compaction-handling","title":"Compaction Handling","html":"<h1 id=\"compaction-handling\" data-heading=\"Compaction Handling\">Compaction Handling<a class=\"heading-link\" href=\"#/chapter/08-compaction-handling/compaction-handling\" aria-label=\"Link to Compaction Handling\">#</a></h1>\n<p>Related: <a href=\"#/chapter/05-data-model\">Data model</a>, <a href=\"#/chapter/06-event-kafka-flow/compaction-event-semantics\">Events</a>, <a href=\"#/chapter/07-pos-machine-state\">POS state</a>, <a href=\"#/chapter/09-error-retry-recovery\">Errors/recovery</a>, <a href=\"#/chapter/13-risk-and-edge-cases\">Risks</a>.</p>\n<h2 id=\"purpose\" data-heading=\"Purpose\">Purpose<a class=\"heading-link\" href=\"#/chapter/08-compaction-handling/purpose\" aria-label=\"Link to Purpose\">#</a></h2>\n<p>Compaction keeps the latest record for a topic/key and eventually removes superseded records from sealed segments. DELETE tombstones are retained for a configured period, then may also be removed when they are still the latest key state.</p>\n<p>Sources: <code>broker/src/main/java/com/messaging/broker/compaction/CompactionIndex.java</code>, <code>CompactionRewriter.java</code>.</p>\n<h2 id=\"ingress-indexing\" data-heading=\"Ingress Indexing\">Ingress Indexing<a class=\"heading-link\" href=\"#/chapter/08-compaction-handling/ingress-indexing\" aria-label=\"Link to Ingress Indexing\">#</a></h2>\n<p>Both ingress paths synchronously call:</p>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>CompactionIndex.updateKey(topic, msgKey, offset, timestamp)</code></pre></div>\n<p>Sources:</p>\n<ul><li>TCP producer: <code>broker/src/main/java/com/messaging/broker/handler/DataHandler.java</code></li><li>Pipe: <code>broker/src/main/java/com/messaging/broker/core/BrokerService.java</code></li></ul>\n<p>Backends:</p>\n<ul><li>RocksDB default: <code>RocksDbCompactionIndex.java</code></li><li>Memory: <code>InMemoryCompactionIndex.java</code></li></ul>\n<p>Selected by <code>compaction-index.backend</code> in <code>broker/src/main/resources/application.yml</code>.</p>\n<p>Null keys are not safely compactable and are retained.</p>\n<h2 id=\"delivery-time-filtering\" data-heading=\"Delivery-Time Filtering\">Delivery-Time Filtering<a class=\"heading-link\" href=\"#/chapter/08-compaction-handling/delivery-time-filtering\" aria-label=\"Link to Delivery-Time Filtering\">#</a></h2>\n<p>Physical compaction is delayed, so <code>BatchDeliveryService.applyCompactionFilter</code> prevents consumers from seeing known stale versions:</p>\n<ol><li>Fast-path keeps zero-copy if batch starts after the highest stale offset.</li><li>Otherwise decode records through <code>StorageEngine.read</code>.</li><li>Drop records where <code>CompactionIndex.isSuperseded</code> is true.</li><li>If unchanged, preserve original zero-copy batch.</li><li>If changed, build a heap-backed <code>ByteArrayDeliveryBatch</code>.</li><li>If all records are filtered, advance/persist the original batch's last offset plus one without a network send.</li></ol>\n<p>Source: <code>broker/src/main/java/com/messaging/broker/consumer/BatchDeliveryService.java</code>.</p>\n<p>The filter fails open if decoding fails or returns fewer records than the file-backed batch advertises. This favors availability but can expose stale versions.</p>\n<h2 id=\"scheduled-compaction\" data-heading=\"Scheduled Compaction\">Scheduled Compaction<a class=\"heading-link\" href=\"#/chapter/08-compaction-handling/scheduled-compaction\" aria-label=\"Link to Scheduled Compaction\">#</a></h2>\n<p><code>CompactionScheduler.java</code> is scheduled from:</p>\n<ul><li><code>compaction.schedule.initial-delay</code></li><li><code>compaction.schedule.interval</code></li></ul>\n<p>It uses:</p>\n<ul><li>an atomic single-flight guard;</li><li>one compaction executor;</li><li>sorted topics;</li><li><code>max-topics-per-run</code>;</li><li><code>min-segments-per-topic</code>;</li><li>process CPU and heap guards.</li></ul>\n<p>The default initial delay is 5 minutes and interval 24 hours in <code>application.yml</code>.</p>\n<h2 id=\"planning\" data-heading=\"Planning\">Planning<a class=\"heading-link\" href=\"#/chapter/08-compaction-handling/planning\" aria-label=\"Link to Planning\">#</a></h2>\n<p><code>CompactionPlanner.java</code> selects a window of sealed segments:</p>\n<ul><li>base offset above the stored checkpoint;</li><li>at most configured window size;</li><li>active segment excluded.</li></ul>\n<p>The scheduler skips topics without enough sealed segments.</p>\n<h2 id=\"rewrite\" data-heading=\"Rewrite\">Rewrite<a class=\"heading-link\" href=\"#/chapter/08-compaction-handling/rewrite\" aria-label=\"Link to Rewrite\">#</a></h2>\n<p><code>CompactionRewriter.java</code>:</p>\n<ol><li>Reads selected segments with <code>CompactionSegmentReader</code>.</li><li>Writes staging output with <code>CompactionSegmentWriter</code>.</li><li>Keeps original offsets.</li><li>Removes superseded records.</li><li>Removes expired latest DELETE tombstones.</li><li>Forces output.</li><li>Atomically moves staging names to <code>.compacted.log/.index</code>.</li><li>Calls <code>SegmentManager.replaceSegments</code>.</li><li>Updates checkpoint and stale watermark as allowed.</li></ol>\n<p>Reader/writer code uses a 256 KB streaming buffer rather than loading an entire segment into memory.</p>\n<p>If no records survive, selected segments can be removed without replacement. The code logs that consumers may stall; delivery must rely on gap-aware index/offset behavior.</p>\n<h2 id=\"persistent-index-and-checkpoints\" data-heading=\"Persistent Index And Checkpoints\">Persistent Index And Checkpoints<a class=\"heading-link\" href=\"#/chapter/08-compaction-handling/persistent-index-and-checkpoints\" aria-label=\"Link to Persistent Index And Checkpoints\">#</a></h2>\n<p><code>SharedRocksDb.java</code> provides the <code>compaction</code> column family.</p>\n<ul><li>Latest key state: <code>RocksDbCompactionIndex.java</code></li><li>Checkpoint: <code>CompactionCheckpointStore.java</code></li></ul>\n<p>Checkpoint advancement is withheld when an unexpired tombstone remains in the rewritten window so it can be reconsidered after retention expires.</p>\n<h2 id=\"manual-trigger\" data-heading=\"Manual Trigger\">Manual Trigger<a class=\"heading-link\" href=\"#/chapter/08-compaction-handling/manual-trigger\" aria-label=\"Link to Manual Trigger\">#</a></h2>\n<p><code>POST /admin/compaction/trigger</code> calls <code>CompactionScheduler.triggerAsync</code> with a preparation callback that force-rolls active segments for every topic. Source: <code>broker/src/main/java/com/messaging/broker/http/CompactionController.java</code>.</p>\n<p>This makes data immediately eligible, but adds:</p>\n<ul><li>filesystem IO;</li><li>more segment files;</li><li>lock interaction with pipe append;</li><li>possible delivery/file-region interaction.</li></ul>\n<h2 id=\"recovery-safety\" data-heading=\"Recovery Safety\">Recovery Safety<a class=\"heading-link\" href=\"#/chapter/08-compaction-handling/recovery-safety\" aria-label=\"Link to Recovery Safety\">#</a></h2>\n<ul><li>Staging <code>.compacting.*</code> files are not treated as normal segments.</li><li>Final <code>.compacted.*</code> pairs are discoverable by recovery.</li><li>Atomic move is used for publication.</li><li>Segment map replacement is protected by a write lock; cleanup occurs after the map swap.</li></ul>\n<p>Sources: <code>DefaultStorageRecoveryService.java</code>, <code>SegmentManager.java</code>, compaction writer/rewriter.</p>\n<h2 id=\"pos-and-refresh-interaction\" data-heading=\"POS And Refresh Interaction\">POS And Refresh Interaction<a class=\"heading-link\" href=\"#/chapter/08-compaction-handling/pos-and-refresh-interaction\" aria-label=\"Link to POS And Refresh Interaction\">#</a></h2>\n<p>Compaction is not automatically paused during refresh. Delivery filtering and storage locks are expected to keep replay correct. Relevant journey tests:</p>\n<ul><li><code>CompactionDuringRefreshJourneySpec.groovy</code></li><li><code>RefreshWithCompactedAndActiveSegmentsJourneySpec.groovy</code></li><li><code>CompactionRaceWindowLegacyDeliveryJourneySpec.groovy</code></li><li><code>CompactionDeliveryRecoveryJourneySpec.groovy</code></li></ul>\n<p>The scheduled default may fire during a long initial replay. Existing production incident notes in <code>BUGS.md</code> describe this as risky; code currently relies on CPU/heap guards and operator configuration, not replay awareness.</p>\n<h2 id=\"performance-impact\" data-heading=\"Performance Impact\">Performance Impact<a class=\"heading-link\" href=\"#/chapter/08-compaction-handling/performance-impact\" aria-label=\"Link to Performance Impact\">#</a></h2>\n<ul><li><strong>Memory:</strong> streaming rewrite is bounded, but each decoded record allocates key/data strings; delivery filtering creates heap batches when records are removed.</li><li><strong>CPU:</strong> full record decode and RocksDB lookups; guarded by process CPU threshold.</li><li><strong>IO:</strong> reads and rewrites all selected segment bytes, even if little is removed.</li><li><strong>Network:</strong> no direct network output, but storage locks/read failures can delay delivery.</li><li><strong>Latency:</strong> write-lock replacement and page-cache churn can increase delivery latency.</li><li><strong>Compatibility:</strong> offsets are preserved; consumers must tolerate gaps.</li></ul>\n<p>Sources: compaction reader/rewriter/scheduler and <code>BatchDeliveryService.java</code>.</p>\n<h2 id=\"confirmed-gaps-and-risks\" data-heading=\"Confirmed Gaps And Risks\">Confirmed Gaps And Risks<a class=\"heading-link\" href=\"#/chapter/08-compaction-handling/confirmed-gaps-and-risks\" aria-label=\"Link to Confirmed Gaps And Risks\">#</a></h2>\n<ol><li>No-op rewrites are not skipped; unique-key segments can be rewritten for zero reclaim.</li><li>Force-roll can race active pipe writes.</li><li>Compaction may run during startup replay/refresh.</li><li>If all records are removed, earliest/head behavior across a missing range is delicate.</li><li>Delivery filter fail-open may temporarily expose stale data.</li><li><code>StorageEngine.compact()</code> itself is a no-op; callers must use broker <code>CompactionScheduler</code>.</li></ol>\n<p>Tests cover many races at small scale, but not production segment sizes or real POS storage pressure. See <a href=\"#/chapter/13-risk-and-edge-cases/compaction\">Risks</a>.</p>","headings":[{"level":2,"title":"Purpose","id":"purpose"},{"level":2,"title":"Ingress Indexing","id":"ingress-indexing"},{"level":2,"title":"Delivery-Time Filtering","id":"delivery-time-filtering"},{"level":2,"title":"Scheduled Compaction","id":"scheduled-compaction"},{"level":2,"title":"Planning","id":"planning"},{"level":2,"title":"Rewrite","id":"rewrite"},{"level":2,"title":"Persistent Index And Checkpoints","id":"persistent-index-and-checkpoints"},{"level":2,"title":"Manual Trigger","id":"manual-trigger"},{"level":2,"title":"Recovery Safety","id":"recovery-safety"},{"level":2,"title":"POS And Refresh Interaction","id":"pos-and-refresh-interaction"},{"level":2,"title":"Performance Impact","id":"performance-impact"},{"level":2,"title":"Confirmed Gaps And Risks","id":"confirmed-gaps-and-risks"}],"plainText":"Compaction Handling Related: Data model, Events, POS state, Errors/recovery, Risks. Purpose Compaction keeps the latest record for a topic/key and eventually removes superseded records from sealed segments. DELETE tombstones are retained for a configured period, then may also be removed when they are still the latest key state. Sources: broker/src/main/java/com/messaging/broker/compaction/CompactionIndex.java, CompactionRewriter.java. Ingress Indexing Both ingress paths synchronously call: Sources: TCP producer: broker/src/main/java/com/messaging/broker/handler/DataHandler.java Pipe: broker/src/main/java/com/messaging/broker/core/BrokerService.java Backends: RocksDB default: RocksDbCompactionIndex.java Memory: InMemoryCompactionIndex.java Selected by compaction-index.backend in broker/src/main/resources/application.yml. Null keys are not safely compactable and are retained. Delivery-Time Filtering Physical compaction is delayed, so BatchDeliveryService.applyCompactionFilter prevents consumers from seeing known stale versions: Fast-path keeps zero-copy if batch starts after the highest stale offset. Otherwise decode records through StorageEngine.read. Drop records where CompactionIndex.isSuperseded is true. If unchanged, preserve original zero-copy batch. If changed, build a heap-backed ByteArrayDeliveryBatch. If all records are filtered, advance/persist the original batch's last offset plus one without a network send. Source: broker/src/main/java/com/messaging/broker/consumer/BatchDeliveryService.java. The filter fails open if decoding fails or returns fewer records than the file-backed batch advertises. This favors availability but can expose stale versions. Scheduled Compaction CompactionScheduler.java is scheduled from: compaction.schedule.initial-delay compaction.schedule.interval It uses: an atomic single-flight guard; one compaction executor; sorted topics; max-topics-per-run; min-segments-per-topic; process CPU and heap guards. The default initial delay is 5 minutes and interval 24 hours in application.yml. Planning CompactionPlanner.java selects a window of sealed segments: base offset above the stored checkpoint; at most configured window size; active segment excluded. The scheduler skips topics without enough sealed segments. Rewrite CompactionRewriter.java: Reads selected segments with CompactionSegmentReader. Writes staging output with CompactionSegmentWriter. Keeps original offsets. Removes superseded records. Removes expired latest DELETE tombstones. Forces output. Atomically moves staging names to .compacted.log/.index. Calls SegmentManager.replaceSegments. Updates checkpoint and stale watermark as allowed. Reader/writer code uses a 256 KB streaming buffer rather than loading an entire segment into memory. If no records survive, selected segments can be removed without replacement. The code logs that consumers may stall; delivery must rely on gap-aware index/offset behavior. Persistent Index And Checkpoints SharedRocksDb.java provides the compaction column family. Latest key state: RocksDbCompactionIndex.java Checkpoint: CompactionCheckpointStore.java Checkpoint advancement is withheld when an unexpired tombstone remains in the rewritten window so it can be reconsidered after retention expires. Manual Trigger POST /admin/compaction/trigger calls CompactionScheduler.triggerAsync with a preparation callback that force-rolls active segments for every topic. Source: broker/src/main/java/com/messaging/broker/http/CompactionController.java. This makes data immediately eligible, but adds: filesystem IO; more segment files; lock interaction with pipe append; possible delivery/file-region interaction. Recovery Safety Staging .compacting. files are not treated as normal segments. Final .compacted. pairs are discoverable by recovery. Atomic move is used for publication. Segment map replacement is protected by a write lock; cleanup occurs after the map swap. Sources: DefaultStorageRecoveryService.java, SegmentManager.java, compaction writer/rewriter. POS And Refresh Interaction Compaction is not automatically paused during refresh. Delivery filtering and storage locks are expected to keep replay correct. Relevant journey tests: CompactionDuringRefreshJourneySpec.groovy RefreshWithCompactedAndActiveSegmentsJourneySpec.groovy CompactionRaceWindowLegacyDeliveryJourneySpec.groovy CompactionDeliveryRecoveryJourneySpec.groovy The scheduled default may fire during a long initial replay. Existing production incident notes in BUGS.md describe this as risky; code currently relies on CPU/heap guards and operator configuration, not replay awareness. Performance Impact Memory: streaming rewrite is bounded, but each decoded record allocates key/data strings; delivery filtering creates heap batches when records are removed. CPU: full record decode and RocksDB lookups; guarded by process CPU threshold. IO: reads and rewrites all selected segment bytes, even if little is removed. Network: no direct network output, but storage locks/read failures can delay delivery. Latency: write-lock replacement and page-cache churn can increase delivery latency. Compatibility: offsets are preserved; consumers must tolerate gaps. Sources: compaction reader/rewriter/scheduler and BatchDeliveryService.java. Confirmed Gaps And Risks No-op rewrites are not skipped; unique-key segments can be rewritten for zero reclaim. Force-roll can race active pipe writes. Compaction may run during startup replay/refresh. If all records are removed, earliest/head behavior across a missing range is delicate. Delivery filter fail-open may temporarily expose stale data. StorageEngine.compact() itself is a no-op; callers must use broker CompactionScheduler. Tests cover many races at small scale, but not production segment sizes or real POS storage pressure. See Risks.","lineCount":155},{"order":9,"file":"09-error-retry-recovery.md","slug":"09-error-retry-recovery","title":"Error, Retry, And Recovery","html":"<h1 id=\"error-retry-and-recovery\" data-heading=\"Error, Retry, And Recovery\">Error, Retry, And Recovery<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/error-retry-and-recovery\" aria-label=\"Link to Error, Retry, And Recovery\">#</a></h1>\n<p>Related: <a href=\"#/chapter/01-system-overview\">Overview</a>, <a href=\"#/chapter/07-pos-machine-state\">POS state</a>, <a href=\"#/chapter/08-compaction-handling\">Compaction</a>, <a href=\"#/chapter/12-debugging-guide\">Debugging</a>, <a href=\"#/chapter/10-test-map\">Tests</a>.</p>\n<h2 id=\"exception-model\" data-heading=\"Exception Model\">Exception Model<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/exception-model\" aria-label=\"Link to Exception Model\">#</a></h2>\n<p>Shared exception hierarchy (all <strong>unchecked</strong> — <code>MessagingException extends RuntimeException</code>):</p>\n<ul><li><code>MessagingException</code></li><li><code>StorageException</code></li><li><code>NetworkException</code></li><li><code>ConsumerException</code></li><li><code>DataRefreshException</code></li><li><code>ErrorCode</code></li></ul>\n<p>Sources: <code>common/src/main/java/com/messaging/common/exception/</code>.</p>\n<p>Exceptions can carry context through <code>MessagingException.withContext</code>. <code>ExceptionLogger.java</code> formats shared logging; <code>ExceptionLogger.logAndThrow(log, ex)</code> logs at ERROR and rethrows.</p>\n<p><strong>Convention (enforced):</strong> code raises a <code>MessagingException</code> subclass with an <code>ErrorCode</code> — never a bare <code>IllegalArgumentException</code> / <code>IllegalStateException</code> / <code>RuntimeException</code>. Because the base is unchecked, every layer can comply without <code>throws</code> pollution: value objects (<code>DeliveryKey</code>, <code>ConsumerKey</code>, <code>BatchMetadata</code>, <code>SubscribeRequest</code>) and hot-path decoders (<code>EventType.get</code>, <code>EventFactory</code>) throw structured exceptions with no signature ripple into the read path. Existing <code>catch</code>/<code>throws</code> declarations remain valid; the project catches these at its boundaries (message handlers, <code>BrokerService</code> startup, the delivery loop). Generic validation/lifecycle guards use <code>VALIDATION_INVALID_ARGUMENT</code> / <code>BROKER_INVALID_STATE</code>.</p>\n<h2 id=\"startup-failures\" data-heading=\"Startup Failures\">Startup Failures<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/startup-failures\" aria-label=\"Link to Startup Failures\">#</a></h2>\n<ul><li>Storage recovery / network-startup failure is fatal: <code>BrokerService</code> rethrows the</li></ul>\n<p><code>StorageException</code> / <code>NetworkException</code> via <code>ExceptionLogger.logAndThrow</code>. Being unchecked, it propagates straight out of the <code>ServerStartupEvent</code> listener and fails the boot (no <code>RuntimeException</code> wrapper needed).</p>\n<ul><li>TCP bind failure is fatal: <code>NettyTcpServer.java</code>, <code>BrokerService.java</code>.</li><li>ACK seeding failure is non-fatal; reconciliation is expected to expose gaps.</li><li>Registry absence/failure is non-fatal; topology manager can continue without a parent.</li></ul>\n<h2 id=\"storage-recovery\" data-heading=\"Storage Recovery\">Storage Recovery<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/storage-recovery\" aria-label=\"Link to Storage Recovery\">#</a></h2>\n<p><code>DefaultStorageRecoveryService.java</code> handles:</p>\n<ul><li>current and legacy index versions;</li><li>log bytes without index entries;</li><li>index entries beyond log end;</li><li>staging compaction files;</li><li>discovery of topic/partition directories.</li></ul>\n<p><code>FileChannelStorageEngine.recover</code> creates managers for discovered partitions. Storage corruption that cannot be reconciled prevents startup.</p>\n<p>Tests: <code>SegmentCrashRecoverySpec.groovy</code>, <code>DefaultStorageRecoveryServiceSpec.groovy</code>, integration equivalents, restart journeys.</p>\n<h2 id=\"pipe-retry-and-recovery\" data-heading=\"Pipe Retry And Recovery\">Pipe Retry And Recovery<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/pipe-retry-and-recovery\" aria-label=\"Link to Pipe Retry And Recovery\">#</a></h2>\n<p><code>HttpPipeConnector.java</code>:</p>\n<ul><li>adaptive poll delay between configured min/max;</li><li>retries network/HTTP/parser failures;</li><li>persists only the last successfully handled offset;</li><li>resumes from <code>pipe-offset.properties</code>;</li><li>supports pause/resume during refresh;</li><li>copies/releases streaming Netty buffers before parsing.</li></ul>\n<p>The connector treats partial successful processing transactionally: successfully handled records can advance in <code>finally</code>, while the failed record and later data are retried.</p>\n<p><code>TopologyManager.java</code> probes a new parent before switching and keeps the old parent when the probe fails.</p>\n<h2 id=\"delivery-retry\" data-heading=\"Delivery Retry\">Delivery Retry<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/delivery-retry\" aria-label=\"Link to Delivery Retry\">#</a></h2>\n<h3 id=\"adaptive-scheduling\" data-heading=\"Adaptive Scheduling\">Adaptive Scheduling<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/adaptive-scheduling\" aria-label=\"Link to Adaptive Scheduling\">#</a></h3>\n<p><code>AdaptiveBackoffPolicy.java</code> calculates delay between configured minimum and maximum. Success reduces delay; no data/failure increases it.</p>\n<p><code>TopicFairScheduler.java</code> permits configured concurrent work per topic and retains at most one pending retry per delivery key.</p>\n<h3 id=\"consumer-failure-backoff\" data-heading=\"Consumer Failure Backoff\">Consumer Failure Backoff<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/consumer-failure-backoff\" aria-label=\"Link to Consumer Failure Backoff\">#</a></h3>\n<p><code>RemoteConsumer.java</code> records consecutive failures and backoff time. <code>BatchDeliveryService.java</code>:</p>\n<ul><li>blocks while backoff remains;</li><li>retries failed consumers;</li><li>unregisters after ten consecutive failures;</li><li>clears state immediately if send failed before timeout installation;</li><li>leaves timeout-owned state intact if failure occurred after timeout installation.</li></ul>\n<h3 id=\"ack-timeout\" data-heading=\"ACK Timeout\">ACK Timeout<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/ack-timeout\" aria-label=\"Link to ACK Timeout\">#</a></h3>\n<p>Modern:</p>\n<ul><li>each delivery gets a generation;</li><li>timeout claims only that generation;</li><li>claimed timeout restores original offset and clears state.</li></ul>\n<p>Sources: <code>BatchDeliveryService.java</code>, <code>InMemoryInFlightDeliveryStore.java</code>.</p>\n<p>Legacy:</p>\n<ul><li>one pending merged batch per client;</li><li>generation-specific timeout clears the slot and metrics.</li></ul>\n<p>Sources: <code>ConsumerRegistry.java</code>, <code>InMemoryPendingAckStore.java</code>.</p>\n<h2 id=\"readiness-retry\" data-heading=\"Readiness Retry\">Readiness Retry<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/readiness-retry\" aria-label=\"Link to Readiness Retry\">#</a></h2>\n<p><code>ConsumerReadinessManager.java</code> retries READY every 5 seconds up to three times and cancels the retry when matching readiness is recorded.</p>\n<p>Refresh uses separate retry timing in <code>RefreshCoordinator.java</code>: RESET every 5 seconds and READY checks every 10 seconds.</p>\n<h2 id=\"client-reconnect\" data-heading=\"Client Reconnect\">Client Reconnect<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/client-reconnect\" aria-label=\"Link to Client Reconnect\">#</a></h2>\n<p><code>ClientConsumerManager.java</code>:</p>\n<ul><li>detects disconnect through Netty close future;</li><li>maintains per-topic/group reconnect guards;</li><li>exponential reconnect delay <code>5s, 10s, 20s, 40s, 60s cap</code>;</li><li>health-checks every 10 seconds;</li><li>cleans one connection before recreating it;</li><li>shares one event-loop group.</li></ul>\n<h2 id=\"ack-recovery\" data-heading=\"ACK Recovery\">ACK Recovery<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/ack-recovery\" aria-label=\"Link to ACK Recovery\">#</a></h2>\n<h3 id=\"seeder\" data-heading=\"Seeder\">Seeder<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/seeder\" aria-label=\"Link to Seeder\">#</a></h3>\n<p><code>AckStoreSeeder.java</code> scans all committed offsets after storage recovery and fills missing per-record ACK entries in chunks. It distinguishes no committed offset from offset <code>0</code>.</p>\n<h3 id=\"reconciliation\" data-heading=\"Reconciliation\">Reconciliation<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/reconciliation\" aria-label=\"Link to Reconciliation\">#</a></h3>\n<p><code>AckReconciliationScheduler.java</code> periodically scans registered topic/group ranges up to committed offsets:</p>\n<ul><li>reports missing count;</li><li>optionally backfills when <code>auto-sync-enabled=true</code>;</li><li>keeps in-memory scan checkpoints;</li><li>pauses and clears checkpoints for a topic during refresh;</li><li>resumes with a full scan after refresh.</li></ul>\n<p>Only active registered pairs are reconciled. Historical disconnected groups are not confirmed to be scanned.</p>\n<h2 id=\"refresh-recovery\" data-heading=\"Refresh Recovery\">Refresh Recovery<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/refresh-recovery\" aria-label=\"Link to Refresh Recovery\">#</a></h2>\n<p>Detailed state behavior is in <a href=\"#/chapter/07-pos-machine-state\">POS state</a>.</p>\n<p>Recovery source: <code>RefreshRecoveryService.java</code>. It loads persisted contexts, pauses pipe first, records downtime, restores task scheduling, and resumes by state.</p>\n<p>Abort watchdog behavior:</p>\n<ul><li>10-minute initial window;</li><li>REPLAYING re-arms when progress is recent;</li><li>READY_SENT receives one extra window;</li><li>terminal states stop work.</li></ul>\n<h2 id=\"compaction-recovery\" data-heading=\"Compaction Recovery\">Compaction Recovery<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/compaction-recovery\" aria-label=\"Link to Compaction Recovery\">#</a></h2>\n<p>See <a href=\"#/chapter/08-compaction-handling/recovery-safety\">Compaction recovery</a>. The key mechanisms are staging files, atomic rename, checkpoints, and segment replacement locks.</p>\n<h2 id=\"property-file-recovery\" data-heading=\"Property-File Recovery\">Property-File Recovery<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/property-file-recovery\" aria-label=\"Link to Property-File Recovery\">#</a></h2>\n<p><code>PropertiesFileStore.java</code> uses temp files, force, and move. <code>FlushingPropertiesStore.java</code>:</p>\n<ul><li>retries/logs periodic flush failure;</li><li>performs a final flush during stop;</li><li>can propagate a final flush failure.</li></ul>\n<p>Consumer offsets intentionally allow a small replay window because synchronous writes are rate-limited.</p>\n<h2 id=\"error-handling-by-interface\" data-heading=\"Error Handling By Interface\">Error Handling By Interface<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/error-handling-by-interface\" aria-label=\"Link to Error Handling By Interface\">#</a></h2>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Interface</th>\n<th>Behavior</th>\n</tr></thead><tbody>\n<tr>\n<td>TCP producer/subscribe/commit validation</td>\n<td>Log and close connection</td>\n</tr>\n<tr>\n<td>Malformed ACK lengths</td>\n<td>Log and close connection</td>\n</tr>\n<tr>\n<td>Unknown legacy ACK group</td>\n<td>Soft-fail without close</td>\n</tr>\n<tr>\n<td>Delivery send/read failure</td>\n<td>Backoff, rollback/timeout, eventual unregister</td>\n</tr>\n<tr>\n<td>Pipe record storage failure</td>\n<td>Return false; do not advance that offset</td>\n</tr>\n<tr>\n<td>HTTP controller validation</td>\n<td>Usually return error map with HTTP 200</td>\n</tr>\n<tr>\n<td>Storage recovery failure</td>\n<td>Fail broker startup</td>\n</tr>\n<tr>\n<td>ACK seeding/reconciliation failure</td>\n<td>Degrade and log</td>\n</tr>\n</tbody></table></div>\n<p>Sources: broker handlers/controllers and <code>BrokerService.java</code>.</p>\n<h2 id=\"recovery-risks\" data-heading=\"Recovery Risks\">Recovery Risks<a class=\"heading-link\" href=\"#/chapter/09-error-retry-recovery/recovery-risks\" aria-label=\"Link to Recovery Risks\">#</a></h2>\n<ul><li>A full journey run observed a pending modern ACK surviving beyond timeout after reconnect; exact rerun passed.</li><li>Broker shutdown may invoke <code>NettyTcpServer.shutdown</code> twice through explicit broker shutdown and bean disposal; integration logs showed <code>RejectedExecutionException</code> during the second disposal.</li><li><code>ClientMessageHandler</code> keeps a channel alive for generic <code>DecoderException</code>, while <code>ZeroCopyBatchDecoder</code> explicitly closes on partial batch. Other decoder-state failures may not reset the stream.</li><li>Final property flush failure can surface during shutdown after network/executors have already stopped.</li></ul>\n<p>See <a href=\"#/chapter/13-risk-and-edge-cases\">Risks</a> and <a href=\"#/chapter/12-debugging-guide\">Debugging</a>.</p>","headings":[{"level":2,"title":"Exception Model","id":"exception-model"},{"level":2,"title":"Startup Failures","id":"startup-failures"},{"level":2,"title":"Storage Recovery","id":"storage-recovery"},{"level":2,"title":"Pipe Retry And Recovery","id":"pipe-retry-and-recovery"},{"level":2,"title":"Delivery Retry","id":"delivery-retry"},{"level":3,"title":"Adaptive Scheduling","id":"adaptive-scheduling"},{"level":3,"title":"Consumer Failure Backoff","id":"consumer-failure-backoff"},{"level":3,"title":"ACK Timeout","id":"ack-timeout"},{"level":2,"title":"Readiness Retry","id":"readiness-retry"},{"level":2,"title":"Client Reconnect","id":"client-reconnect"},{"level":2,"title":"ACK Recovery","id":"ack-recovery"},{"level":3,"title":"Seeder","id":"seeder"},{"level":3,"title":"Reconciliation","id":"reconciliation"},{"level":2,"title":"Refresh Recovery","id":"refresh-recovery"},{"level":2,"title":"Compaction Recovery","id":"compaction-recovery"},{"level":2,"title":"Property-File Recovery","id":"property-file-recovery"},{"level":2,"title":"Error Handling By Interface","id":"error-handling-by-interface"},{"level":2,"title":"Recovery Risks","id":"recovery-risks"}],"plainText":"Error, Retry, And Recovery Related: Overview, POS state, Compaction, Debugging, Tests. Exception Model Shared exception hierarchy (all unchecked — MessagingException extends RuntimeException): MessagingException StorageException NetworkException ConsumerException DataRefreshException ErrorCode Sources: common/src/main/java/com/messaging/common/exception/. Exceptions can carry context through MessagingException.withContext. ExceptionLogger.java formats shared logging; ExceptionLogger.logAndThrow(log, ex) logs at ERROR and rethrows. Convention (enforced): code raises a MessagingException subclass with an ErrorCode — never a bare IllegalArgumentException / IllegalStateException / RuntimeException. Because the base is unchecked, every layer can comply without throws pollution: value objects (DeliveryKey, ConsumerKey, BatchMetadata, SubscribeRequest) and hot-path decoders (EventType.get, EventFactory) throw structured exceptions with no signature ripple into the read path. Existing catch/throws declarations remain valid; the project catches these at its boundaries (message handlers, BrokerService startup, the delivery loop). Generic validation/lifecycle guards use VALIDATIONINVALIDARGUMENT / BROKERINVALIDSTATE. Startup Failures Storage recovery / network-startup failure is fatal: BrokerService rethrows the StorageException / NetworkException via ExceptionLogger.logAndThrow. Being unchecked, it propagates straight out of the ServerStartupEvent listener and fails the boot (no RuntimeException wrapper needed). TCP bind failure is fatal: NettyTcpServer.java, BrokerService.java. ACK seeding failure is non-fatal; reconciliation is expected to expose gaps. Registry absence/failure is non-fatal; topology manager can continue without a parent. Storage Recovery DefaultStorageRecoveryService.java handles: current and legacy index versions; log bytes without index entries; index entries beyond log end; staging compaction files; discovery of topic/partition directories. FileChannelStorageEngine.recover creates managers for discovered partitions. Storage corruption that cannot be reconciled prevents startup. Tests: SegmentCrashRecoverySpec.groovy, DefaultStorageRecoveryServiceSpec.groovy, integration equivalents, restart journeys. Pipe Retry And Recovery HttpPipeConnector.java: adaptive poll delay between configured min/max; retries network/HTTP/parser failures; persists only the last successfully handled offset; resumes from pipe-offset.properties; supports pause/resume during refresh; copies/releases streaming Netty buffers before parsing. The connector treats partial successful processing transactionally: successfully handled records can advance in finally, while the failed record and later data are retried. TopologyManager.java probes a new parent before switching and keeps the old parent when the probe fails. Delivery Retry Adaptive Scheduling AdaptiveBackoffPolicy.java calculates delay between configured minimum and maximum. Success reduces delay; no data/failure increases it. TopicFairScheduler.java permits configured concurrent work per topic and retains at most one pending retry per delivery key. Consumer Failure Backoff RemoteConsumer.java records consecutive failures and backoff time. BatchDeliveryService.java: blocks while backoff remains; retries failed consumers; unregisters after ten consecutive failures; clears state immediately if send failed before timeout installation; leaves timeout-owned state intact if failure occurred after timeout installation. ACK Timeout Modern: each delivery gets a generation; timeout claims only that generation; claimed timeout restores original offset and clears state. Sources: BatchDeliveryService.java, InMemoryInFlightDeliveryStore.java. Legacy: one pending merged batch per client; generation-specific timeout clears the slot and metrics. Sources: ConsumerRegistry.java, InMemoryPendingAckStore.java. Readiness Retry ConsumerReadinessManager.java retries READY every 5 seconds up to three times and cancels the retry when matching readiness is recorded. Refresh uses separate retry timing in RefreshCoordinator.java: RESET every 5 seconds and READY checks every 10 seconds. Client Reconnect ClientConsumerManager.java: detects disconnect through Netty close future; maintains per-topic/group reconnect guards; exponential reconnect delay 5s, 10s, 20s, 40s, 60s cap; health-checks every 10 seconds; cleans one connection before recreating it; shares one event-loop group. ACK Recovery Seeder AckStoreSeeder.java scans all committed offsets after storage recovery and fills missing per-record ACK entries in chunks. It distinguishes no committed offset from offset 0. Reconciliation AckReconciliationScheduler.java periodically scans registered topic/group ranges up to committed offsets: reports missing count; optionally backfills when auto-sync-enabled=true; keeps in-memory scan checkpoints; pauses and clears checkpoints for a topic during refresh; resumes with a full scan after refresh. Only active registered pairs are reconciled. Historical disconnected groups are not confirmed to be scanned. Refresh Recovery Detailed state behavior is in POS state. Recovery source: RefreshRecoveryService.java. It loads persisted contexts, pauses pipe first, records downtime, restores task scheduling, and resumes by state. Abort watchdog behavior: 10-minute initial window; REPLAYING re-arms when progress is recent; READYSENT receives one extra window; terminal states stop work. Compaction Recovery See Compaction recovery. The key mechanisms are staging files, atomic rename, checkpoints, and segment replacement locks. Property-File Recovery PropertiesFileStore.java uses temp files, force, and move. FlushingPropertiesStore.java: retries/logs periodic flush failure; performs a final flush during stop; can propagate a final flush failure. Consumer offsets intentionally allow a small replay window because synchronous writes are rate-limited. Error Handling By Interface Interface Behavior --- --- TCP producer/subscribe/commit validation Log and close connection Malformed ACK lengths Log and close connection Unknown legacy ACK group Soft-fail without close Delivery send/read failure Backoff, rollback/timeout, eventual unregister Pipe record storage failure Return false; do not advance that offset HTTP controller validation Usually return error map with HTTP 200 Storage recovery failure Fail broker startup ACK seeding/reconciliation failure Degrade and log Sources: broker handlers/controllers and BrokerService.java. Recovery Risks A full journey run observed a pending modern ACK surviving beyond timeout after reconnect; exact rerun passed. Broker shutdown may invoke NettyTcpServer.shutdown twice through explicit broker shutdown and bean disposal; integration logs showed RejectedExecutionException during the second disposal. ClientMessageHandler keeps a channel alive for generic DecoderException, while ZeroCopyBatchDecoder explicitly closes on partial batch. Other decoder-state failures may not reset the stream. Final property flush failure can surface during shutdown after network/executors have already stopped. See Risks and Debugging.","lineCount":189},{"order":10,"file":"10-test-map.md","slug":"10-test-map","title":"Test Map","html":"<h1 id=\"test-map\" data-heading=\"Test Map\">Test Map<a class=\"heading-link\" href=\"#/chapter/10-test-map/test-map\" aria-label=\"Link to Test Map\">#</a></h1>\n<p>Related: <a href=\"#/chapter/03-feature-catalog\">Features</a>, <a href=\"#/chapter/07-pos-machine-state\">POS state</a>, <a href=\"#/chapter/08-compaction-handling\">Compaction</a>, <a href=\"#/chapter/13-risk-and-edge-cases\">Risks</a>, <a href=\"#/chapter/15-open-questions\">Open questions</a>.</p>\n<h2 id=\"build-test-layout\" data-heading=\"Build Test Layout\">Build Test Layout<a class=\"heading-link\" href=\"#/chapter/10-test-map/build-test-layout\" aria-label=\"Link to Build Test Layout\">#</a></h2>\n<p>The root <code>build.gradle</code> defines <code>integrationTest</code>, <code>systemTest</code>, and <code>performanceTest</code>. Each main library module redirects <code>test</code> to <code>src/unitTest/groovy</code>. <code>broker/build.gradle</code> additionally maps:</p>\n<ul><li><code>journeyTest</code> -&gt; <code>broker/src/systemTest/{groovy,java}</code></li><li><code>systemTest</code> -&gt; <code>broker/src/blackboxSystemTest/{groovy,java}</code></li></ul>\n<p>This naming is important when adding or running tests.</p>\n<h2 id=\"executed-results\" data-heading=\"Executed Results\">Executed Results<a class=\"heading-link\" href=\"#/chapter/10-test-map/executed-results\" aria-label=\"Link to Executed Results\">#</a></h2>\n<p>Commands were run on 2026-06-11.</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Command</th>\n<th>Result</th>\n<th>Evidence/notes</th>\n</tr></thead><tbody>\n<tr>\n<td><code>./gradlew test --no-daemon --console=plain</code></td>\n<td>PASS</td>\n<td>740 tests: broker 565, common 25, network 62, pipe 10, storage 78; client/test-consumer no unit source</td>\n</tr>\n<tr>\n<td><code>./gradlew integrationTest --no-daemon --console=plain</code></td>\n<td>FAIL</td>\n<td>Broker reached 118 tests with 2 failures; aggregate stopped before other modules</td>\n</tr>\n<tr>\n<td>Per-module integration command excluding broker</td>\n<td>PASS</td>\n<td>common 20, storage 34, network 69, pipe 33, client 11; test-consumer no source</td>\n</tr>\n<tr>\n<td>Exact broker legacy integration rerun</td>\n<td>FAIL</td>\n<td>2/2 failed again</td>\n</tr>\n<tr>\n<td><code>./gradlew :broker:journeyTest</code></td>\n<td>FAIL once</td>\n<td>36 tests, 1 reconnect/refresh timeout</td>\n</tr>\n<tr>\n<td>Exact failed journey rerun</td>\n<td>PASS</td>\n<td><code>ConsumerCrashDuringReplayJourneySpec</code> passed alone</td>\n</tr>\n<tr>\n<td><code>./gradlew :broker:systemTest</code></td>\n<td>PASS</td>\n<td>1 separate-process black-box test</td>\n</tr>\n<tr>\n<td><code>./gradlew performanceTest</code></td>\n<td>PASS/NO-SOURCE</td>\n<td>All seven module performance source sets empty</td>\n</tr>\n<tr>\n<td><code>./gradlew build</code></td>\n<td>PASS</td>\n<td>Compiles, packages, and runs unit tests; does not include integration/journey/system</td>\n</tr>\n</tbody></table></div>\n<p>Unique full-suite inventory attempted: 740 unit + 285 integration + 36 journey + 1 black-box = 1,062 tests.</p>\n<p>Qodana is configured in <code>qodana.yaml</code>, but <code>qodana</code> was not installed locally. No Checkstyle, SpotBugs, PMD, lint, or contract-test Gradle task was found by <code>./gradlew tasks --all</code>.</p>\n<p>Java runtime used by the shell was OpenJDK 21.0.7; Gradle compiles source/target Java 17 from <code>build.gradle</code>.</p>\n<h2 id=\"observed-failures\" data-heading=\"Observed Failures\">Observed Failures<a class=\"heading-link\" href=\"#/chapter/10-test-map/observed-failures\" aria-label=\"Link to Observed Failures\">#</a></h2>\n<h3 id=\"repeatable-legacy-integration-failure\" data-heading=\"Repeatable Legacy Integration Failure\">Repeatable Legacy Integration Failure<a class=\"heading-link\" href=\"#/chapter/10-test-map/repeatable-legacy-integration-failure\" aria-label=\"Link to Repeatable Legacy Integration Failure\">#</a></h3>\n<p>File: <code>broker/src/integrationTest/groovy/com/messaging/broker/core/BrokerLegacyConsumerIntegrationSpec.groovy</code>.</p>\n<p>Failures:</p>\n<ol><li><code>legacy consumer receives merged batch and ACK advances offsets</code></li><li><code>legacy refresh flow resolves RESET and READY through generic ACKs</code></li></ol>\n<p>Both time out before registration/readiness. Logs state:</p>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>Unknown legacy service: price-quote-service. No topics configured.</code></pre></div>\n<p>Cause is code/config related:</p>\n<ul><li>test registers <code>price-quote-service</code>;</li><li><code>test-consumer/src/main/resources/application.yml</code> also defaults legacy service name to <code>price-quote-service</code>;</li><li>broker <code>legacy-clients.service-topics</code> defines <code>price-quote</code>, not <code>price-quote-service</code>.</li></ul>\n<p>Source: <code>broker/src/main/resources/application.yml</code>, <code>BrokerLegacyConsumerIntegrationSpec.groovy</code>.</p>\n<h3 id=\"flaky-journey-failure\" data-heading=\"Flaky Journey Failure\">Flaky Journey Failure<a class=\"heading-link\" href=\"#/chapter/10-test-map/flaky-journey-failure\" aria-label=\"Link to Flaky Journey Failure\">#</a></h3>\n<p>File: <code>broker/src/systemTest/groovy/com/messaging/broker/systemtest/journey/ConsumerCrashDuringReplayJourneySpec.groovy</code>.</p>\n<p>Aggregate failure: consumer A never received refresh READY after consumer B reconnected. Logs showed group B blocked on a pending ACK for over 25 seconds despite <code>broker.consumer.ack-timeout=5000</code>.</p>\n<p>The exact test passed on a clean rerun. Classification: concurrency/timing flake with a real stale-pending-state signal, not a confirmed deterministic failure.</p>\n<h3 id=\"shutdown-warning\" data-heading=\"Shutdown Warning\">Shutdown Warning<a class=\"heading-link\" href=\"#/chapter/10-test-map/shutdown-warning\" aria-label=\"Link to Shutdown Warning\">#</a></h3>\n<p>Broker integration logs showed <code>RejectedExecutionException: event executor terminated</code> when Micronaut disposed <code>NettyTcpServer</code> after <code>BrokerService.shutdown</code> had already shut it down. Tests continued. Source: <code>network/src/main/java/com/messaging/network/tcp/NettyTcpServer.java</code>, <code>BrokerService.java</code>.</p>\n<h2 id=\"broker-unit-suites\" data-heading=\"Broker Unit Suites\">Broker Unit Suites<a class=\"heading-link\" href=\"#/chapter/10-test-map/broker-unit-suites\" aria-label=\"Link to Broker Unit Suites\">#</a></h2>\n<p>ACK:</p>\n<ul><li><code>AckReconciliationSchedulerSpec</code></li><li><code>AckRecordSpec</code></li><li><code>InMemoryAckStoreSpec</code></li><li><code>RocksDbAckStoreSpec</code></li></ul>\n<p>Compaction:</p>\n<ul><li><code>CompactionCheckpointStoreSpec</code></li><li><code>CompactionPlannerSpec</code></li><li><code>CompactionRewriterSpec</code></li><li><code>CompactionSchedulerSpec</code></li><li><code>InMemoryCompactionIndexSpec</code></li><li><code>RocksDbCompactionIndexSpec</code></li></ul>\n<p>Consumer/delivery/refresh:</p>\n<ul><li><code>AdaptiveBackoffPolicySpec</code></li><li><code>BatchAckServiceRocksDbSpec</code></li><li><code>BatchDeliveryServiceSpec</code></li><li><code>ConsumerAnnotationProcessorSpec</code></li><li><code>ConsumerContextSpec</code></li><li><code>ConsumerDeliveryManagerSpec</code></li><li><code>ConsumerOffsetTrackerSpec</code></li><li><code>ConsumerRegistrationManagerSpec</code></li><li><code>ConsumerRegistrySpec</code></li><li><code>DeliveryStateStoreSpec</code></li><li><code>FlushingPropertiesStoreSpec</code></li><li><code>InMemoryConsumerSessionStoreSpec</code></li><li><code>InMemoryInFlightDeliveryStoreSpec</code></li><li><code>InMemoryPendingAckStoreSpec</code></li><li><code>PropertiesFileStoreSpec</code></li><li><code>RefreshContextSpec</code></li><li><code>RefreshCoordinatorSpec</code></li><li><code>RefreshInitiatorSpec</code></li><li><code>RefreshReadyServiceSpec</code></li><li><code>RefreshRecoveryServiceSpec</code></li><li><code>RefreshReplayServiceSpec</code></li><li><code>RefreshResetServiceSpec</code></li><li><code>RefreshResultSpec</code></li><li><code>RefreshStateMachineSpec</code></li><li><code>RefreshStateStoreSpec</code></li><li><code>RemoteConsumerSpec</code></li><li><code>TopicFairSchedulerSpec</code></li></ul>\n<p>Core/handler/legacy/model/monitoring:</p>\n<ul><li><code>BrokerServiceSpec</code>, <code>ShutdownCoordinatorSpec</code>, <code>TopologyManagerSpec</code>, <code>TopologyPropertiesStoreSpec</code></li><li><code>BatchAckHandlerSpec</code>, <code>MessageParserFactorySpec</code>, <code>ParseExceptionSpec</code>, <code>ReadyAckHandlerSpec</code>, <code>ResetAckHandlerSpec</code>, <code>SubscribeHandlerSpec</code></li><li>JSON/legacy parser and validation specs under <code>broker/src/unitTest/groovy/com/messaging/broker/handler/</code></li><li><code>LegacyConsumerDeliveryManagerSpec</code>, <code>MergedBatchSpec</code>, <code>TopicCursorSpec</code></li><li>DTO specs under <code>broker/src/unitTest/groovy/com/messaging/broker/model/</code></li><li><code>BrokerMetricsSpec</code>, <code>DataRefreshMetricsSpec</code>, <code>LogContextSpec</code>, <code>TraceIdsSpec</code></li></ul>\n<h2 id=\"broker-integration-suites\" data-heading=\"Broker Integration Suites\">Broker Integration Suites<a class=\"heading-link\" href=\"#/chapter/10-test-map/broker-integration-suites\" aria-label=\"Link to Broker Integration Suites\">#</a></h2>\n<ul><li>ACK/compaction: <code>AckStoreIntegrationSpec</code>, <code>CompactionDeliveryFilterIntegrationSpec</code>, <code>CompactionIndexWiringIntegrationSpec</code></li><li>Wiring/executors: <code>ExecutorFactoryIntegrationSpec</code>, <code>HandlerRegistryIntegrationSpec</code></li><li>Consumer: <code>AdaptiveBatchDeliveryManagerIntegrationSpec</code>, <code>ConsumerOffsetTrackerIntegrationSpec</code>, <code>RefreshStateMachineIntegrationSpec</code>, <code>TopicFairSchedulerIntegrationSpec</code>, <code>WatermarkGatePolicyIntegrationSpec</code></li><li>Core: <code>BrokerLegacyConsumerIntegrationSpec</code>, <code>BrokerRegistryIntegrationSpec</code>, <code>BrokerServiceCriticalIntegrationSpec</code>, <code>BrokerServiceIntegrationSpec</code>, <code>BrokerSetupIntegrationSpec</code>, <code>CloudRegistryClientIntegrationSpec</code>, <code>TopologyManagerProbeIntegrationSpec</code></li><li>Handlers: <code>BatchAckHandlerIntegrationSpec</code>, <code>ClientDisconnectHandlerIntegrationSpec</code>, <code>CommitOffsetHandlerIntegrationSpec</code>, <code>DataHandlerIntegrationSpec</code>, <code>SubscribeHandlerIntegrationSpec</code></li><li>HTTP: <code>PipeMessageForwarderIntegrationSpec</code>, <code>RefreshControllerIntegrationSpec</code>, <code>TestDataControllerIntegrationSpec</code>, <code>ThreadDiagnosticsControllerIntegrationSpec</code></li><li>Monitoring: <code>ConsumerGroupMetricsIntegrationSpec</code>, <code>MemoryMonitorIntegrationSpec</code>, <code>ThreadMonitorIntegrationSpec</code></li></ul>\n<p>All are under <code>broker/src/integrationTest/groovy/com/messaging/broker/</code>.</p>\n<h2 id=\"journey-suites\" data-heading=\"Journey Suites\">Journey Suites<a class=\"heading-link\" href=\"#/chapter/10-test-map/journey-suites\" aria-label=\"Link to Journey Suites\">#</a></h2>\n<p>All under <code>broker/src/systemTest/groovy/com/messaging/broker/systemtest/journey/</code>:</p>\n<ul><li>Startup/restart: <code>BrokerRestartJourneySpec</code>, <code>AckStoreSeederRestartJourneySpec</code></li><li>Delivery: <code>MessageDeliveryJourneySpec</code>, <code>MultiConsumerJourneySpec</code>, <code>LegacyConsumerJourneySpec</code>, <code>FlakyConsumerJourneySpec</code>, <code>ConsumerReconnectJourneySpec</code>, <code>GappedOffsetJourneySpec</code>, <code>DuplicateMsgKeyJourneySpec</code>, <code>LargeRecordRocksDbAckSpec</code>, <code>SegmentRolloverWithPipeDataJourneySpec</code></li><li>Pipe: <code>PipeOutageJourneySpec</code></li><li>Refresh: <code>DataRefreshJourneySpec</code>, <code>ConcurrentRefreshJourneySpec</code>, <code>ConsumerCrashDuringReplayJourneySpec</code>, <code>LateConsumerDuringRefreshJourneySpec</code>, <code>RefreshRestartRecoveryJourneySpec</code>, <code>RefreshWithPendingPipeDataJourneySpec</code>, <code>RefreshAwareReconciliationJourneySpec</code></li><li>Compaction: <code>CompactionConcurrentPipeDiffKeyJourneySpec</code>, <code>CompactionConcurrentPipeSameKeyJourneySpec</code>, <code>CompactionDeliveryRecoveryJourneySpec</code>, <code>CompactionDuringRefreshJourneySpec</code>, <code>CompactionRaceWindowLegacyDeliveryJourneySpec</code>, <code>CompactionRestartRecoveryJourneySpec</code>, <code>LateConsumerAfterCompactionJourneySpec</code>, <code>RefreshWithCompactedAndActiveSegmentsJourneySpec</code>, <code>TombstoneLifecycleJourneySpec</code></li></ul>\n<p>These map directly to <a href=\"#/chapter/03-feature-catalog\">Features</a>, <a href=\"#/chapter/07-pos-machine-state\">POS state</a>, and <a href=\"#/chapter/08-compaction-handling\">Compaction</a>.</p>\n<h2 id=\"black-box-system-suite\" data-heading=\"Black-Box System Suite\">Black-Box System Suite<a class=\"heading-link\" href=\"#/chapter/10-test-map/black-box-system-suite\" aria-label=\"Link to Black-Box System Suite\">#</a></h2>\n<p><code>broker/src/blackboxSystemTest/groovy/com/messaging/broker/systemtest/blackbox/ModernConsumerEndToEndSystemSpec.groovy</code> launches broker and test-consumer JVM processes using classpaths supplied by <code>broker/build.gradle</code>.</p>\n<h2 id=\"other-module-suites\" data-heading=\"Other Module Suites\">Other Module Suites<a class=\"heading-link\" href=\"#/chapter/10-test-map/other-module-suites\" aria-label=\"Link to Other Module Suites\">#</a></h2>\n<h3 id=\"common\" data-heading=\"Common\">Common<a class=\"heading-link\" href=\"#/chapter/10-test-map/common\" aria-label=\"Link to Common\">#</a></h3>\n<p>Models, exceptions, no-op error handler, and auth filter under <code>common/src/unitTest/groovy/</code> and <code>common/src/integrationTest/groovy/</code>.</p>\n<h3 id=\"storage\" data-heading=\"Storage\">Storage<a class=\"heading-link\" href=\"#/chapter/10-test-map/storage\" aria-label=\"Link to Storage\">#</a></h3>\n<p>Engine, segment, concurrent read, crash recovery, memory leak, metadata, recovery service, and watermarks under <code>storage/src/unitTest/groovy/</code> and <code>storage/src/integrationTest/groovy/</code>.</p>\n<h3 id=\"network\" data-heading=\"Network\">Network<a class=\"heading-link\" href=\"#/chapter/10-test-map/network\" aria-label=\"Link to Network\">#</a></h3>\n<p>Binary/JSON/zero-copy codecs, handlers, protocol detection, legacy event codecs/state, metrics, and TCP integration under <code>network/src/unitTest/groovy/</code> and <code>network/src/integrationTest/groovy/</code>.</p>\n<h3 id=\"pipe\" data-heading=\"Pipe\">Pipe<a class=\"heading-link\" href=\"#/chapter/10-test-map/pipe\" aria-label=\"Link to Pipe\">#</a></h3>\n<p><code>HttpPipeConnectorSpec</code>, connector integration, <code>PipeServerIntegrationSpec</code>, and <code>PipeMetricsIntegrationSpec</code>.</p>\n<h3 id=\"client\" data-heading=\"Client\">Client<a class=\"heading-link\" href=\"#/chapter/10-test-map/client\" aria-label=\"Link to Client\">#</a></h3>\n<p><code>client/src/integrationTest/groovy/com/messaging/client/ClientConsumerManagerIntegrationSpec.groovy</code> verifies discovery, connection, subscribe, data/control routing, and lifecycle.</p>\n<h2 id=\"coverage-gaps\" data-heading=\"Coverage Gaps\">Coverage Gaps<a class=\"heading-link\" href=\"#/chapter/10-test-map/coverage-gaps\" aria-label=\"Link to Coverage Gaps\">#</a></h2>\n<ul><li>No contract-test framework or suite.</li><li>No performance tests despite configured source sets.</li><li>No local static-analysis execution; Qodana config only.</li><li><code>storage/src/test/java/com/messaging/storage/mmap/MMapStorageEngineTest.java</code> is orphaned from the custom <code>test</code> source set.</li><li>No confirmed inbound HTTP security tests.</li><li>No production-size compaction/load test.</li><li>No real power-loss/filesystem durability test.</li><li>No deterministic stress test for the stale reconnect pending-ACK signal.</li><li>No test enforcing the legacy service-name mapping as a contract.</li></ul>\n<h2 id=\"pipe-consistency-added-2026-06-11\" data-heading=\"Pipe Consistency (added 2026-06-11)\">Pipe Consistency (added 2026-06-11)<a class=\"heading-link\" href=\"#/chapter/10-test-map/pipe-consistency-added-2026-06-11\" aria-label=\"Link to Pipe Consistency (added 2026-06-11)\">#</a></h2>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Spec</th>\n<th>Source set</th>\n<th>Covers</th>\n</tr></thead><tbody>\n<tr>\n<td><code>consistency/KeyspaceDigestSpec</code></td>\n<td>unitTest</td>\n<td>digest order-independence, watermark filter, single-key bucket flip, count asymmetry, hash determinism</td>\n</tr>\n<tr>\n<td><code>consistency/PipeConsistencyServiceSpec</code></td>\n<td>unitTest</td>\n<td>consistent path (1 network call), missing vs benign-dead-key, stale vs zombie, fabricated (authoritative ABSENT → INCONSISTENT, non-authoritative → extraKeys), lag classification, watermark clamp, UNREACHABLE/UNSUPPORTED_PARENT, single-flight, all-topics walk</td>\n</tr>\n<tr>\n<td><code>compaction/CompactionIndexForEachEntrySpec</code></td>\n<td>unitTest</td>\n<td>streaming iteration contract on both index backends, meta-key exclusion</td>\n</tr>\n<tr>\n<td><code>http/PipeConsistencyEndpointsIntegrationSpec</code></td>\n<td>integrationTest</td>\n<td>digest/bucket/classify against real storage+index, admin trigger + report polling, input validation</td>\n</tr>\n<tr>\n<td><code>http/PipeConsistencyDisabledIntegrationSpec</code></td>\n<td>integrationTest</td>\n<td>fail-closed 404/503 contract with feature disabled</td>\n</tr>\n<tr>\n<td><code>consistency/PipeConsistencySchedulerSpec</code></td>\n<td>unitTest</td>\n<td>auto-run fires all-topics check; skip reasons (disabled/running/offline/memory-pressure); cloud target needs no parent; throwing check contained</td>\n</tr>\n</tbody></table></div>\n<p>| <code>journey/PipeConsistencyEscalationJourneySpec</code> | journeyTest | real broker: clamp→pending→escalation, head-probe filtering of behind candidate, full verdict from qualified verifier, streak reset | | <code>blackbox/PipeConsistencySystemSpec</code> | systemTest | cross-process: subprocess broker + real pipe ingest + admin HTTP trigger + CONSISTENT verdict vs stub parent |</p>\n<p>Escalation unit coverage in <code>PipeConsistencyServiceSpec</code>: pending-first-clamp, dead/behind candidate skip, escalated INCONSISTENT, all-behind stays pending, streak reset. Cloud-server: <code>PipeConsistencyEndpointTest</code> (8 tests incl. head + verifierCandidates contract).</p>\n<p>Pipe-ingest sync invariant (segments ↔ compaction index, added 2026-06-12): <code>core/BrokerServiceSpec</code> (unitTest) — append+updateKey both fire on success; duplicate re-send skips the storage write but heals the index; throwing <code>updateKey</code> fails the ingest so the pipe offset is not advanced. <code>compaction/RocksDbCompactionIndexSpec</code> — <code>updateKey</code> propagates a backend <code>RocksDBException</code> as <code>IllegalStateException</code> instead of swallowing. <code>ConsistencyTamperTestController</code> has NO specs by design (throwaway fault-injection tool).</p>\n<p>Coverage gaps: drill-down cap paths (413 / max-drilldown-buckets) only unit-covered.</p>\n<p>Note: the two legacy integration failures listed above (and the <code>price-quote-service</code> naming) were resolved on 2026-06-11 — specs now use the fleet's <code>price-quote</code> service name; <code>./gradlew :broker:integrationTest</code> and <code>:broker:journeyTest</code> pass.</p>","headings":[{"level":2,"title":"Build Test Layout","id":"build-test-layout"},{"level":2,"title":"Executed Results","id":"executed-results"},{"level":2,"title":"Observed Failures","id":"observed-failures"},{"level":3,"title":"Repeatable Legacy Integration Failure","id":"repeatable-legacy-integration-failure"},{"level":3,"title":"Flaky Journey Failure","id":"flaky-journey-failure"},{"level":3,"title":"Shutdown Warning","id":"shutdown-warning"},{"level":2,"title":"Broker Unit Suites","id":"broker-unit-suites"},{"level":2,"title":"Broker Integration Suites","id":"broker-integration-suites"},{"level":2,"title":"Journey Suites","id":"journey-suites"},{"level":2,"title":"Black-Box System Suite","id":"black-box-system-suite"},{"level":2,"title":"Other Module Suites","id":"other-module-suites"},{"level":3,"title":"Common","id":"common"},{"level":3,"title":"Storage","id":"storage"},{"level":3,"title":"Network","id":"network"},{"level":3,"title":"Pipe","id":"pipe"},{"level":3,"title":"Client","id":"client"},{"level":2,"title":"Coverage Gaps","id":"coverage-gaps"},{"level":2,"title":"Pipe Consistency (added 2026-06-11)","id":"pipe-consistency-added-2026-06-11"}],"plainText":"Test Map Related: Features, POS state, Compaction, Risks, Open questions. Build Test Layout The root build.gradle defines integrationTest, systemTest, and performanceTest. Each main library module redirects test to src/unitTest/groovy. broker/build.gradle additionally maps: journeyTest -> broker/src/systemTest/{groovy,java} systemTest -> broker/src/blackboxSystemTest/{groovy,java} This naming is important when adding or running tests. Executed Results Commands were run on 2026-06-11. Command Result Evidence/notes --- --- --- ./gradlew test --no-daemon --console=plain PASS 740 tests: broker 565, common 25, network 62, pipe 10, storage 78; client/test-consumer no unit source ./gradlew integrationTest --no-daemon --console=plain FAIL Broker reached 118 tests with 2 failures; aggregate stopped before other modules Per-module integration command excluding broker PASS common 20, storage 34, network 69, pipe 33, client 11; test-consumer no source Exact broker legacy integration rerun FAIL 2/2 failed again ./gradlew :broker:journeyTest FAIL once 36 tests, 1 reconnect/refresh timeout Exact failed journey rerun PASS ConsumerCrashDuringReplayJourneySpec passed alone ./gradlew :broker:systemTest PASS 1 separate-process black-box test ./gradlew performanceTest PASS/NO-SOURCE All seven module performance source sets empty ./gradlew build PASS Compiles, packages, and runs unit tests; does not include integration/journey/system Unique full-suite inventory attempted: 740 unit + 285 integration + 36 journey + 1 black-box = 1,062 tests. Qodana is configured in qodana.yaml, but qodana was not installed locally. No Checkstyle, SpotBugs, PMD, lint, or contract-test Gradle task was found by ./gradlew tasks --all. Java runtime used by the shell was OpenJDK 21.0.7; Gradle compiles source/target Java 17 from build.gradle. Observed Failures Repeatable Legacy Integration Failure File: broker/src/integrationTest/groovy/com/messaging/broker/core/BrokerLegacyConsumerIntegrationSpec.groovy. Failures: legacy consumer receives merged batch and ACK advances offsets legacy refresh flow resolves RESET and READY through generic ACKs Both time out before registration/readiness. Logs state: Cause is code/config related: test registers price-quote-service; test-consumer/src/main/resources/application.yml also defaults legacy service name to price-quote-service; broker legacy-clients.service-topics defines price-quote, not price-quote-service. Source: broker/src/main/resources/application.yml, BrokerLegacyConsumerIntegrationSpec.groovy. Flaky Journey Failure File: broker/src/systemTest/groovy/com/messaging/broker/systemtest/journey/ConsumerCrashDuringReplayJourneySpec.groovy. Aggregate failure: consumer A never received refresh READY after consumer B reconnected. Logs showed group B blocked on a pending ACK for over 25 seconds despite broker.consumer.ack-timeout=5000. The exact test passed on a clean rerun. Classification: concurrency/timing flake with a real stale-pending-state signal, not a confirmed deterministic failure. Shutdown Warning Broker integration logs showed RejectedExecutionException: event executor terminated when Micronaut disposed NettyTcpServer after BrokerService.shutdown had already shut it down. Tests continued. Source: network/src/main/java/com/messaging/network/tcp/NettyTcpServer.java, BrokerService.java. Broker Unit Suites ACK: AckReconciliationSchedulerSpec AckRecordSpec InMemoryAckStoreSpec RocksDbAckStoreSpec Compaction: CompactionCheckpointStoreSpec CompactionPlannerSpec CompactionRewriterSpec CompactionSchedulerSpec InMemoryCompactionIndexSpec RocksDbCompactionIndexSpec Consumer/delivery/refresh: AdaptiveBackoffPolicySpec BatchAckServiceRocksDbSpec BatchDeliveryServiceSpec ConsumerAnnotationProcessorSpec ConsumerContextSpec ConsumerDeliveryManagerSpec ConsumerOffsetTrackerSpec ConsumerRegistrationManagerSpec ConsumerRegistrySpec DeliveryStateStoreSpec FlushingPropertiesStoreSpec InMemoryConsumerSessionStoreSpec InMemoryInFlightDeliveryStoreSpec InMemoryPendingAckStoreSpec PropertiesFileStoreSpec RefreshContextSpec RefreshCoordinatorSpec RefreshInitiatorSpec RefreshReadyServiceSpec RefreshRecoveryServiceSpec RefreshReplayServiceSpec RefreshResetServiceSpec RefreshResultSpec RefreshStateMachineSpec RefreshStateStoreSpec RemoteConsumerSpec TopicFairSchedulerSpec Core/handler/legacy/model/monitoring: BrokerServiceSpec, ShutdownCoordinatorSpec, TopologyManagerSpec, TopologyPropertiesStoreSpec BatchAckHandlerSpec, MessageParserFactorySpec, ParseExceptionSpec, ReadyAckHandlerSpec, ResetAckHandlerSpec, SubscribeHandlerSpec JSON/legacy parser and validation specs under broker/src/unitTest/groovy/com/messaging/broker/handler/ LegacyConsumerDeliveryManagerSpec, MergedBatchSpec, TopicCursorSpec DTO specs under broker/src/unitTest/groovy/com/messaging/broker/model/ BrokerMetricsSpec, DataRefreshMetricsSpec, LogContextSpec, TraceIdsSpec Broker Integration Suites ACK/compaction: AckStoreIntegrationSpec, CompactionDeliveryFilterIntegrationSpec, CompactionIndexWiringIntegrationSpec Wiring/executors: ExecutorFactoryIntegrationSpec, HandlerRegistryIntegrationSpec Consumer: AdaptiveBatchDeliveryManagerIntegrationSpec, ConsumerOffsetTrackerIntegrationSpec, RefreshStateMachineIntegrationSpec, TopicFairSchedulerIntegrationSpec, WatermarkGatePolicyIntegrationSpec Core: BrokerLegacyConsumerIntegrationSpec, BrokerRegistryIntegrationSpec, BrokerServiceCriticalIntegrationSpec, BrokerServiceIntegrationSpec, BrokerSetupIntegrationSpec, CloudRegistryClientIntegrationSpec, TopologyManagerProbeIntegrationSpec Handlers: BatchAckHandlerIntegrationSpec, ClientDisconnectHandlerIntegrationSpec, CommitOffsetHandlerIntegrationSpec, DataHandlerIntegrationSpec, SubscribeHandlerIntegrationSpec HTTP: PipeMessageForwarderIntegrationSpec, RefreshControllerIntegrationSpec, TestDataControllerIntegrationSpec, ThreadDiagnosticsControllerIntegrationSpec Monitoring: ConsumerGroupMetricsIntegrationSpec, MemoryMonitorIntegrationSpec, ThreadMonitorIntegrationSpec All are under broker/src/integrationTest/groovy/com/messaging/broker/. Journey Suites All under broker/src/systemTest/groovy/com/messaging/broker/systemtest/journey/: Startup/restart: BrokerRestartJourneySpec, AckStoreSeederRestartJourneySpec Delivery: MessageDeliveryJourneySpec, MultiConsumerJourneySpec, LegacyConsumerJourneySpec, FlakyConsumerJourneySpec, ConsumerReconnectJourneySpec, GappedOffsetJourneySpec, DuplicateMsgKeyJourneySpec, LargeRecordRocksDbAckSpec, SegmentRolloverWithPipeDataJourneySpec Pipe: PipeOutageJourneySpec Refresh: DataRefreshJourneySpec, ConcurrentRefreshJourneySpec, ConsumerCrashDuringReplayJourneySpec, LateConsumerDuringRefreshJourneySpec, RefreshRestartRecoveryJourneySpec, RefreshWithPendingPipeDataJourneySpec, RefreshAwareReconciliationJourneySpec Compaction: CompactionConcurrentPipeDiffKeyJourneySpec, CompactionConcurrentPipeSameKeyJourneySpec, CompactionDeliveryRecoveryJourneySpec, CompactionDuringRefreshJourneySpec, CompactionRaceWindowLegacyDeliveryJourneySpec, CompactionRestartRecoveryJourneySpec, LateConsumerAfterCompactionJourneySpec, RefreshWithCompactedAndActiveSegmentsJourneySpec, TombstoneLifecycleJourneySpec These map directly to Features, POS state, and Compaction. Black-Box System Suite broker/src/blackboxSystemTest/groovy/com/messaging/broker/systemtest/blackbox/ModernConsumerEndToEndSystemSpec.groovy launches broker and test-consumer JVM processes using classpaths supplied by broker/build.gradle. Other Module Suites Common Models, exceptions, no-op error handler, and auth filter under common/src/unitTest/groovy/ and common/src/integrationTest/groovy/. Storage Engine, segment, concurrent read, crash recovery, memory leak, metadata, recovery service, and watermarks under storage/src/unitTest/groovy/ and storage/src/integrationTest/groovy/. Network Binary/JSON/zero-copy codecs, handlers, protocol detection, legacy event codecs/state, metrics, and TCP integration under network/src/unitTest/groovy/ and network/src/integrationTest/groovy/. Pipe HttpPipeConnectorSpec, connector integration, PipeServerIntegrationSpec, and PipeMetricsIntegrationSpec. Client client/src/integrationTest/groovy/com/messaging/client/ClientConsumerManagerIntegrationSpec.groovy verifies discovery, connection, subscribe, data/control routing, and lifecycle. Coverage Gaps No contract-test framework or suite. No performance tests despite configured source sets. No local static-analysis execution; Qodana config only. storage/src/test/java/com/messaging/storage/mmap/MMapStorageEngineTest.java is orphaned from the custom test source set. No confirmed inbound HTTP security tests. No production-size compaction/load test. No real power-loss/filesystem durability test. No deterministic stress test for the stale reconnect pending-ACK signal. No test enforcing the legacy service-name mapping as a contract. Pipe Consistency (added 2026-06-11) Spec Source set Covers --- --- --- consistency/KeyspaceDigestSpec unitTest digest order-independence, watermark filter, single-key bucket flip, count asymmetry, hash determinism consistency/PipeConsistencyServiceSpec unitTest consistent path (1 network call), missing vs benign-dead-key, stale vs zombie, fabricated (authoritative ABSENT → INCONSISTENT, non-authoritative → extraKeys), lag classification, watermark clamp, UNREACHABLE/UNSUPPORTEDPARENT, single-flight, all-topics walk compaction/CompactionIndexForEachEntrySpec unitTest streaming iteration contract on both index backends, meta-key exclusion http/PipeConsistencyEndpointsIntegrationSpec integrationTest digest/bucket/classify against real storage+index, admin trigger + report polling, input validation http/PipeConsistencyDisabledIntegrationSpec integrationTest fail-closed 404/503 contract with feature disabled consistency/PipeConsistencySchedulerSpec unitTest auto-run fires all-topics check; skip reasons (disabled/running/offline/memory-pressure); cloud target needs no parent; throwing check contained journey/PipeConsistencyEscalationJourneySpec journeyTest real broker: clamp→pending→escalation, head-probe filtering of behind candidate, full verdict from qualified verifier, streak reset blackbox/PipeConsistencySystemSpec systemTest cross-process: subprocess broker + real pipe ingest + admin HTTP trigger + CONSISTENT verdict vs stub parent Escalation unit coverage in PipeConsistencyServiceSpec: pending-first-clamp, dead/behind candidate skip, escalated INCONSISTENT, all-behind stays pending, streak reset. Cloud-server: PipeConsistencyEndpointTest (8 tests incl. head + verifierCandidates contract). Pipe-ingest sync invariant (segments ↔ compaction index, added 2026-06-12): core/BrokerServiceSpec (unitTest) — append+updateKey both fire on success; duplicate re-send skips the storage write but heals the index; throwing updateKey fails the ingest so the pipe offset is not advanced. compaction/RocksDbCompactionIndexSpec — updateKey propagates a backend RocksDBException as IllegalStateException instead of swallowing. ConsistencyTamperTestController has NO specs by design (throwaway fault-injection tool). Coverage gaps: drill-down cap paths (413 / max-drilldown-buckets) only unit-covered. Note: the two legacy integration failures listed above (and the price-quote-service naming) were resolved on 2026-06-11 — specs now use the fleet's price-quote service name; ./gradlew :broker:integrationTest and :broker:journeyTest pass.","lineCount":222},{"order":11,"file":"11-runtime-config.md","slug":"11-runtime-config","title":"Runtime Configuration","html":"<h1 id=\"runtime-configuration\" data-heading=\"Runtime Configuration\">Runtime Configuration<a class=\"heading-link\" href=\"#/chapter/11-runtime-config/runtime-configuration\" aria-label=\"Link to Runtime Configuration\">#</a></h1>\n<p>Related: <a href=\"#/chapter/01-system-overview\">Overview</a>, <a href=\"#/chapter/02-module-map\">Module map</a>, <a href=\"#/chapter/04-api-catalog\">APIs</a>, <a href=\"#/chapter/07-pos-machine-state\">POS state</a>, <a href=\"#/chapter/13-risk-and-edge-cases\">Risks</a>.</p>\n<p>Primary broker config: <code>broker/src/main/resources/application.yml</code>. Consumer config: <code>test-consumer/src/main/resources/application.yml</code>. Container config: <code>Dockerfile</code>.</p>\n<h2 id=\"ports-and-identity\" data-heading=\"Ports And Identity\">Ports And Identity<a class=\"heading-link\" href=\"#/chapter/11-runtime-config/ports-and-identity\" aria-label=\"Link to Ports And Identity\">#</a></h2>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Setting</th>\n<th>Application default</th>\n<th>Docker default</th>\n</tr></thead><tbody>\n<tr>\n<td>Broker TCP</td>\n<td><code>19092</code></td>\n<td><code>9092</code></td>\n</tr>\n<tr>\n<td>Broker HTTP</td>\n<td><code>8082</code></td>\n<td><code>8081</code></td>\n</tr>\n<tr>\n<td>Node ID</td>\n<td><code>local-001</code></td>\n<td><code>broker-001</code></td>\n</tr>\n</tbody></table></div>\n<p>Environment variables make Docker values override YAML. Documentation and operator commands must not assume one set.</p>\n<h2 id=\"storage\" data-heading=\"Storage\">Storage<a class=\"heading-link\" href=\"#/chapter/11-runtime-config/storage\" aria-label=\"Link to Storage\">#</a></h2>\n<ul><li>Type: <code>filechannel</code></li><li>Data dir: <code>${DATA_DIR:/Users/anuragmishra/Desktop/workspace/messaging/data/}</code></li><li>Segment size: 100 MB</li><li>Flush interval: 1 second</li><li>Cache tail retained: 4 MB</li></ul>\n<p>Code property paths use both <code>data-dir</code> and <code>dataDir</code>; Micronaut relaxed binding is expected, but <code>ContainerResourceMonitor.java</code> specifically injects <code>broker.storage.dataDir</code>. Tests comment on supporting both forms.</p>\n<p>Implementations are selected by <code>broker.storage.type</code> in <code>FileChannelStorageEngine.java</code> and <code>MMapStorageEngine.java</code>.</p>\n<h2 id=\"network-and-delivery\" data-heading=\"Network And Delivery\">Network And Delivery<a class=\"heading-link\" href=\"#/chapter/11-runtime-config/network-and-delivery\" aria-label=\"Link to Network And Delivery\">#</a></h2>\n<ul><li>Netty boss threads: 1</li><li>Netty worker threads: 4</li><li>ACK timeout: 120 seconds</li><li>Send timeout: <code>base 1s + floor(batchMB) * 2s</code></li><li>Storage read timeout: 30 seconds</li><li>Batch byte limit: 256 KB</li><li>Adaptive poll: 200 ms to 5 seconds</li><li>Fair scheduler: 2 threads, 1 in-flight task per topic</li></ul>\n<p>Sources: <code>application.yml</code>, <code>BatchDeliveryService.java</code>, <code>TopicFairScheduler.java</code>.</p>\n<h2 id=\"executors\" data-heading=\"Executors\">Executors<a class=\"heading-link\" href=\"#/chapter/11-runtime-config/executors\" aria-label=\"Link to Executors\">#</a></h2>\n<p>Configured:</p>\n<ul><li>ACK: 2 threads, queue 1000</li><li>ACK storage: 2 threads, queue 100</li><li>consumer scheduler: 2 threads</li><li>storage: 2 threads, queue 100</li></ul>\n<p>Factory also creates compaction, registry, refresh, and flush executors with code defaults in <code>broker/src/main/java/com/messaging/broker/config/ExecutorFactory.java</code>.</p>\n<h2 id=\"topology-and-pipe\" data-heading=\"Topology And Pipe\">Topology And Pipe<a class=\"heading-link\" href=\"#/chapter/11-runtime-config/topology-and-pipe\" aria-label=\"Link to Topology And Pipe\">#</a></h2>\n<ul><li>Registry URL: <code>http://localhost:8080</code></li><li>Node ID sent as query parameter</li><li>Pipe min/max interval: 500 ms / 20 seconds</li><li>Pipe limit: 5</li><li>Topology registry poll interval: 30 seconds in <code>TopologyManager.java</code></li><li>Parent probe: <code>/health</code>, 2-second connect and 3-second request timeout in <code>TopologyManager.java</code></li></ul>\n<p>Persistent files: <code>topology.properties</code>, <code>pipe-offset.properties</code>.</p>\n<h2 id=\"ack-store\" data-heading=\"ACK Store\">ACK Store<a class=\"heading-link\" href=\"#/chapter/11-runtime-config/ack-store\" aria-label=\"Link to ACK Store\">#</a></h2>\n<ul><li>Backend: RocksDB</li><li>Live replay: enabled</li><li>Startup seeding: enabled</li><li>Path: <code>${DATA_DIR}/ack-store</code></li><li>Shared block cache: 16 MB</li><li>Reconciliation: every 5 minutes, first after 2 minutes</li><li>Auto-sync: disabled</li></ul>\n<p>Sources: <code>application.yml</code>, ACK package, <code>SharedRocksDb.java</code>.</p>\n<h2 id=\"compaction\" data-heading=\"Compaction\">Compaction<a class=\"heading-link\" href=\"#/chapter/11-runtime-config/compaction\" aria-label=\"Link to Compaction\">#</a></h2>\n<ul><li>Backend: RocksDB index</li><li>Enabled</li><li>Tombstone retention: 7 days</li><li>Window: 10 sealed segments</li><li>Max topics/run: unlimited integer default</li><li>Minimum sealed segments/topic: 1</li><li>CPU guard: 70%</li><li>Heap guard: 70%</li><li>Schedule: initial 5 minutes, then every 24 hours</li></ul>\n<p>Sources: <code>application.yml</code>, <code>CompactionScheduler.java</code>.</p>\n<h2 id=\"legacy-service-topic-map\" data-heading=\"Legacy Service Topic Map\">Legacy Service Topic Map<a class=\"heading-link\" href=\"#/chapter/11-runtime-config/legacy-service-topic-map\" aria-label=\"Link to Legacy Service Topic Map\">#</a></h2>\n<p>Source: <code>broker/src/main/resources/application.yml</code>.</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Service key</th>\n<th>Topics</th>\n</tr></thead><tbody>\n<tr>\n<td><code>price-quote</code></td>\n<td><code>prices-v1</code>, <code>reference-data-v5</code>, <code>non-promotable-products</code>, <code>prices-v4</code>, <code>minimum-price</code>, <code>deposit</code></td>\n</tr>\n<tr>\n<td><code>product-svc-lite</code></td>\n<td><code>product-base-document</code></td>\n</tr>\n<tr>\n<td><code>search-enterprise</code></td>\n<td><code>search-product</code></td>\n</tr>\n<tr>\n<td><code>tesco-location</code></td>\n<td><code>location</code>, <code>location-clusters</code></td>\n</tr>\n<tr>\n<td><code>customer-order-on-till</code></td>\n<td><code>selling-restrictions</code></td>\n</tr>\n<tr>\n<td><code>colleague-facts</code></td>\n<td><code>colleague-facts-jobs</code>, <code>colleague-facts-legacy</code></td>\n</tr>\n<tr>\n<td><code>loss-prevention-api</code></td>\n<td>four loss-prevention topics</td>\n</tr>\n<tr>\n<td><code>stored-value-services</code></td>\n<td>banned/active promotion topics</td>\n</tr>\n<tr>\n<td><code>colleague-identity</code></td>\n<td><code>colleague-card-pin</code></td>\n</tr>\n<tr>\n<td><code>distributed-identity</code></td>\n<td><code>colleague-card-pin-v2</code></td>\n</tr>\n<tr>\n<td><code>dcxp-content</code></td>\n<td><code>dcxp-content</code></td>\n</tr>\n<tr>\n<td><code>restriction-service-on-tills</code></td>\n<td><code>restriction-rules</code></td>\n</tr>\n<tr>\n<td><code>dcxp-ugc</code></td>\n<td><code>dcxp-ugc</code></td>\n</tr>\n</tbody></table></div>\n<p>Mismatch: test-consumer defaults legacy service name to <code>price-quote-service</code>, which is not a key in this map.</p>\n<h2 id=\"security-and-authentication\" data-heading=\"Security And Authentication\">Security And Authentication<a class=\"heading-link\" href=\"#/chapter/11-runtime-config/security-and-authentication\" aria-label=\"Link to Security And Authentication\">#</a></h2>\n<p>Broker YAML defines:</p>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>broker.registry.auth.bearer-token</code></pre></div>\n<p>Outbound filter requires:</p>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>text</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>broker.http.auth.token</code></pre></div>\n<p>Source: <code>common/src/main/java/com/messaging/common/http/AuthTokenClientFilter.java</code>.</p>\n<p>No adapter between the keys was found, so setting only <code>AUTH_BEARER_TOKEN</code> from current YAML may not activate the filter.</p>\n<p>No inbound server filter or controller <code>@Secured</code> annotation was found. Management endpoint sensitivity is configured, but enforcement is not confirmed.</p>\n<h2 id=\"monitoring\" data-heading=\"Monitoring\">Monitoring<a class=\"heading-link\" href=\"#/chapter/11-runtime-config/monitoring\" aria-label=\"Link to Monitoring\">#</a></h2>\n<p>Prometheus, JVM, processor, uptime, file, and logback metrics are enabled. Health/prometheus/metrics are marked non-sensitive; other management endpoints default sensitive.</p>\n<p>Scheduled monitors:</p>\n<ul><li>container resources: 10 seconds;</li><li>memory: 10 seconds;</li><li>storage segments: 15 seconds;</li><li>threads: 30 seconds.</li></ul>\n<p>Sources: monitoring package and <code>application.yml</code>.</p>\n<h2 id=\"consumer-runtime\" data-heading=\"Consumer Runtime\">Consumer Runtime<a class=\"heading-link\" href=\"#/chapter/11-runtime-config/consumer-runtime\" aria-label=\"Link to Consumer Runtime\">#</a></h2>\n<p><code>test-consumer</code> defaults:</p>\n<ul><li>HTTP <code>8081</code></li><li>modern topics from <code>CONSUMER_TOPICS</code>/<code>CONSUMER_TOPIC</code></li><li>group from <code>CONSUMER_GROUP</code></li><li>broker host <code>localhost</code>, port <code>9092</code></li><li>legacy mode disabled</li><li>legacy service default <code>price-quote-service</code></li></ul>\n<p>Source: <code>test-consumer/src/main/resources/application.yml</code>.</p>\n<h2 id=\"container\" data-heading=\"Container\">Container<a class=\"heading-link\" href=\"#/chapter/11-runtime-config/container\" aria-label=\"Link to Container\">#</a></h2>\n<p><code>Dockerfile</code>:</p>\n<ul><li>builds with Gradle 8.5/JDK 17;</li><li>runs Eclipse Temurin 17 JRE;</li><li>imports a repository CA certificate at build time;</li><li>creates non-root <code>broker</code>;</li><li>installs SQLite and curl;</li><li>defaults heap to 512 MB initial / 2 GB max with G1;</li><li>health-checks <code>/health</code>.</li></ul>\n<p>The image builds <code>:broker:build -x test</code>, so image construction does not execute tests.</p>\n<h2 id=\"static-analysis\" data-heading=\"Static Analysis\">Static Analysis<a class=\"heading-link\" href=\"#/chapter/11-runtime-config/static-analysis\" aria-label=\"Link to Static Analysis\">#</a></h2>\n<p><code>qodana.yaml</code> selects <code>qodana.starter</code>, JDK 21, and <code>jetbrains/qodana-jvm:latest</code>. No Qodana CLI was installed during this analysis and no CI file describing invocation was found.</p>\n<h2 id=\"environment-specific-test-config\" data-heading=\"Environment-Specific Test Config\">Environment-Specific Test Config<a class=\"heading-link\" href=\"#/chapter/11-runtime-config/environment-specific-test-config\" aria-label=\"Link to Environment-Specific Test Config\">#</a></h2>\n<ul><li>Broker integration: <code>broker/src/integrationTest/resources/application-test.yml</code></li><li>Journey: <code>broker/src/systemTest/resources/application-system-test.yml</code></li><li>Module integration configs under each module's <code>src/integrationTest/resources/</code></li></ul>\n<p>Tests disable or delay pipe/compaction/reconciliation where isolation requires it.</p>\n<h2 id=\"pipe-consistency\" data-heading=\"Pipe Consistency\">Pipe Consistency<a class=\"heading-link\" href=\"#/chapter/11-runtime-config/pipe-consistency\" aria-label=\"Link to Pipe Consistency\">#</a></h2>\n<p><code>pipe.consistency.*</code> in <code>application.yml</code> (all env-overridable, see file for env names):</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Setting</th>\n<th>Default</th>\n<th>Meaning</th>\n</tr></thead><tbody>\n<tr>\n<td><code>enabled</code></td>\n<td><code>false</code></td>\n<td>fail-closed master switch; endpoints 404 and admin trigger 503 when off</td>\n</tr>\n<tr>\n<td><code>buckets</code></td>\n<td><code>64</code></td>\n<td>digest buckets; raise for multi-million-key topics</td>\n</tr>\n<tr>\n<td><code>scan-yield-every</code></td>\n<td><code>10000</code></td>\n<td>cooperative <code>Thread.yield()</code> cadence during index scans</td>\n</tr>\n<tr>\n<td><code>max-drilldown-buckets</code></td>\n<td><code>8</code></td>\n<td>above this, report divergence without key-level drill-down</td>\n</tr>\n<tr>\n<td><code>max-classify-entries</code></td>\n<td><code>1000</code></td>\n<td>per-check classification batch cap</td>\n</tr>\n<tr>\n<td><code>max-bucket-entries</code></td>\n<td><code>100000</code></td>\n<td>server-side cap per bucket response (HTTP 413 above)</td>\n</tr>\n<tr>\n<td><code>max-concurrent-scans</code></td>\n<td><code>2</code></td>\n<td>parent-side scan semaphore; excess children get 429</td>\n</tr>\n<tr>\n<td><code>schedule.enabled</code></td>\n<td><code>true</code></td>\n<td>auto-detection kill-switch (feature flag still gates everything)</td>\n</tr>\n<tr>\n<td><code>schedule.interval</code></td>\n<td><code>6h</code></td>\n<td>cadence of automatic all-topics checks</td>\n</tr>\n<tr>\n<td><code>schedule.initial-delay</code></td>\n<td><code>10m</code></td>\n<td>first automatic check after startup</td>\n</tr>\n<tr>\n<td><code>schedule.target</code></td>\n<td><code>parent</code></td>\n<td><code>parent</code> or <code>cloud</code></td>\n</tr>\n<tr>\n<td><code>escalation.enabled</code></td>\n<td><code>true</code></td>\n<td>clamp-persistence escalation to in-store verifiers</td>\n</tr>\n<tr>\n<td><code>escalation.after-clamped-checks</code></td>\n<td><code>2</code></td>\n<td>consecutive clamped checks before escalating</td>\n</tr>\n<tr>\n<td><code>escalation.max-candidates</code></td>\n<td><code>5</code></td>\n<td>head-probe budget per escalation</td>\n</tr>\n</tbody></table></div>","headings":[{"level":2,"title":"Ports And Identity","id":"ports-and-identity"},{"level":2,"title":"Storage","id":"storage"},{"level":2,"title":"Network And Delivery","id":"network-and-delivery"},{"level":2,"title":"Executors","id":"executors"},{"level":2,"title":"Topology And Pipe","id":"topology-and-pipe"},{"level":2,"title":"ACK Store","id":"ack-store"},{"level":2,"title":"Compaction","id":"compaction"},{"level":2,"title":"Legacy Service Topic Map","id":"legacy-service-topic-map"},{"level":2,"title":"Security And Authentication","id":"security-and-authentication"},{"level":2,"title":"Monitoring","id":"monitoring"},{"level":2,"title":"Consumer Runtime","id":"consumer-runtime"},{"level":2,"title":"Container","id":"container"},{"level":2,"title":"Static Analysis","id":"static-analysis"},{"level":2,"title":"Environment-Specific Test Config","id":"environment-specific-test-config"},{"level":2,"title":"Pipe Consistency","id":"pipe-consistency"}],"plainText":"Runtime Configuration Related: Overview, Module map, APIs, POS state, Risks. Primary broker config: broker/src/main/resources/application.yml. Consumer config: test-consumer/src/main/resources/application.yml. Container config: Dockerfile. Ports And Identity Setting Application default Docker default --- ---: ---: Broker TCP 19092 9092 Broker HTTP 8082 8081 Node ID local-001 broker-001 Environment variables make Docker values override YAML. Documentation and operator commands must not assume one set. Storage Type: filechannel Data dir: ${DATADIR:/Users/anuragmishra/Desktop/workspace/messaging/data/} Segment size: 100 MB Flush interval: 1 second Cache tail retained: 4 MB Code property paths use both data-dir and dataDir; Micronaut relaxed binding is expected, but ContainerResourceMonitor.java specifically injects broker.storage.dataDir. Tests comment on supporting both forms. Implementations are selected by broker.storage.type in FileChannelStorageEngine.java and MMapStorageEngine.java. Network And Delivery Netty boss threads: 1 Netty worker threads: 4 ACK timeout: 120 seconds Send timeout: base 1s + floor(batchMB)  2s Storage read timeout: 30 seconds Batch byte limit: 256 KB Adaptive poll: 200 ms to 5 seconds Fair scheduler: 2 threads, 1 in-flight task per topic Sources: application.yml, BatchDeliveryService.java, TopicFairScheduler.java. Executors Configured: ACK: 2 threads, queue 1000 ACK storage: 2 threads, queue 100 consumer scheduler: 2 threads storage: 2 threads, queue 100 Factory also creates compaction, registry, refresh, and flush executors with code defaults in broker/src/main/java/com/messaging/broker/config/ExecutorFactory.java. Topology And Pipe Registry URL: http://localhost:8080 Node ID sent as query parameter Pipe min/max interval: 500 ms / 20 seconds Pipe limit: 5 Topology registry poll interval: 30 seconds in TopologyManager.java Parent probe: /health, 2-second connect and 3-second request timeout in TopologyManager.java Persistent files: topology.properties, pipe-offset.properties. ACK Store Backend: RocksDB Live replay: enabled Startup seeding: enabled Path: ${DATADIR}/ack-store Shared block cache: 16 MB Reconciliation: every 5 minutes, first after 2 minutes Auto-sync: disabled Sources: application.yml, ACK package, SharedRocksDb.java. Compaction Backend: RocksDB index Enabled Tombstone retention: 7 days Window: 10 sealed segments Max topics/run: unlimited integer default Minimum sealed segments/topic: 1 CPU guard: 70% Heap guard: 70% Schedule: initial 5 minutes, then every 24 hours Sources: application.yml, CompactionScheduler.java. Legacy Service Topic Map Source: broker/src/main/resources/application.yml. Service key Topics --- --- price-quote prices-v1, reference-data-v5, non-promotable-products, prices-v4, minimum-price, deposit product-svc-lite product-base-document search-enterprise search-product tesco-location location, location-clusters customer-order-on-till selling-restrictions colleague-facts colleague-facts-jobs, colleague-facts-legacy loss-prevention-api four loss-prevention topics stored-value-services banned/active promotion topics colleague-identity colleague-card-pin distributed-identity colleague-card-pin-v2 dcxp-content dcxp-content restriction-service-on-tills restriction-rules dcxp-ugc dcxp-ugc Mismatch: test-consumer defaults legacy service name to price-quote-service, which is not a key in this map. Security And Authentication Broker YAML defines: Outbound filter requires: Source: common/src/main/java/com/messaging/common/http/AuthTokenClientFilter.java. No adapter between the keys was found, so setting only AUTHBEARERTOKEN from current YAML may not activate the filter. No inbound server filter or controller @Secured annotation was found. Management endpoint sensitivity is configured, but enforcement is not confirmed. Monitoring Prometheus, JVM, processor, uptime, file, and logback metrics are enabled. Health/prometheus/metrics are marked non-sensitive; other management endpoints default sensitive. Scheduled monitors: container resources: 10 seconds; memory: 10 seconds; storage segments: 15 seconds; threads: 30 seconds. Sources: monitoring package and application.yml. Consumer Runtime test-consumer defaults: HTTP 8081 modern topics from CONSUMERTOPICS/CONSUMERTOPIC group from CONSUMERGROUP broker host localhost, port 9092 legacy mode disabled legacy service default price-quote-service Source: test-consumer/src/main/resources/application.yml. Container Dockerfile: builds with Gradle 8.5/JDK 17; runs Eclipse Temurin 17 JRE; imports a repository CA certificate at build time; creates non-root broker; installs SQLite and curl; defaults heap to 512 MB initial / 2 GB max with G1; health-checks /health. The image builds :broker:build -x test, so image construction does not execute tests. Static Analysis qodana.yaml selects qodana.starter, JDK 21, and jetbrains/qodana-jvm:latest. No Qodana CLI was installed during this analysis and no CI file describing invocation was found. Environment-Specific Test Config Broker integration: broker/src/integrationTest/resources/application-test.yml Journey: broker/src/systemTest/resources/application-system-test.yml Module integration configs under each module's src/integrationTest/resources/ Tests disable or delay pipe/compaction/reconciliation where isolation requires it. Pipe Consistency pipe.consistency. in application.yml (all env-overridable, see file for env names): Setting Default Meaning --- ---: --- enabled false fail-closed master switch; endpoints 404 and admin trigger 503 when off buckets 64 digest buckets; raise for multi-million-key topics scan-yield-every 10000 cooperative Thread.yield() cadence during index scans max-drilldown-buckets 8 above this, report divergence without key-level drill-down max-classify-entries 1000 per-check classification batch cap max-bucket-entries 100000 server-side cap per bucket response (HTTP 413 above) max-concurrent-scans 2 parent-side scan semaphore; excess children get 429 schedule.enabled true auto-detection kill-switch (feature flag still gates everything) schedule.interval 6h cadence of automatic all-topics checks schedule.initial-delay 10m first automatic check after startup schedule.target parent parent or cloud escalation.enabled true clamp-persistence escalation to in-store verifiers escalation.after-clamped-checks 2 consecutive clamped checks before escalating escalation.max-candidates 5 head-probe budget per escalation","lineCount":206},{"order":12,"file":"12-debugging-guide.md","slug":"12-debugging-guide","title":"Debugging Guide","html":"<h1 id=\"debugging-guide\" data-heading=\"Debugging Guide\">Debugging Guide<a class=\"heading-link\" href=\"#/chapter/12-debugging-guide/debugging-guide\" aria-label=\"Link to Debugging Guide\">#</a></h1>\n<p>Related: <a href=\"#/chapter/00-index\">Index</a>, <a href=\"#/chapter/11-runtime-config\">Runtime config</a>, <a href=\"#/chapter/09-error-retry-recovery\">Errors/recovery</a>, <a href=\"#/chapter/10-test-map\">Tests</a>, <a href=\"#/chapter/13-risk-and-edge-cases\">Risks</a>.</p>\n<h2 id=\"broker-does-not-start\" data-heading=\"Broker Does Not Start\">Broker Does Not Start<a class=\"heading-link\" href=\"#/chapter/12-debugging-guide/broker-does-not-start\" aria-label=\"Link to Broker Does Not Start\">#</a></h2>\n<ol><li>Check storage recovery exceptions from <code>BrokerService.java</code>.</li><li>Inspect <code>&lt;dataDir&gt;/&lt;topic&gt;/partition-0</code> for mismatched <code>.log/.index</code> pairs and staging files.</li><li>Check TCP bind port from <code>broker.network.port</code>.</li><li>Check RocksDB lock/path messages; ACK seeding failure alone should not stop startup.</li><li>Confirm Java and Gradle compatibility: source target 17, current local runtime observed 21.</li></ol>\n<p>Relevant files: <code>BrokerService.java</code>, <code>DefaultStorageRecoveryService.java</code>, <code>NettyTcpServer.java</code>, <code>SharedRocksDb.java</code>.</p>\n<h2 id=\"consumer-receives-no-data\" data-heading=\"Consumer Receives No Data\">Consumer Receives No Data<a class=\"heading-link\" href=\"#/chapter/12-debugging-guide/consumer-receives-no-data\" aria-label=\"Link to Consumer Receives No Data\">#</a></h2>\n<p>Check in this order:</p>\n<ol><li>Registration exists in <code>ConsumerRegistry</code>.</li><li>READY_ACK opened readiness state.</li><li>Refresh is not <code>RESET_SENT</code>.</li><li>Storage head is at or above consumer offset.</li><li>No <code>DeliveryKey</code> is in-flight or waiting for ACK.</li><li>Failure backoff/max-failure gate is not active.</li><li>Topic fairness scheduler is still rescheduling.</li><li>Compaction filter did not advance an all-stale batch.</li></ol>\n<p>Files: <code>SubscribeHandler.java</code>, <code>ConsumerReadinessManager.java</code>, <code>WatermarkGatePolicy.java</code>, <code>RefreshGatePolicy.java</code>, <code>BatchDeliveryService.java</code>, <code>DeliveryScheduler.java</code>.</p>\n<p>Useful logs:</p>\n<ul><li><code>event=consumer.registered</code></li><li><code>event=ready_ack.processed</code></li><li><code>event=batch_delivery.blocked</code></li><li><code>event=batch_delivery.storage_read_slow</code></li><li><code>event=delivery_scheduler.reschedule_failed</code></li></ul>\n<h2 id=\"pending-ack-never-clears\" data-heading=\"Pending ACK Never Clears\">Pending ACK Never Clears<a class=\"heading-link\" href=\"#/chapter/12-debugging-guide/pending-ack-never-clears\" aria-label=\"Link to Pending ACK Never Clears\">#</a></h2>\n<p>Inspect:</p>\n<ul><li><code>group:topic</code> pending offset and send time in <code>InMemoryInFlightDeliveryStore</code>;</li><li>BATCH_ACK topic/group payload;</li><li>ACK executor rejection;</li><li>timeout scheduling generation;</li><li>disconnect cleanup.</li></ul>\n<p>Sources: broker <code>BatchAckHandler.java</code>, <code>BatchAckService.java</code>, <code>InMemoryInFlightDeliveryStore.java</code>, <code>ConsumerRegistry.unregisterConsumer</code>.</p>\n<p>Known signal: <code>ConsumerCrashDuringReplayJourneySpec</code> once logged pending ACK age above 25 seconds with a 5-second timeout; exact rerun passed.</p>\n<h2 id=\"refresh-is-stuck\" data-heading=\"Refresh Is Stuck\">Refresh Is Stuck<a class=\"heading-link\" href=\"#/chapter/12-debugging-guide/refresh-is-stuck\" aria-label=\"Link to Refresh Is Stuck\">#</a></h2>\n<p>Use:</p>\n<ul><li><code>GET /admin/refresh-status?topic=...</code></li><li><code>GET /admin/refresh-current</code></li><li>refresh metrics and logs.</li></ul>\n<p>By state:</p>\n<ul><li><code>RESET_SENT</code>: compare expected consumers and RESET ACK set; check client <code>onReset</code>, connection, and service mapping.</li><li><code>REPLAYING</code>: inspect <code>consumer-offsets.properties</code>, storage head, pending ACKs, and replay gap metric.</li><li><code>READY_SENT</code>: inspect READY ACK set and client <code>onReady</code>; retry occurs every 10 seconds.</li><li><code>ABORTED</code>: automatic operational recovery policy is not confirmed; inspect pipe/reconciliation state.</li></ul>\n<p>Files: <code>RefreshCoordinator.java</code>, phase services, <code>RefreshStateStore.java</code>.</p>\n<h2 id=\"pipe-is-not-advancing\" data-heading=\"Pipe Is Not Advancing\">Pipe Is Not Advancing<a class=\"heading-link\" href=\"#/chapter/12-debugging-guide/pipe-is-not-advancing\" aria-label=\"Link to Pipe Is Not Advancing\">#</a></h2>\n<ol><li>Confirm topology returned a parent.</li><li>Confirm parent <code>/health</code> returns success.</li><li>Inspect <code>pipe-offset.properties</code>.</li><li>Check <code>event=pipe_connector.connected</code>, paused/resumed logs, and poll failures.</li><li>Verify parent poll endpoint topic behavior.</li><li>Check duplicate guard against local topic head.</li><li>Check storage exception from <code>BrokerService.handlePipeMessage</code>.</li></ol>\n<p>Files: <code>TopologyManager.java</code>, <code>CloudRegistryClient.java</code>, <code>HttpPipeConnector.java</code>, <code>BrokerService.java</code>.</p>\n<h2 id=\"compaction-problems\" data-heading=\"Compaction Problems\">Compaction Problems<a class=\"heading-link\" href=\"#/chapter/12-debugging-guide/compaction-problems\" aria-label=\"Link to Compaction Problems\">#</a></h2>\n<p>Use <code>GET /admin/compaction/status</code>, logs, and segment directory.</p>\n<p>Check:</p>\n<ul><li>compaction enabled and schedule;</li><li>CPU/heap guard skipped run;</li><li>sealed segment count/minimum;</li><li>checkpoint;</li><li><code>.compacting.*</code> leftovers;</li><li>matching <code>.compacted.log/.index</code>;</li><li><code>replaceSegments</code> errors;</li><li>delivery pending ACKs/file-region failures during replacement.</li></ul>\n<p>Files: <code>CompactionScheduler.java</code>, <code>CompactionPlanner.java</code>, <code>CompactionRewriter.java</code>, <code>SegmentManager.java</code>.</p>\n<p>Do not call <code>StorageEngine.compact()</code> expecting broker compaction; current engine implementations leave it as a placeholder.</p>\n<h2 id=\"legacy-client-does-not-register\" data-heading=\"Legacy Client Does Not Register\">Legacy Client Does Not Register<a class=\"heading-link\" href=\"#/chapter/12-debugging-guide/legacy-client-does-not-register\" aria-label=\"Link to Legacy Client Does Not Register\">#</a></h2>\n<ol><li>First byte must be legacy REGISTER ordinal <code>0</code>.</li><li>Inspect <code>Detected protocol: LEGACY</code>.</li><li>Compare exact service name with <code>legacy-clients.service-topics</code>.</li><li>Confirm legacy clients are enabled.</li><li>Check <code>Unknown legacy service</code> logs.</li></ol>\n<p>Files: <code>DefaultProtocolDetectionService.java</code>, <code>LegacyEventDecoder.java</code>, <code>SubscribeHandler.java</code>, <code>LegacyClientConfig.java</code>, <code>application.yml</code>.</p>\n<p>Current confirmed mismatch: <code>price-quote-service</code> versus configured <code>price-quote</code>.</p>\n<h2 id=\"offset-problems\" data-heading=\"Offset Problems\">Offset Problems<a class=\"heading-link\" href=\"#/chapter/12-debugging-guide/offset-problems\" aria-label=\"Link to Offset Problems\">#</a></h2>\n<p>Identify semantics first:</p>\n<ul><li>modern: next offset to deliver;</li><li>legacy: last acknowledged offset;</li><li>storage head: last stored offset;</li><li>pipe: last successful parent offset.</li></ul>\n<p>Inspect <code>consumer-offsets.properties</code>, then trace writes in <code>BatchAckService</code>, <code>LegacyConsumerDeliveryManager</code>, <code>CommitOffsetHandler</code>, and refresh reset.</p>\n<p>For gaps after compaction, verify reads seek first offset greater than or equal to requested in <code>Segment.java</code>.</p>\n<h2 id=\"resource-pressure\" data-heading=\"Resource Pressure\">Resource Pressure<a class=\"heading-link\" href=\"#/chapter/12-debugging-guide/resource-pressure\" aria-label=\"Link to Resource Pressure\">#</a></h2>\n<p>HTTP diagnostics:</p>\n<ul><li><code>/diagnostics/threads/top-cpu</code></li><li><code>/diagnostics/threads/top-memory</code></li><li><code>/diagnostics/threads/by-category</code></li><li><code>/diagnostics/threads/deadlocks</code></li></ul>\n<p>Metrics/logs:</p>\n<ul><li>storage slow-read warnings;</li><li>executor queue descriptions;</li><li>compaction CPU/heap skips;</li><li>pending ACK age;</li><li>pipe streaming failures;</li><li>page-cache advisory logs.</li></ul>\n<p>Files: monitoring package, <code>ExecutorFactory.java</code>, <code>FileChannelStorageEngine.java</code>.</p>\n<h2 id=\"test-reproduction\" data-heading=\"Test Reproduction\">Test Reproduction<a class=\"heading-link\" href=\"#/chapter/12-debugging-guide/test-reproduction\" aria-label=\"Link to Test Reproduction\">#</a></h2>\n<div class=\"code-block\"><div class=\"code-toolbar\"><span>bash</span><button class=\"copy-code\" type=\"button\">Copy</button></div><pre><code>./gradlew test\n./gradlew :broker:integrationTest --tests com.messaging.broker.core.BrokerLegacyConsumerIntegrationSpec --rerun-tasks\n./gradlew :broker:journeyTest --tests com.messaging.broker.systemtest.journey.ConsumerCrashDuringReplayJourneySpec --rerun-tasks\n./gradlew :broker:systemTest</code></pre></div>\n<p>Reports are written under <code>&lt;module&gt;/build/reports/tests/&lt;task&gt;/index.html</code>.</p>\n<h2 id=\"safe-change-checklist\" data-heading=\"Safe Change Checklist\">Safe Change Checklist<a class=\"heading-link\" href=\"#/chapter/12-debugging-guide/safe-change-checklist\" aria-label=\"Link to Safe Change Checklist\">#</a></h2>\n<p>Before changing delivery/storage/refresh/compaction:</p>\n<ul><li>Memory: preserve streaming/chunking and batch limits.</li><li>CPU: avoid per-record global scans or busy polling.</li><li>IO: preserve group commit, atomic files, and bounded rewrites.</li><li>Network: preserve header/payload ordering and one-topic/group connection assumption.</li><li>Latency: preserve scheduler fairness and timeout ownership.</li><li>Existing flow: test modern, legacy, pipe, restart, refresh, and compaction intersections.</li><li>Compatibility: preserve legacy event ordinals and modern message codes.</li><li>Compaction: preserve original offsets and staging publication.</li><li>POS state: preserve RESET/READY ACK gates and reconnect cleanup.</li></ul>","headings":[{"level":2,"title":"Broker Does Not Start","id":"broker-does-not-start"},{"level":2,"title":"Consumer Receives No Data","id":"consumer-receives-no-data"},{"level":2,"title":"Pending ACK Never Clears","id":"pending-ack-never-clears"},{"level":2,"title":"Refresh Is Stuck","id":"refresh-is-stuck"},{"level":2,"title":"Pipe Is Not Advancing","id":"pipe-is-not-advancing"},{"level":2,"title":"Compaction Problems","id":"compaction-problems"},{"level":2,"title":"Legacy Client Does Not Register","id":"legacy-client-does-not-register"},{"level":2,"title":"Offset Problems","id":"offset-problems"},{"level":2,"title":"Resource Pressure","id":"resource-pressure"},{"level":2,"title":"Test Reproduction","id":"test-reproduction"},{"level":2,"title":"Safe Change Checklist","id":"safe-change-checklist"}],"plainText":"Debugging Guide Related: Index, Runtime config, Errors/recovery, Tests, Risks. Broker Does Not Start Check storage recovery exceptions from BrokerService.java. Inspect <dataDir>/<topic>/partition-0 for mismatched .log/.index pairs and staging files. Check TCP bind port from broker.network.port. Check RocksDB lock/path messages; ACK seeding failure alone should not stop startup. Confirm Java and Gradle compatibility: source target 17, current local runtime observed 21. Relevant files: BrokerService.java, DefaultStorageRecoveryService.java, NettyTcpServer.java, SharedRocksDb.java. Consumer Receives No Data Check in this order: Registration exists in ConsumerRegistry. READYACK opened readiness state. Refresh is not RESETSENT. Storage head is at or above consumer offset. No DeliveryKey is in-flight or waiting for ACK. Failure backoff/max-failure gate is not active. Topic fairness scheduler is still rescheduling. Compaction filter did not advance an all-stale batch. Files: SubscribeHandler.java, ConsumerReadinessManager.java, WatermarkGatePolicy.java, RefreshGatePolicy.java, BatchDeliveryService.java, DeliveryScheduler.java. Useful logs: event=consumer.registered event=readyack.processed event=batchdelivery.blocked event=batchdelivery.storagereadslow event=deliveryscheduler.reschedulefailed Pending ACK Never Clears Inspect: group:topic pending offset and send time in InMemoryInFlightDeliveryStore; BATCHACK topic/group payload; ACK executor rejection; timeout scheduling generation; disconnect cleanup. Sources: broker BatchAckHandler.java, BatchAckService.java, InMemoryInFlightDeliveryStore.java, ConsumerRegistry.unregisterConsumer. Known signal: ConsumerCrashDuringReplayJourneySpec once logged pending ACK age above 25 seconds with a 5-second timeout; exact rerun passed. Refresh Is Stuck Use: GET /admin/refresh-status?topic=... GET /admin/refresh-current refresh metrics and logs. By state: RESETSENT: compare expected consumers and RESET ACK set; check client onReset, connection, and service mapping. REPLAYING: inspect consumer-offsets.properties, storage head, pending ACKs, and replay gap metric. READYSENT: inspect READY ACK set and client onReady; retry occurs every 10 seconds. ABORTED: automatic operational recovery policy is not confirmed; inspect pipe/reconciliation state. Files: RefreshCoordinator.java, phase services, RefreshStateStore.java. Pipe Is Not Advancing Confirm topology returned a parent. Confirm parent /health returns success. Inspect pipe-offset.properties. Check event=pipeconnector.connected, paused/resumed logs, and poll failures. Verify parent poll endpoint topic behavior. Check duplicate guard against local topic head. Check storage exception from BrokerService.handlePipeMessage. Files: TopologyManager.java, CloudRegistryClient.java, HttpPipeConnector.java, BrokerService.java. Compaction Problems Use GET /admin/compaction/status, logs, and segment directory. Check: compaction enabled and schedule; CPU/heap guard skipped run; sealed segment count/minimum; checkpoint; .compacting. leftovers; matching .compacted.log/.index; replaceSegments errors; delivery pending ACKs/file-region failures during replacement. Files: CompactionScheduler.java, CompactionPlanner.java, CompactionRewriter.java, SegmentManager.java. Do not call StorageEngine.compact() expecting broker compaction; current engine implementations leave it as a placeholder. Legacy Client Does Not Register First byte must be legacy REGISTER ordinal 0. Inspect Detected protocol: LEGACY. Compare exact service name with legacy-clients.service-topics. Confirm legacy clients are enabled. Check Unknown legacy service logs. Files: DefaultProtocolDetectionService.java, LegacyEventDecoder.java, SubscribeHandler.java, LegacyClientConfig.java, application.yml. Current confirmed mismatch: price-quote-service versus configured price-quote. Offset Problems Identify semantics first: modern: next offset to deliver; legacy: last acknowledged offset; storage head: last stored offset; pipe: last successful parent offset. Inspect consumer-offsets.properties, then trace writes in BatchAckService, LegacyConsumerDeliveryManager, CommitOffsetHandler, and refresh reset. For gaps after compaction, verify reads seek first offset greater than or equal to requested in Segment.java. Resource Pressure HTTP diagnostics: /diagnostics/threads/top-cpu /diagnostics/threads/top-memory /diagnostics/threads/by-category /diagnostics/threads/deadlocks Metrics/logs: storage slow-read warnings; executor queue descriptions; compaction CPU/heap skips; pending ACK age; pipe streaming failures; page-cache advisory logs. Files: monitoring package, ExecutorFactory.java, FileChannelStorageEngine.java. Test Reproduction Reports are written under <module>/build/reports/tests/<task>/index.html. Safe Change Checklist Before changing delivery/storage/refresh/compaction: Memory: preserve streaming/chunking and batch limits. CPU: avoid per-record global scans or busy polling. IO: preserve group commit, atomic files, and bounded rewrites. Network: preserve header/payload ordering and one-topic/group connection assumption. Latency: preserve scheduler fairness and timeout ownership. Existing flow: test modern, legacy, pipe, restart, refresh, and compaction intersections. Compatibility: preserve legacy event ordinals and modern message codes. Compaction: preserve original offsets and staging publication. POS state: preserve RESET/READY ACK gates and reconnect cleanup.","lineCount":169},{"order":13,"file":"13-risk-and-edge-cases.md","slug":"13-risk-and-edge-cases","title":"Risk And Edge Cases","html":"<h1 id=\"risk-and-edge-cases\" data-heading=\"Risk And Edge Cases\">Risk And Edge Cases<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/risk-and-edge-cases\" aria-label=\"Link to Risk And Edge Cases\">#</a></h1>\n<p>Related: <a href=\"#/chapter/10-test-map\">Tests</a>, <a href=\"#/chapter/11-runtime-config\">Runtime config</a>, <a href=\"#/chapter/12-debugging-guide\">Debugging</a>, <a href=\"#/chapter/15-open-questions\">Open questions</a>.</p>\n<h2 id=\"criticalhigh\" data-heading=\"Critical/High\">Critical/High<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/criticalhigh\" aria-label=\"Link to Critical/High\">#</a></h2>\n<h3 id=\"security\" data-heading=\"Security\">Security<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/security\" aria-label=\"Link to Security\">#</a></h3>\n<ul><li>No inbound HTTP authentication implementation was found for admin, diagnostics, compaction, refresh, or <code>/test</code> APIs.</li><li><code>TestDataController</code> accepts a filesystem SQLite path and concatenates <code>tableName</code> into SQL.</li><li>Outbound auth config names do not match: YAML <code>broker.registry.auth.bearer-token</code>, filter <code>broker.http.auth.token</code>.</li></ul>\n<p>Sources: controllers, <code>AuthTokenClientFilter.java</code>, <code>application.yml</code>.</p>\n<p>Impact: unauthorized refresh/compaction, local file access, SQL identifier injection, operational data exposure.</p>\n<h3 id=\"legacy-service-name-contract\" data-heading=\"Legacy Service Name Contract\">Legacy Service Name Contract<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/legacy-service-name-contract\" aria-label=\"Link to Legacy Service Name Contract\">#</a></h3>\n<p>RESOLVED 2026-06-11: broker specs now use <code>price-quote</code> matching the broker YAML and the deployed consumer fleet; <code>:broker:integrationTest</code> and <code>:broker:journeyTest</code> pass. The underlying rule stands: the <code>legacy-clients.service-topics</code> key must exactly equal the serviceName legacy clients send in RegisterEvent — renaming either side silently disables registration (client connects but never receives topics).</p>\n<h3 id=\"compaction-selecting-the-active-segment-after-restart-fixed-2026-06-12\" data-heading=\"Compaction Selecting The Active Segment After Restart (FIXED 2026-06-12)\">Compaction Selecting The Active Segment After Restart (FIXED 2026-06-12)<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/compaction-selecting-the-active-segment-after-restart-fixed-2026-06-12\" aria-label=\"Link to Compaction Selecting The Active Segment After Restart (FIXED 2026-06-12)\">#</a></h3>\n<p>Recovery used to leave the re-activated last segment in BOTH the sealed-segments map and <code>activeSegment</code>. The first compaction run after a restart (5m initial delay) selected the LIVE write target into its rewrite window, sealed and deleted it, and every append for the topic failed forever with <code>Segment is not active</code> — which also stalled the entire pipe stream (single global cursor stops at the first failed record). Fixed three ways: recovery removes the active segment from the map (invariant: active never in map), <code>getInactiveSegments()</code> filters the active segment defensively, and <code>replaceSegments()</code> refuses the active segment exactly like <code>removeSegments()</code> always did. Regression spec: <code>SegmentManagerSpec.&quot;restart never exposes the re-activated segment to compaction&quot;</code>. Operational signature when running an unfixed build: repeating <code>CRITICAL: Failed to store pipe message at offset N ... Segment is not active</code> with the same offset, starting minutes after a broker restart.</p>\n<h3 id=\"segment-lifecycle-audit-2026-06-12-five-more-state-machine-bugs-fixed\" data-heading=\"Segment Lifecycle Audit (2026-06-12) — five more state-machine bugs FIXED\">Segment Lifecycle Audit (2026-06-12) — five more state-machine bugs FIXED<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/segment-lifecycle-audit-2026-06-12-five-more-state-machine-bugs-fixed\" aria-label=\"Link to Segment Lifecycle Audit (2026-06-12) — five more state-machine bugs FIXED\">#</a></h3>\n<p>Found by a systematic walk of every active/sealed/compact/replace/recover transition after the active-segment-compaction bug:</p>\n<ol><li><strong>Same-base re-compaction data loss (CRITICAL):</strong> an unadvanced checkpoint (unexpired</li></ol>\n<p>tombstones) re-selects the already-compacted segment; the rewrite ATOMIC_MOVEs onto the SAME <code>.compacted.log</code> path, and cleanup then unlinked the just-installed file and erased its metadata row. Fixed: <code>cleanupDetachedSegment(deleteMetadata, deleteFiles)</code> skips exactly the artifacts the replacement shares (base ⇒ metadata, path ⇒ files).</p>\n<ol><li><strong>Empty force-roll same-file twin (HIGH):</strong> the <code>size==0</code> skip never fired (fresh</li></ol>\n<p>segments are 6 header bytes), so force-rolling an idle topic sealed the active segment into the map AND reopened the SAME file as the new active — compacting the twin unlinked the live file. Fixed: skip by <code>recordCount==0</code> + a hard same-base/empty guard in <code>rollSegment</code>.</p>\n<ol><li><strong>In-flight zero-copy vs close (HIGH operational):</strong> compaction closed the segment's</li></ol>\n<p>channel while Netty was still sendfile-ing from it (read lock covers batch construction only) → <code>ClosedChannelException</code>, consumer connection resets every compaction. Fixed: batch leases — <code>Segment.close()</code> defers the channel close until the last <code>BatchFileRegion.close()</code> (Netty always deallocates); unlink-while-open is POSIX-safe.</p>\n<ol><li><strong>Recovery double-load (MEDIUM):</strong> with both <code>X.log</code> and <code>X.compacted.log</code> on disk</li></ol>\n<p>(crash between finalise and cleanup), both were loaded and the second silently shadowed the first in the map, leaking channels. Fixed: dedupe by base preferring <code>.compacted.log</code> with a loud warning.</p>\n<ol><li><strong>Lock-discipline + JMM hygiene (LOW):</strong> <code>forceRollActiveSegment</code> rolled without the</li></ol>\n<p>segment write lock (readers could observe half-rolled state) — the lock now lives inside <code>rollSegment</code> (reentrant); <code>Segment.nextOffset/logPosition/recordCount/active</code> are now volatile (they are read lock-free by delivery threads).</p>\n<p>Regression specs: <code>SegmentManagerSpec</code> (restart/compaction exposure, empty force-roll, same-base replacement), <code>SegmentSpec</code> (in-flight transfer survives close), <code>DefaultStorageRecoveryServiceSpec</code> (duplicate-base dedupe).</p>\n<h3 id=\"full-concurrency-sweep-2026-06-12-mode-combination-matrix\" data-heading=\"Full Concurrency Sweep (2026-06-12) — mode-combination matrix\">Full Concurrency Sweep (2026-06-12) — mode-combination matrix<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/full-concurrency-sweep-2026-06-12-mode-combination-matrix\" aria-label=\"Link to Full Concurrency Sweep (2026-06-12) — mode-combination matrix\">#</a></h3>\n<p>Systematic pass over normal × refresh × compaction × consistency interactions, after the segment-lifecycle fixes. Verified safe by design (with the load-bearing mechanism):</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Combination</th>\n<th>Why safe</th>\n</tr></thead><tbody>\n<tr>\n<td>consistency scan × concurrent ingest</td>\n<td>RocksDB iterators are snapshot views; child digests BEFORE calling parent so races degrade to benign drill-down classifications</td>\n</tr>\n<tr>\n<td>consistency × compaction (either side)</td>\n<td>digest input (key→latestOffset) is compaction-invariant; classify reads go through the segment read lock + batch leases</td>\n</tr>\n<tr>\n<td>consistency × refresh</td>\n<td>disjoint state — consistency reads the compaction index only; refresh wipes ACK store and pauses reconciliation, neither used by checks</td>\n</tr>\n<tr>\n<td>consistency × consistency (many children → one parent)</td>\n<td>server-side scan semaphore (429 above 2), single-flight on each child</td>\n</tr>\n<tr>\n<td>compaction × manual trigger × consistency check</td>\n<td>all three serialize on the single-threaded <code>compactionExecutor</code></td>\n</tr>\n<tr>\n<td>refresh replay × compaction swap</td>\n<td>replay reads under the segment read lock; in-flight zero-copy protected by batch leases</td>\n</tr>\n<tr>\n<td>pipe ingest × producer ingest same topic</td>\n<td><code>CompactionIndex.updateLock</code> serializes read-modify-write; appendLock serializes segment writes</td>\n</tr>\n<tr>\n<td>shutdown × periodic flush</td>\n<td><code>flush()</code> and <code>close()</code> synchronize on the Segment; seal flips <code>active</code> before channel close so a racing flush no-ops</td>\n</tr>\n<tr>\n<td>delivery × ACK × timeout</td>\n<td>generation-gated <code>InMemoryInFlightDeliveryStore</code> (striped locks; claims validate generation AND pending offset) — hardened earlier, re-verified</td>\n</tr>\n</tbody></table></div>\n<p><strong>Fixed this sweep:</strong> <code>CompactionScheduler.compact()</code> and <code>AckReconciliationScheduler</code> ran their minutes-long bodies INLINE on Micronaut's shared 4-thread scheduled pool — the same pool as the 1-second segment-fsync flusher. On a small POS (pool = 2×cores) a compaction sweep plus a first-run reconciliation could starve the flusher, silently growing the power-cut data-loss window from 1s to minutes. Both now offload to the single-threaded <code>compactionExecutor</code> (which also serializes them with consistency checks).</p>\n<p>Known residual (accepted, documented): a parent-side compaction racing a <code>classify</code> physical-presence read can transiently answer &quot;absent&quot; → one-off zombie false positive; the next scheduled check self-corrects. Legacy delivery reads index FILES directly and can lose a topic for one merged batch during a compaction swap (retries next poll).</p>\n<h3 id=\"compaction-index-is-derived-and-has-no-rebuild\" data-heading=\"Compaction Index Is Derived And Has No Rebuild\">Compaction Index Is Derived And Has No Rebuild<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/compaction-index-is-derived-and-has-no-rebuild\" aria-label=\"Link to Compaction Index Is Derived And Has No Rebuild\">#</a></h3>\n<p>The pipe-consistency audit (and delivery-time superseded filtering) trust the RocksDB compaction index. There is no rebuild-from-segments mechanism: a wiped or corrupted index with intact segments makes the node deliver superseded records and report falsely inconsistent against any peer. Failure mode is loud, not silent. Follow-up: an offline rebuild/spot-check admin tool reusing <code>CompactionSegmentReader</code>.</p>\n<p>Segment↔index divergence windows on the pipe ingest path (<code>handlePipeMessage</code> writes segments then the index then returns true → pipe offset advances) — two of three closed:</p>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Window</th>\n<th>Status</th>\n</tr></thead><tbody>\n<tr>\n<td><code>RocksDbCompactionIndex.updateKey</code> swallowed <code>RocksDBException</code> → ingest &quot;succeeded&quot;, record in segments but never indexed</td>\n<td>CLOSED — updateKey throws a structured <code>StorageException</code> (<code>STORAGE_METADATA_ERROR</code>) via <code>ExceptionLogger.logAndThrow</code>; ingest returns false, pipe retries the record (<code>CompactionIndex.updateKey</code> now declares <code>throws StorageException</code>; implementations MUST throw on backend failure)</td>\n</tr>\n<tr>\n<td>Crash (or lost RocksDB WAL tail on power cut) between <code>append</code> and <code>updateKey</code> → parent re-sends, dedupe branch skipped the index write forever</td>\n<td>CLOSED — the duplicate-skip branch now heals the index (<code>updateKey</code> is idempotent/monotone, no-op for genuine duplicates)</td>\n</tr>\n<tr>\n<td>At-rest RocksDB loss/corruption with intact segments</td>\n<td>OPEN — fails loud via the consistency check; rebuild tool backlogged (above)</td>\n</tr>\n</tbody></table></div>\n<p>Producer <code>DataHandler</code> path with throwing updateKey is safe: no ACK → producer retries → the retried append gets a new offset that supersedes the orphan.</p>\n<h3 id=\"pos-refresh-and-reconnect\" data-heading=\"POS Refresh And Reconnect\">POS Refresh And Reconnect<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/pos-refresh-and-reconnect\" aria-label=\"Link to POS Refresh And Reconnect\">#</a></h3>\n<p>One full journey run retained a reconnected group's pending ACK beyond timeout and never reached READY; isolated rerun passed.</p>\n<p>Sources: <code>ConsumerCrashDuringReplayJourneySpec.groovy</code>, delivery state classes.</p>\n<p>Impact: pipe remains paused and all refresh participants wait.</p>\n<h3 id=\"compaction\" data-heading=\"Compaction\">Compaction<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/compaction\" aria-label=\"Link to Compaction\">#</a></h3>\n<ul><li>Scheduled compaction can run during long replay/refresh.</li><li>Manual force-roll can race pipe append.</li><li>Rewriter can rewrite large unique-key windows for zero reclaimed bytes.</li><li>Segment replacement interacts with in-flight file regions.</li><li>All-records-deleted windows create large offset gaps.</li></ul>\n<p>Sources: compaction package, <code>CompactionController.java</code>, <code>SegmentManager.java</code>, <code>BUGS.md</code>.</p>\n<h2 id=\"medium\" data-heading=\"Medium\">Medium<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/medium\" aria-label=\"Link to Medium\">#</a></h2>\n<h3 id=\"pipe-and-topology\" data-heading=\"Pipe And Topology\">Pipe And Topology<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/pipe-and-topology\" aria-label=\"Link to Pipe And Topology\">#</a></h3>\n<ul><li>Connector omits <code>topic</code>; local PipeServer defaults <code>price-topic</code>.</li><li>Only the first parent is selected.</li><li>Node ID query URL encoding was not observed.</li><li><code>PipeMessageForwarder.getCurrentOffset()</code> returns <code>0</code>.</li><li>Probe requires <code>/health</code>; journey mock returned 404 and retained old/no parent.</li></ul>\n<p>Sources: <code>HttpPipeConnector.java</code>, <code>PipeServer.java</code>, <code>TopologyManager.java</code>, <code>PipeMessageForwarder.java</code>.</p>\n<h3 id=\"offset-semantics\" data-heading=\"Offset Semantics\">Offset Semantics<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/offset-semantics\" aria-label=\"Link to Offset Semantics\">#</a></h3>\n<ul><li>Modern offsets are next-to-deliver; legacy offsets are last-acknowledged.</li><li><code>allConsumersCaughtUp</code> reads one property representation for both.</li><li><code>CommitOffsetHandler</code> clamps to storage head, not head plus one.</li><li>Registration clamps a corrupt value above head+1 back to head, intentionally allowing replay.</li></ul>\n<p>Sources: <code>BatchDeliveryService.java</code>, <code>LegacyConsumerDeliveryManager.java</code>, <code>ConsumerRegistry.java</code>, <code>CommitOffsetHandler.java</code>, <code>ConsumerRegistrationManager.java</code>.</p>\n<p>Impact: off-by-one mistakes can skip or redeliver records.</p>\n<h3 id=\"storage-integrity\" data-heading=\"Storage Integrity\">Storage Integrity<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/storage-integrity\" aria-label=\"Link to Storage Integrity\">#</a></h3>\n<ul><li>Current record format has no per-record CRC.</li><li>Non-monotonic externally supplied offsets log a warning but append.</li><li><code>StorageEngine.compact()</code> and <code>validateStorage()</code> are placeholders.</li><li>Real filesystem atomicity/power-loss behavior is not verified.</li></ul>\n<p>Sources: storage segment and engine classes.</p>\n<h3 id=\"shutdown\" data-heading=\"Shutdown\">Shutdown<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/shutdown\" aria-label=\"Link to Shutdown\">#</a></h3>\n<p>Integration logs showed a second <code>NettyTcpServer.shutdown</code> call during bean disposal after explicit broker shutdown, causing <code>RejectedExecutionException</code>.</p>\n<p>Impact: noisy shutdown and possible incomplete cleanup in other failure orderings.</p>\n<h3 id=\"delivery-filter-fail-open\" data-heading=\"Delivery Filter Fail-Open\">Delivery Filter Fail-Open<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/delivery-filter-fail-open\" aria-label=\"Link to Delivery Filter Fail-Open\">#</a></h3>\n<p>Compaction filtering returns the unfiltered batch on decode/index errors.</p>\n<p>Impact: stale keyed values may be delivered temporarily.</p>\n<h3 id=\"http-error-semantics\" data-heading=\"HTTP Error Semantics\">HTTP Error Semantics<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/http-error-semantics\" aria-label=\"Link to HTTP Error Semantics\">#</a></h3>\n<p>Most controllers return error maps with HTTP 200 rather than typed 4xx/5xx responses.</p>\n<p>Impact: automation must inspect response body and may treat failures as success.</p>\n<h2 id=\"loweroperational\" data-heading=\"Lower/Operational\">Lower/Operational<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/loweroperational\" aria-label=\"Link to Lower/Operational\">#</a></h2>\n<ul><li><code>GET /admin/refresh-current</code> reports only one arbitrary active topic.</li><li><code>GET /test/stats</code> is placeholder data.</li><li>Client invokes all handlers for a topic, including multiple groups, for each decoded connection.</li><li>READY retry stops after three attempts; registration remains but delivery stays blocked.</li><li>Reconciliation scans active registered groups only.</li><li>Properties files rewrite the full map; large group/topic counts increase IO.</li><li>Metrics cleanup keys may differ from ephemeral client IDs; code attempts group/topic cleanup.</li><li>Gradle uses deprecated features incompatible with Gradle 9.</li><li>Qodana requests JDK 21 while project source target is 17.</li><li>Docker build skips tests.</li></ul>\n<h2 id=\"performance-guardrail-matrix\" data-heading=\"Performance Guardrail Matrix\">Performance Guardrail Matrix<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/performance-guardrail-matrix\" aria-label=\"Link to Performance Guardrail Matrix\">#</a></h2>\n<div class=\"table-wrap\"><table><thead><tr>\n<th>Area</th>\n<th>Existing guard</th>\n<th>Residual risk</th>\n</tr></thead><tbody>\n<tr>\n<td>Memory</td>\n<td>256 KB batch default, streaming pipe, chunked ACK writes, streaming compaction</td>\n<td>Heap replacement batches and decoded compaction strings</td>\n</tr>\n<tr>\n<td>CPU</td>\n<td>adaptive polling, fairness, compaction CPU threshold</td>\n<td>Large topic count and full compaction scans</td>\n</tr>\n<tr>\n<td>IO</td>\n<td>1s group commit, page-cache advice, atomic properties, bounded windows</td>\n<td>No-op rewrites and SQLite/property rewrites</td>\n</tr>\n<tr>\n<td>Network</td>\n<td>send timeout, channel writability, one connection/topic/group</td>\n<td>Many groups create many sockets</td>\n</tr>\n<tr>\n<td>Latency</td>\n<td>storage-read timeout, ACK timeout, scheduler backoff</td>\n<td>Refresh and compaction lock interactions</td>\n</tr>\n<tr>\n<td>Compatibility</td>\n<td>legacy adapter, old index recovery</td>\n<td>Service-name and offset-semantic drift</td>\n</tr>\n<tr>\n<td>Compaction safety</td>\n<td>staging/atomic move, offsets preserved, delivery filter</td>\n<td>In-flight region lifetime and all-deleted ranges</td>\n</tr>\n<tr>\n<td>POS safety</td>\n<td>READY/RESET gates, persisted refresh, reconnect</td>\n<td>Flaky stale pending ACK and abort policy</td>\n</tr>\n</tbody></table></div>\n<h2 id=\"change-hotspots\" data-heading=\"Change Hotspots\">Change Hotspots<a class=\"heading-link\" href=\"#/chapter/13-risk-and-edge-cases/change-hotspots\" aria-label=\"Link to Change Hotspots\">#</a></h2>\n<p>Changes in these files require broad tests:</p>\n<ul><li><code>Segment.java</code>, <code>SegmentManager.java</code></li><li><code>NettyTcpServer.java</code>, <code>ZeroCopyBatchDecoder.java</code></li><li><code>BatchDeliveryService.java</code>, <code>BatchAckService.java</code></li><li><code>ConsumerRegistry.java</code>, <code>InMemoryInFlightDeliveryStore.java</code></li><li><code>RefreshCoordinator.java</code>, phase services</li><li><code>CompactionRewriter.java</code>, <code>CompactionScheduler.java</code></li><li><code>HttpPipeConnector.java</code>, <code>TopologyManager.java</code></li><li><code>ClientConsumerManager.java</code></li></ul>\n<p>Use the feature-to-test links in <a href=\"#/chapter/10-test-map\">Test map</a>.</p>","headings":[{"level":2,"title":"Critical/High","id":"criticalhigh"},{"level":3,"title":"Security","id":"security"},{"level":3,"title":"Legacy Service Name Contract","id":"legacy-service-name-contract"},{"level":3,"title":"Compaction Selecting The Active Segment After Restart (FIXED 2026-06-12)","id":"compaction-selecting-the-active-segment-after-restart-fixed-2026-06-12"},{"level":3,"title":"Segment Lifecycle Audit (2026-06-12) — five more state-machine bugs FIXED","id":"segment-lifecycle-audit-2026-06-12-five-more-state-machine-bugs-fixed"},{"level":3,"title":"Full Concurrency Sweep (2026-06-12) — mode-combination matrix","id":"full-concurrency-sweep-2026-06-12-mode-combination-matrix"},{"level":3,"title":"Compaction Index Is Derived And Has No Rebuild","id":"compaction-index-is-derived-and-has-no-rebuild"},{"level":3,"title":"POS Refresh And Reconnect","id":"pos-refresh-and-reconnect"},{"level":3,"title":"Compaction","id":"compaction"},{"level":2,"title":"Medium","id":"medium"},{"level":3,"title":"Pipe And Topology","id":"pipe-and-topology"},{"level":3,"title":"Offset Semantics","id":"offset-semantics"},{"level":3,"title":"Storage Integrity","id":"storage-integrity"},{"level":3,"title":"Shutdown","id":"shutdown"},{"level":3,"title":"Delivery Filter Fail-Open","id":"delivery-filter-fail-open"},{"level":3,"title":"HTTP Error Semantics","id":"http-error-semantics"},{"level":2,"title":"Lower/Operational","id":"loweroperational"},{"level":2,"title":"Performance Guardrail Matrix","id":"performance-guardrail-matrix"},{"level":2,"title":"Change Hotspots","id":"change-hotspots"}],"plainText":"Risk And Edge Cases Related: Tests, Runtime config, Debugging, Open questions. Critical/High Security No inbound HTTP authentication implementation was found for admin, diagnostics, compaction, refresh, or /test APIs. TestDataController accepts a filesystem SQLite path and concatenates tableName into SQL. Outbound auth config names do not match: YAML broker.registry.auth.bearer-token, filter broker.http.auth.token. Sources: controllers, AuthTokenClientFilter.java, application.yml. Impact: unauthorized refresh/compaction, local file access, SQL identifier injection, operational data exposure. Legacy Service Name Contract RESOLVED 2026-06-11: broker specs now use price-quote matching the broker YAML and the deployed consumer fleet; :broker:integrationTest and :broker:journeyTest pass. The underlying rule stands: the legacy-clients.service-topics key must exactly equal the serviceName legacy clients send in RegisterEvent — renaming either side silently disables registration (client connects but never receives topics). Compaction Selecting The Active Segment After Restart (FIXED 2026-06-12) Recovery used to leave the re-activated last segment in BOTH the sealed-segments map and activeSegment. The first compaction run after a restart (5m initial delay) selected the LIVE write target into its rewrite window, sealed and deleted it, and every append for the topic failed forever with Segment is not active — which also stalled the entire pipe stream (single global cursor stops at the first failed record). Fixed three ways: recovery removes the active segment from the map (invariant: active never in map), getInactiveSegments() filters the active segment defensively, and replaceSegments() refuses the active segment exactly like removeSegments() always did. Regression spec: SegmentManagerSpec.\"restart never exposes the re-activated segment to compaction\". Operational signature when running an unfixed build: repeating CRITICAL: Failed to store pipe message at offset N ... Segment is not active with the same offset, starting minutes after a broker restart. Segment Lifecycle Audit (2026-06-12) — five more state-machine bugs FIXED Found by a systematic walk of every active/sealed/compact/replace/recover transition after the active-segment-compaction bug: Same-base re-compaction data loss (CRITICAL): an unadvanced checkpoint (unexpired tombstones) re-selects the already-compacted segment; the rewrite ATOMICMOVEs onto the SAME .compacted.log path, and cleanup then unlinked the just-installed file and erased its metadata row. Fixed: cleanupDetachedSegment(deleteMetadata, deleteFiles) skips exactly the artifacts the replacement shares (base ⇒ metadata, path ⇒ files). Empty force-roll same-file twin (HIGH): the size==0 skip never fired (fresh segments are 6 header bytes), so force-rolling an idle topic sealed the active segment into the map AND reopened the SAME file as the new active — compacting the twin unlinked the live file. Fixed: skip by recordCount==0 + a hard same-base/empty guard in rollSegment. In-flight zero-copy vs close (HIGH operational): compaction closed the segment's channel while Netty was still sendfile-ing from it (read lock covers batch construction only) → ClosedChannelException, consumer connection resets every compaction. Fixed: batch leases — Segment.close() defers the channel close until the last BatchFileRegion.close() (Netty always deallocates); unlink-while-open is POSIX-safe. Recovery double-load (MEDIUM): with both X.log and X.compacted.log on disk (crash between finalise and cleanup), both were loaded and the second silently shadowed the first in the map, leaking channels. Fixed: dedupe by base preferring .compacted.log with a loud warning. Lock-discipline + JMM hygiene (LOW): forceRollActiveSegment rolled without the segment write lock (readers could observe half-rolled state) — the lock now lives inside rollSegment (reentrant); Segment.nextOffset/logPosition/recordCount/active are now volatile (they are read lock-free by delivery threads). Regression specs: SegmentManagerSpec (restart/compaction exposure, empty force-roll, same-base replacement), SegmentSpec (in-flight transfer survives close), DefaultStorageRecoveryServiceSpec (duplicate-base dedupe). Full Concurrency Sweep (2026-06-12) — mode-combination matrix Systematic pass over normal × refresh × compaction × consistency interactions, after the segment-lifecycle fixes. Verified safe by design (with the load-bearing mechanism): Combination Why safe --- --- consistency scan × concurrent ingest RocksDB iterators are snapshot views; child digests BEFORE calling parent so races degrade to benign drill-down classifications consistency × compaction (either side) digest input (key→latestOffset) is compaction-invariant; classify reads go through the segment read lock + batch leases consistency × refresh disjoint state — consistency reads the compaction index only; refresh wipes ACK store and pauses reconciliation, neither used by checks consistency × consistency (many children → one parent) server-side scan semaphore (429 above 2), single-flight on each child compaction × manual trigger × consistency check all three serialize on the single-threaded compactionExecutor refresh replay × compaction swap replay reads under the segment read lock; in-flight zero-copy protected by batch leases pipe ingest × producer ingest same topic CompactionIndex.updateLock serializes read-modify-write; appendLock serializes segment writes shutdown × periodic flush flush() and close() synchronize on the Segment; seal flips active before channel close so a racing flush no-ops delivery × ACK × timeout generation-gated InMemoryInFlightDeliveryStore (striped locks; claims validate generation AND pending offset) — hardened earlier, re-verified Fixed this sweep: CompactionScheduler.compact() and AckReconciliationScheduler ran their minutes-long bodies INLINE on Micronaut's shared 4-thread scheduled pool — the same pool as the 1-second segment-fsync flusher. On a small POS (pool = 2×cores) a compaction sweep plus a first-run reconciliation could starve the flusher, silently growing the power-cut data-loss window from 1s to minutes. Both now offload to the single-threaded compactionExecutor (which also serializes them with consistency checks). Known residual (accepted, documented): a parent-side compaction racing a classify physical-presence read can transiently answer \"absent\" → one-off zombie false positive; the next scheduled check self-corrects. Legacy delivery reads index FILES directly and can lose a topic for one merged batch during a compaction swap (retries next poll). Compaction Index Is Derived And Has No Rebuild The pipe-consistency audit (and delivery-time superseded filtering) trust the RocksDB compaction index. There is no rebuild-from-segments mechanism: a wiped or corrupted index with intact segments makes the node deliver superseded records and report falsely inconsistent against any peer. Failure mode is loud, not silent. Follow-up: an offline rebuild/spot-check admin tool reusing CompactionSegmentReader. Segment↔index divergence windows on the pipe ingest path (handlePipeMessage writes segments then the index then returns true → pipe offset advances) — two of three closed: Window Status --- --- RocksDbCompactionIndex.updateKey swallowed RocksDBException → ingest \"succeeded\", record in segments but never indexed CLOSED — updateKey throws a structured StorageException (STORAGEMETADATAERROR) via ExceptionLogger.logAndThrow; ingest returns false, pipe retries the record (CompactionIndex.updateKey now declares throws StorageException; implementations MUST throw on backend failure) Crash (or lost RocksDB WAL tail on power cut) between append and updateKey → parent re-sends, dedupe branch skipped the index write forever CLOSED — the duplicate-skip branch now heals the index (updateKey is idempotent/monotone, no-op for genuine duplicates) At-rest RocksDB loss/corruption with intact segments OPEN — fails loud via the consistency check; rebuild tool backlogged (above) Producer DataHandler path with throwing updateKey is safe: no ACK → producer retries → the retried append gets a new offset that supersedes the orphan. POS Refresh And Reconnect One full journey run retained a reconnected group's pending ACK beyond timeout and never reached READY; isolated rerun passed. Sources: ConsumerCrashDuringReplayJourneySpec.groovy, delivery state classes. Impact: pipe remains paused and all refresh participants wait. Compaction Scheduled compaction can run during long replay/refresh. Manual force-roll can race pipe append. Rewriter can rewrite large unique-key windows for zero reclaimed bytes. Segment replacement interacts with in-flight file regions. All-records-deleted windows create large offset gaps. Sources: compaction package, CompactionController.java, SegmentManager.java, BUGS.md. Medium Pipe And Topology Connector omits topic; local PipeServer defaults price-topic. Only the first parent is selected. Node ID query URL encoding was not observed. PipeMessageForwarder.getCurrentOffset() returns 0. Probe requires /health; journey mock returned 404 and retained old/no parent. Sources: HttpPipeConnector.java, PipeServer.java, TopologyManager.java, PipeMessageForwarder.java. Offset Semantics Modern offsets are next-to-deliver; legacy offsets are last-acknowledged. allConsumersCaughtUp reads one property representation for both. CommitOffsetHandler clamps to storage head, not head plus one. Registration clamps a corrupt value above head+1 back to head, intentionally allowing replay. Sources: BatchDeliveryService.java, LegacyConsumerDeliveryManager.java, ConsumerRegistry.java, CommitOffsetHandler.java, ConsumerRegistrationManager.java. Impact: off-by-one mistakes can skip or redeliver records. Storage Integrity Current record format has no per-record CRC. Non-monotonic externally supplied offsets log a warning but append. StorageEngine.compact() and validateStorage() are placeholders. Real filesystem atomicity/power-loss behavior is not verified. Sources: storage segment and engine classes. Shutdown Integration logs showed a second NettyTcpServer.shutdown call during bean disposal after explicit broker shutdown, causing RejectedExecutionException. Impact: noisy shutdown and possible incomplete cleanup in other failure orderings. Delivery Filter Fail-Open Compaction filtering returns the unfiltered batch on decode/index errors. Impact: stale keyed values may be delivered temporarily. HTTP Error Semantics Most controllers return error maps with HTTP 200 rather than typed 4xx/5xx responses. Impact: automation must inspect response body and may treat failures as success. Lower/Operational GET /admin/refresh-current reports only one arbitrary active topic. GET /test/stats is placeholder data. Client invokes all handlers for a topic, including multiple groups, for each decoded connection. READY retry stops after three attempts; registration remains but delivery stays blocked. Reconciliation scans active registered groups only. Properties files rewrite the full map; large group/topic counts increase IO. Metrics cleanup keys may differ from ephemeral client IDs; code attempts group/topic cleanup. Gradle uses deprecated features incompatible with Gradle 9. Qodana requests JDK 21 while project source target is 17. Docker build skips tests. Performance Guardrail Matrix Area Existing guard Residual risk --- --- --- Memory 256 KB batch default, streaming pipe, chunked ACK writes, streaming compaction Heap replacement batches and decoded compaction strings CPU adaptive polling, fairness, compaction CPU threshold Large topic count and full compaction scans IO 1s group commit, page-cache advice, atomic properties, bounded windows No-op rewrites and SQLite/property rewrites Network send timeout, channel writability, one connection/topic/group Many groups create many sockets Latency storage-read timeout, ACK timeout, scheduler backoff Refresh and compaction lock interactions Compatibility legacy adapter, old index recovery Service-name and offset-semantic drift Compaction safety staging/atomic move, offsets preserved, delivery filter In-flight region lifetime and all-deleted ranges POS safety READY/RESET gates, persisted refresh, reconnect Flaky stale pending ACK and abort policy Change Hotspots Changes in these files require broad tests: Segment.java, SegmentManager.java NettyTcpServer.java, ZeroCopyBatchDecoder.java BatchDeliveryService.java, BatchAckService.java ConsumerRegistry.java, InMemoryInFlightDeliveryStore.java RefreshCoordinator.java, phase services CompactionRewriter.java, CompactionScheduler.java HttpPipeConnector.java, TopologyManager.java ClientConsumerManager.java Use the feature-to-test links in Test map.","lineCount":230},{"order":14,"file":"14-glossary.md","slug":"14-glossary","title":"Glossary","html":"<h1 id=\"glossary\" data-heading=\"Glossary\">Glossary<a class=\"heading-link\" href=\"#/chapter/14-glossary/glossary\" aria-label=\"Link to Glossary\">#</a></h1>\n<p>Related: <a href=\"#/chapter/01-system-overview\">Overview</a>, <a href=\"#/chapter/05-data-model\">Data model</a>, <a href=\"#/chapter/06-event-kafka-flow\">Events</a>, <a href=\"#/chapter/07-pos-machine-state\">POS state</a>, <a href=\"#/chapter/08-compaction-handling\">Compaction</a>.</p>\n<ul><li><strong>ACK store:</strong> Per-record consumption audit stored in RocksDB, separate from the committed consumer offset.</li><li><strong>Active segment:</strong> Writable current segment for a topic/partition.</li><li><strong>Batch ACK:</strong> Consumer confirmation that a delivered batch was decoded/processed.</li><li><strong>Compaction checkpoint:</strong> Highest sealed range already processed for a topic.</li><li><strong>Compaction index:</strong> Latest known offset/timestamp per topic/message key plus stale-offset watermark.</li><li><strong>Consumer group:</strong> Logical independent progress owner for a topic.</li><li><strong>ConsumerKey:</strong> <code>clientId + topic + group</code>; identifies one live socket registration.</li><li><strong>DeliveryBatch:</strong> Transfer object exposing record count, byte count, offsets, and <code>transferTo</code>.</li><li><strong>DeliveryKey:</strong> <code>group + topic</code>; identifies logical in-flight and committed delivery state.</li><li><strong>File region:</strong> Netty transfer object used to send segment bytes without copying through a broker heap buffer.</li><li><strong>Group commit:</strong> Periodic force of active storage rather than force per append.</li><li><strong>Journey test:</strong> In-process end-to-end broker/cloud/consumer test from <code>broker/src/systemTest</code>, run by <code>journeyTest</code>.</li><li><strong>Legacy client:</strong> Event-protocol consumer that registers a service name and may subscribe to multiple mapped topics over one connection.</li><li><strong>Modern client:</strong> BrokerMessage-protocol consumer, normally one TCP connection per topic/group.</li><li><strong>Next-to-deliver offset:</strong> Modern offset value immediately after the last acknowledged original batch offset.</li><li><strong>Parent:</strong> Upstream broker/cloud HTTP endpoint selected by topology.</li><li><strong>Partition:</strong> Storage subdivision. Production code in this repository uses partition <code>0</code>.</li><li><strong>Pipe:</strong> HTTP polling replication link from parent to local broker.</li><li><strong>POS:</strong> Point-of-sale/store terminal context. Represented through consumer connection/readiness/refresh state, not a dedicated entity.</li><li><strong>READY:</strong> Broker control event opening normal delivery or ending refresh.</li><li><strong>RESET:</strong> Broker control event asking a consumer to clear/reinitialize its local view before replay.</li><li><strong>Sealed segment:</strong> Read-only segment eligible for compaction.</li><li><strong>Sparse offsets:</strong> Stored offsets with gaps, expected after pipe replication or compaction.</li><li><strong>Stale record:</strong> Earlier topic/key version superseded by a later offset.</li><li><strong>System test:</strong> Separate-process black-box test from <code>broker/src/blackboxSystemTest</code>.</li><li><strong>Tombstone:</strong> Latest keyed DELETE record retained temporarily to propagate deletion.</li><li><strong>Topic fair scheduler:</strong> Scheduler limiting concurrent delivery work per topic and pending retry per delivery key.</li><li><strong>Watermark:</strong> Latest known storage offset used to avoid no-data delivery reads.</li><li><strong>Zero-copy:</strong> Broker transfer of file-backed segment bytes through Netty <code>FileRegion</code>; client still decodes records into objects.</li></ul>\n<p>Source definitions are primarily in <code>common/src/main/java/com/messaging/common/model/</code>, <code>broker/src/main/java/com/messaging/broker/model/</code>, storage segment classes, and consumer/compaction packages.</p>","headings":[],"plainText":"Glossary Related: Overview, Data model, Events, POS state, Compaction. ACK store: Per-record consumption audit stored in RocksDB, separate from the committed consumer offset. Active segment: Writable current segment for a topic/partition. Batch ACK: Consumer confirmation that a delivered batch was decoded/processed. Compaction checkpoint: Highest sealed range already processed for a topic. Compaction index: Latest known offset/timestamp per topic/message key plus stale-offset watermark. Consumer group: Logical independent progress owner for a topic. ConsumerKey: clientId + topic + group; identifies one live socket registration. DeliveryBatch: Transfer object exposing record count, byte count, offsets, and transferTo. DeliveryKey: group + topic; identifies logical in-flight and committed delivery state. File region: Netty transfer object used to send segment bytes without copying through a broker heap buffer. Group commit: Periodic force of active storage rather than force per append. Journey test: In-process end-to-end broker/cloud/consumer test from broker/src/systemTest, run by journeyTest. Legacy client: Event-protocol consumer that registers a service name and may subscribe to multiple mapped topics over one connection. Modern client: BrokerMessage-protocol consumer, normally one TCP connection per topic/group. Next-to-deliver offset: Modern offset value immediately after the last acknowledged original batch offset. Parent: Upstream broker/cloud HTTP endpoint selected by topology. Partition: Storage subdivision. Production code in this repository uses partition 0. Pipe: HTTP polling replication link from parent to local broker. POS: Point-of-sale/store terminal context. Represented through consumer connection/readiness/refresh state, not a dedicated entity. READY: Broker control event opening normal delivery or ending refresh. RESET: Broker control event asking a consumer to clear/reinitialize its local view before replay. Sealed segment: Read-only segment eligible for compaction. Sparse offsets: Stored offsets with gaps, expected after pipe replication or compaction. Stale record: Earlier topic/key version superseded by a later offset. System test: Separate-process black-box test from broker/src/blackboxSystemTest. Tombstone: Latest keyed DELETE record retained temporarily to propagate deletion. Topic fair scheduler: Scheduler limiting concurrent delivery work per topic and pending retry per delivery key. Watermark: Latest known storage offset used to avoid no-data delivery reads. Zero-copy: Broker transfer of file-backed segment bytes through Netty FileRegion; client still decodes records into objects. Source definitions are primarily in common/src/main/java/com/messaging/common/model/, broker/src/main/java/com/messaging/broker/model/, storage segment classes, and consumer/compaction packages.","lineCount":36},{"order":15,"file":"15-open-questions.md","slug":"15-open-questions","title":"Open Questions","html":"<h1 id=\"open-questions\" data-heading=\"Open Questions\">Open Questions<a class=\"heading-link\" href=\"#/chapter/15-open-questions/open-questions\" aria-label=\"Link to Open Questions\">#</a></h1>\n<p>Related: <a href=\"#/chapter/13-risk-and-edge-cases\">Risks</a>, <a href=\"#/chapter/10-test-map\">Tests</a>, <a href=\"#/chapter/11-runtime-config\">Runtime config</a>, <a href=\"#/chapter/12-debugging-guide\">Debugging</a>.</p>\n<p>Items below could not be confirmed from code or need an owner decision.</p>\n<h2 id=\"protocol-and-integration\" data-heading=\"Protocol And Integration\">Protocol And Integration<a class=\"heading-link\" href=\"#/chapter/15-open-questions/protocol-and-integration\" aria-label=\"Link to Protocol And Integration\">#</a></h2>\n<ol><li>What is the canonical legacy service name: <code>price-quote</code> or <code>price-quote-service</code>?<ul><li>Sources: <code>broker/src/main/resources/application.yml</code>, <code>test-consumer/src/main/resources/application.yml</code>, <code>BrokerLegacyConsumerIntegrationSpec.groovy</code>.</li></ul></li><li>How does the external parent select topics when <code>HttpPipeConnector</code> does not send <code>topic</code>?<ul><li>Sources: <code>HttpPipeConnector.java</code>, <code>PipeServer.java</code>.</li></ul></li><li>Is multi-parent failover required, or is selecting the first parent intentional?<ul><li>Source: <code>TopologyManager.java</code>.</li></ul></li><li>Is <code>HEARTBEAT</code> planned or obsolete? It has a message code but no registered handler.</li><li>Is legacy producer ingestion supported? <code>LegacyEventDecoder</code> emits pipe-separated DATA payloads, while <code>DataHandler</code> expects JSON. Not confirmed from an end-to-end test.</li></ol>\n<h2 id=\"security\" data-heading=\"Security\">Security<a class=\"heading-link\" href=\"#/chapter/15-open-questions/security\" aria-label=\"Link to Security\">#</a></h2>\n<ol><li>What component enforces authentication for inbound admin/diagnostic/test HTTP endpoints?</li><li>Should <code>AUTH_BEARER_TOKEN</code> map to <code>broker.http.auth.token</code> instead of the currently documented registry key?</li><li>Should <code>/test/**</code> be disabled outside test/development environments?</li><li>Is arbitrary SQLite file loading an intended production capability?</li></ol>\n<h2 id=\"offset-and-state-semantics\" data-heading=\"Offset And State Semantics\">Offset And State Semantics<a class=\"heading-link\" href=\"#/chapter/15-open-questions/offset-and-state-semantics\" aria-label=\"Link to Offset And State Semantics\">#</a></h2>\n<ol><li>Should all persisted consumer offsets be standardized as next-to-deliver, including legacy?</li><li>Should <code>CommitOffsetHandler</code> accept <code>storageHead + 1</code> for a caught-up modern consumer?</li><li>Does <code>DeliveryStateStore</code> participate in active remote delivery recovery, or is it retained legacy infrastructure? Its direct integration into <code>BatchDeliveryService</code> was not confirmed.</li><li>Should refresh reset to storage earliest offset rather than literal <code>0</code> after retention/compaction?</li><li>Should reconciliation include disconnected historical groups?</li></ol>\n<h2 id=\"refreshpos\" data-heading=\"Refresh/POS\">Refresh/POS<a class=\"heading-link\" href=\"#/chapter/15-open-questions/refreshpos\" aria-label=\"Link to Refresh/POS\">#</a></h2>\n<ol><li>After an abort, who resumes the pipe and ACK reconciliation, and under which conditions?</li><li>What is the expected operator workflow for persisted <code>ABORTED</code> contexts?</li><li>Should compaction pause during refresh or initial replay?</li><li>What caused the aggregate-only stale pending ACK in <code>ConsumerCrashDuringReplayJourneySpec</code>?</li><li>Should a refresh with no registered consumers pause/clear any local POS state, or is immediate <code>COMPLETED</code> correct?</li></ol>\n<h2 id=\"storage-and-compaction\" data-heading=\"Storage And Compaction\">Storage And Compaction<a class=\"heading-link\" href=\"#/chapter/15-open-questions/storage-and-compaction\" aria-label=\"Link to Storage And Compaction\">#</a></h2>\n<ol><li>Is the <code>MMapStorageEngine</code> name intentional even though the shared segment implementation uses <code>FileChannel</code>?</li><li>Should non-monotonic external offsets be rejected rather than appended?</li><li>Is per-record checksum validation required for POS power-loss/corruption detection?</li><li>What are the intended implementations of <code>StorageEngine.compact()</code> and <code>validateStorage()</code>?</li><li>Should no-op compaction windows be detected before rewriting?</li><li>What should <code>getEarliestOffset</code> return when only an active segment exists with a nonzero base offset?</li><li>Is active/recovered segment double-close possible or explicitly idempotent? Not confirmed from tests.</li><li>What general retention policy exists for non-superseded messages? Not confirmed from code.</li></ol>\n<h2 id=\"runtime-and-build\" data-heading=\"Runtime And Build\">Runtime And Build<a class=\"heading-link\" href=\"#/chapter/15-open-questions/runtime-and-build\" aria-label=\"Link to Runtime And Build\">#</a></h2>\n<ol><li>Which port defaults are canonical: YAML <code>19092/8082</code> or Docker <code>9092/8081</code>?</li><li>Where is Qodana executed, and is JDK 21 intentional for a Java 17 target?</li><li>Should Docker image builds run tests rather than <code>-x test</code>?</li><li>Should integration, journey, and system tests be dependencies of a CI aggregate task?</li><li>Should <code>storage/src/test/java/com/messaging/storage/mmap/MMapStorageEngineTest.java</code> be moved into the custom unit source set or deleted?</li><li>Are Gradle 9 deprecation warnings tracked?</li></ol>\n<h2 id=\"documentation-maintenance\" data-heading=\"Documentation Maintenance\">Documentation Maintenance<a class=\"heading-link\" href=\"#/chapter/15-open-questions/documentation-maintenance\" aria-label=\"Link to Documentation Maintenance\">#</a></h2>\n<p>Update this book when any of these change:</p>\n<ul><li>protocol message codes or record binary format;</li><li>service/topic mappings;</li><li>offset semantics;</li><li>refresh transitions/timeouts;</li><li>storage file/schema format;</li><li>compaction retention/checkpoint behavior;</li><li>test source-set wiring;</li><li>runtime ports/authentication.</li></ul>\n<p>The starting point for updates is <a href=\"#/chapter/00-index\">the index</a>.</p>","headings":[{"level":2,"title":"Protocol And Integration","id":"protocol-and-integration"},{"level":2,"title":"Security","id":"security"},{"level":2,"title":"Offset And State Semantics","id":"offset-and-state-semantics"},{"level":2,"title":"Refresh/POS","id":"refreshpos"},{"level":2,"title":"Storage And Compaction","id":"storage-and-compaction"},{"level":2,"title":"Runtime And Build","id":"runtime-and-build"},{"level":2,"title":"Documentation Maintenance","id":"documentation-maintenance"}],"plainText":"Open Questions Related: Risks, Tests, Runtime config, Debugging. Items below could not be confirmed from code or need an owner decision. Protocol And Integration What is the canonical legacy service name: price-quote or price-quote-service? Sources: broker/src/main/resources/application.yml, test-consumer/src/main/resources/application.yml, BrokerLegacyConsumerIntegrationSpec.groovy. How does the external parent select topics when HttpPipeConnector does not send topic? Sources: HttpPipeConnector.java, PipeServer.java. Is multi-parent failover required, or is selecting the first parent intentional? Source: TopologyManager.java. Is HEARTBEAT planned or obsolete? It has a message code but no registered handler. Is legacy producer ingestion supported? LegacyEventDecoder emits pipe-separated DATA payloads, while DataHandler expects JSON. Not confirmed from an end-to-end test. Security What component enforces authentication for inbound admin/diagnostic/test HTTP endpoints? Should AUTHBEARERTOKEN map to broker.http.auth.token instead of the currently documented registry key? Should /test/ be disabled outside test/development environments? Is arbitrary SQLite file loading an intended production capability? Offset And State Semantics Should all persisted consumer offsets be standardized as next-to-deliver, including legacy? Should CommitOffsetHandler accept storageHead + 1 for a caught-up modern consumer? Does DeliveryStateStore participate in active remote delivery recovery, or is it retained legacy infrastructure? Its direct integration into BatchDeliveryService was not confirmed. Should refresh reset to storage earliest offset rather than literal 0 after retention/compaction? Should reconciliation include disconnected historical groups? Refresh/POS After an abort, who resumes the pipe and ACK reconciliation, and under which conditions? What is the expected operator workflow for persisted ABORTED contexts? Should compaction pause during refresh or initial replay? What caused the aggregate-only stale pending ACK in ConsumerCrashDuringReplayJourneySpec? Should a refresh with no registered consumers pause/clear any local POS state, or is immediate COMPLETED correct? Storage And Compaction Is the MMapStorageEngine name intentional even though the shared segment implementation uses FileChannel? Should non-monotonic external offsets be rejected rather than appended? Is per-record checksum validation required for POS power-loss/corruption detection? What are the intended implementations of StorageEngine.compact() and validateStorage()? Should no-op compaction windows be detected before rewriting? What should getEarliestOffset return when only an active segment exists with a nonzero base offset? Is active/recovered segment double-close possible or explicitly idempotent? Not confirmed from tests. What general retention policy exists for non-superseded messages? Not confirmed from code. Runtime And Build Which port defaults are canonical: YAML 19092/8082 or Docker 9092/8081? Where is Qodana executed, and is JDK 21 intentional for a Java 17 target? Should Docker image builds run tests rather than -x test? Should integration, journey, and system tests be dependencies of a CI aggregate task? Should storage/src/test/java/com/messaging/storage/mmap/MMapStorageEngineTest.java be moved into the custom unit source set or deleted? Are Gradle 9 deprecation warnings tracked? Documentation Maintenance Update this book when any of these change: protocol message codes or record binary format; service/topic mappings; offset semantics; refresh transitions/timeouts; storage file/schema format; compaction retention/checkpoint behavior; test source-set wiring; runtime ports/authentication. The starting point for updates is the index.","lineCount":75}],"workflows":[{"slug":"normal-data-transfer","title":"Normal Data Transfer","shortTitle":"Data Transfer","category":"End-to-end data plane","summary":"A producer record is validated, appended to local storage, delivered to a ready POS consumer, and committed only after the consumer ACKs the batch.","scenario":"A producer sends a new price for key product-42 on topic prices-v1, and consumer group till-price-service is connected and ready.","steps":[{"title":"Producer sends DATA","detail":"The modern TCP envelope carries JSON with topic, msg_key, event_type, and data. BinaryMessageDecoder creates the broker message.","signal":"DATA"},{"title":"Broker validates and appends","detail":"DataHandler requires the topic, creates a MessageRecord, and appends it to partition 0 through StorageEngine.","signal":"append(topic, 0, record)"},{"title":"Latest-key index advances","detail":"CompactionIndex.updateKey records the latest offset for product-42 so older versions can be filtered immediately.","signal":"key → latestOffset"},{"title":"Ready consumer is scheduled","detail":"Delivery gates check startup readiness, refresh state, storage watermark, in-flight state, and pending ACK state.","signal":"DeliveryScheduler"},{"title":"Batch is sent","detail":"BatchDeliveryService sends BATCH_HEADER followed by raw segment bytes, normally using zero-copy FileRegion transfer.","signal":"BATCH_HEADER + bytes"},{"title":"Consumer processes and ACKs","detail":"ZeroCopyBatchDecoder emits records to the client handlers. A complete decode produces BATCH_ACK(topic, group).","signal":"BATCH_ACK"},{"title":"Progress becomes durable","detail":"BatchAckService commits the next delivery offset and asynchronously writes per-record ACK audit entries to RocksDB.","signal":"nextOffset persisted"}],"outcome":"The producer receives its append ACK independently of consumer processing. The consumer group advances only after its batch ACK, so disconnect before ACK causes replay.","safeguards":["No delivery before READY_ACK.","One pending delivery generation per topic/group.","Partial client batch decode closes the channel without ACK.","Compaction filtering removes known stale versions before send."],"sources":["broker/src/main/java/com/messaging/broker/handler/DataHandler.java","broker/src/main/java/com/messaging/broker/consumer/DeliveryScheduler.java","broker/src/main/java/com/messaging/broker/consumer/BatchDeliveryService.java","network/src/main/java/com/messaging/network/codec/ZeroCopyBatchDecoder.java","broker/src/main/java/com/messaging/broker/consumer/BatchAckService.java"],"related":[{"label":"Producer flow","route":"#/chapter/06-event-kafka-flow/producer-flow"},{"label":"Modern delivery flow","route":"#/chapter/06-event-kafka-flow/modern-delivery-flow"},{"label":"Data model","route":"#/chapter/05-data-model"}],"searchText":"Normal Data Transfer End-to-end data plane A producer record is validated, appended to local storage, delivered to a ready POS consumer, and committed only after the consumer ACKs the batch. A producer sends a new price for key product-42 on topic prices-v1, and consumer group till-price-service is connected and ready. The producer receives its append ACK independently of consumer processing. The consumer group advances only after its batch ACK, so disconnect before ACK causes replay. Producer sends DATA The modern TCP envelope carries JSON with topic, msg_key, event_type, and data. BinaryMessageDecoder creates the broker message. DATA Broker validates and appends DataHandler requires the topic, creates a MessageRecord, and appends it to partition 0 through StorageEngine. append(topic, 0, record) Latest-key index advances CompactionIndex.updateKey records the latest offset for product-42 so older versions can be filtered immediately. key → latestOffset Ready consumer is scheduled Delivery gates check startup readiness, refresh state, storage watermark, in-flight state, and pending ACK state. DeliveryScheduler Batch is sent BatchDeliveryService sends BATCH_HEADER followed by raw segment bytes, normally using zero-copy FileRegion transfer. BATCH_HEADER + bytes Consumer processes and ACKs ZeroCopyBatchDecoder emits records to the client handlers. A complete decode produces BATCH_ACK(topic, group). BATCH_ACK Progress becomes durable BatchAckService commits the next delivery offset and asynchronously writes per-record ACK audit entries to RocksDB. nextOffset persisted No delivery before READY_ACK. One pending delivery generation per topic/group. Partial client batch decode closes the channel without ACK. Compaction filtering removes known stale versions before send. broker/src/main/java/com/messaging/broker/handler/DataHandler.java broker/src/main/java/com/messaging/broker/consumer/DeliveryScheduler.java broker/src/main/java/com/messaging/broker/consumer/BatchDeliveryService.java network/src/main/java/com/messaging/network/codec/ZeroCopyBatchDecoder.java broker/src/main/java/com/messaging/broker/consumer/BatchAckService.java"},{"slug":"modern-consumer","title":"Modern Consumer Flow","shortTitle":"Modern Consumer","category":"TCP consumer lifecycle","summary":"A modern consumer registers one topic/group connection, passes the READY handshake, receives zero-copy batches, and reconnects from its durable offset.","scenario":"The till-price-service group starts for prices-v1 after the broker already contains records.","steps":[{"title":"Client discovers handlers","detail":"ClientConsumerManager finds MessageHandler beans and creates one connection for each configured topic/group.","signal":"topic:group"},{"title":"Client subscribes","detail":"SUBSCRIBE carries topic and group. SubscribeHandler restores the persisted group:topic offset and registers the socket.","signal":"SUBSCRIBE"},{"title":"Startup readiness handshake","detail":"The broker ACKs subscription, sends READY(topic), and waits until the client calls onReady and returns structured READY_ACK.","signal":"READY ↔ READY_ACK"},{"title":"Delivery gates open","detail":"The scheduler reads from the restored offset only when readiness, refresh, watermark, fairness, and pending-delivery gates allow it.","signal":"ready = true"},{"title":"Client receives a batch","detail":"The broker sends metadata plus raw record bytes. The decoder invokes every matching topic handler with ConsumerRecord values.","signal":"BATCH_HEADER + bytes"},{"title":"ACK commits the next offset","detail":"After full processing, BATCH_ACK lets the broker persist the next-to-deliver offset for that group and topic.","signal":"offset = last + 1"},{"title":"Reconnect resumes","detail":"A dead channel triggers reconnect backoff from 5 to 60 seconds. Stale socket state is cleared while the durable offset remains.","signal":"5s → 60s backoff"}],"outcome":"Each modern group progresses independently. Reconnect replays only work after the last persisted next-to-deliver offset.","safeguards":["READY is retried up to three times.","Client IDs may change on reconnect; durable state is keyed by group/topic.","ACK timeout rolls back pending delivery state for replay.","Refresh late joiners follow refresh-specific RESET/READY handling."],"sources":["client/src/main/java/com/messaging/client/ClientConsumerManager.java","broker/src/main/java/com/messaging/broker/handler/SubscribeHandler.java","broker/src/main/java/com/messaging/broker/consumer/ConsumerReadinessManager.java","broker/src/main/java/com/messaging/broker/consumer/BatchDeliveryService.java"],"related":[{"label":"Modern subscription","route":"#/chapter/06-event-kafka-flow/modern-subscription-flow"},{"label":"Modern delivery","route":"#/chapter/06-event-kafka-flow/modern-delivery-flow"},{"label":"POS startup state","route":"#/chapter/07-pos-machine-state/startup-readiness"}],"searchText":"Modern Consumer Flow TCP consumer lifecycle A modern consumer registers one topic/group connection, passes the READY handshake, receives zero-copy batches, and reconnects from its durable offset. The till-price-service group starts for prices-v1 after the broker already contains records. Each modern group progresses independently. Reconnect replays only work after the last persisted next-to-deliver offset. Client discovers handlers ClientConsumerManager finds MessageHandler beans and creates one connection for each configured topic/group. topic:group Client subscribes SUBSCRIBE carries topic and group. SubscribeHandler restores the persisted group:topic offset and registers the socket. SUBSCRIBE Startup readiness handshake The broker ACKs subscription, sends READY(topic), and waits until the client calls onReady and returns structured READY_ACK. READY ↔ READY_ACK Delivery gates open The scheduler reads from the restored offset only when readiness, refresh, watermark, fairness, and pending-delivery gates allow it. ready = true Client receives a batch The broker sends metadata plus raw record bytes. The decoder invokes every matching topic handler with ConsumerRecord values. BATCH_HEADER + bytes ACK commits the next offset After full processing, BATCH_ACK lets the broker persist the next-to-deliver offset for that group and topic. offset = last + 1 Reconnect resumes A dead channel triggers reconnect backoff from 5 to 60 seconds. Stale socket state is cleared while the durable offset remains. 5s → 60s backoff READY is retried up to three times. Client IDs may change on reconnect; durable state is keyed by group/topic. ACK timeout rolls back pending delivery state for replay. Refresh late joiners follow refresh-specific RESET/READY handling. client/src/main/java/com/messaging/client/ClientConsumerManager.java broker/src/main/java/com/messaging/broker/handler/SubscribeHandler.java broker/src/main/java/com/messaging/broker/consumer/ConsumerReadinessManager.java broker/src/main/java/com/messaging/broker/consumer/BatchDeliveryService.java"},{"slug":"legacy-consumer","title":"Legacy Consumer Flow","shortTitle":"Legacy Consumer","category":"Compatibility data plane","summary":"A legacy service registers by service name, receives a merged batch across configured topics, and uses generic ACK messages that the broker resolves by FIFO expectation.","scenario":"A deployed price service registers once and is mapped to prices-v1 plus reference-data-v5.","steps":[{"title":"Protocol is detected","detail":"The first byte must be legacy REGISTER ordinal 0. ProtocolDetectionDecoder switches the channel to legacy codecs.","signal":"REGISTER = 0"},{"title":"Service name maps to topics","detail":"The legacy adapter converts registration into internal subscription and LegacyClientConfig resolves the configured service-to-topics map.","signal":"service → topics[]"},{"title":"Consumers are registered","detail":"The broker creates one RemoteConsumer per mapped topic while the legacy client keeps one service-level connection.","signal":"one socket, many topics"},{"title":"READY uses generic ACK","detail":"The broker sends legacy READY. LegacyConnectionState records the expected response and converts the next generic ACK to READY_ACK.","signal":"READY → ACK"},{"title":"Topics are merged","detail":"LegacyConsumerDeliveryManager creates a cursor per topic and performs a priority-queue k-way merge by offset.","signal":"k-way offset merge"},{"title":"JSON batch is delivered","detail":"The merged records are encoded as one legacy BatchEvent rather than the modern zero-copy batch format.","signal":"BATCH JSON"},{"title":"Generic ACK commits all topics","detail":"FIFO expectation resolves ACK to BATCH_ACK, then the broker commits the maximum included offset for every topic in the batch.","signal":"ACK → per-topic progress"}],"outcome":"The old client sees one ordered merged stream while the broker preserves separate topic offsets and ACK audit records.","safeguards":["Unknown service names do not register.","Pending batch timeout frees the legacy delivery slot.","Disconnect clears readiness and pending expectations.","Exact service aliases are configuration-sensitive."],"sources":["network/src/main/java/com/messaging/network/legacy/ProtocolDetectionDecoder.java","network/src/main/java/com/messaging/network/legacy/LegacyConnectionState.java","broker/src/main/java/com/messaging/broker/legacy/LegacyClientConfig.java","broker/src/main/java/com/messaging/broker/legacy/LegacyConsumerDeliveryManager.java"],"related":[{"label":"Legacy protocol","route":"#/chapter/06-event-kafka-flow/legacy-wire-protocol"},{"label":"Legacy delivery","route":"#/chapter/06-event-kafka-flow/legacy-delivery-flow"},{"label":"Known mapping risk","route":"#/chapter/10-test-map/observed-failures"}],"searchText":"Legacy Consumer Flow Compatibility data plane A legacy service registers by service name, receives a merged batch across configured topics, and uses generic ACK messages that the broker resolves by FIFO expectation. A deployed price service registers once and is mapped to prices-v1 plus reference-data-v5. The old client sees one ordered merged stream while the broker preserves separate topic offsets and ACK audit records. Protocol is detected The first byte must be legacy REGISTER ordinal 0. ProtocolDetectionDecoder switches the channel to legacy codecs. REGISTER = 0 Service name maps to topics The legacy adapter converts registration into internal subscription and LegacyClientConfig resolves the configured service-to-topics map. service → topics[] Consumers are registered The broker creates one RemoteConsumer per mapped topic while the legacy client keeps one service-level connection. one socket, many topics READY uses generic ACK The broker sends legacy READY. LegacyConnectionState records the expected response and converts the next generic ACK to READY_ACK. READY → ACK Topics are merged LegacyConsumerDeliveryManager creates a cursor per topic and performs a priority-queue k-way merge by offset. k-way offset merge JSON batch is delivered The merged records are encoded as one legacy BatchEvent rather than the modern zero-copy batch format. BATCH JSON Generic ACK commits all topics FIFO expectation resolves ACK to BATCH_ACK, then the broker commits the maximum included offset for every topic in the batch. ACK → per-topic progress Unknown service names do not register. Pending batch timeout frees the legacy delivery slot. Disconnect clears readiness and pending expectations. Exact service aliases are configuration-sensitive. network/src/main/java/com/messaging/network/legacy/ProtocolDetectionDecoder.java network/src/main/java/com/messaging/network/legacy/LegacyConnectionState.java broker/src/main/java/com/messaging/broker/legacy/LegacyClientConfig.java broker/src/main/java/com/messaging/broker/legacy/LegacyConsumerDeliveryManager.java"},{"slug":"refresh","title":"POS Refresh Flow","shortTitle":"Refresh","category":"Reset, replay, ready","summary":"Refresh freezes upstream pipe changes, resets selected POS groups, replays the local snapshot, and reopens normal operation only after every expected consumer is ready.","scenario":"An operator refreshes prices-v1 while two POS consumer groups are connected.","steps":[{"title":"Admin starts refresh","detail":"POST /admin/refresh-topic snapshots registered topic/group consumers, creates a refresh ID, pauses pipe calls, and persists RESET_SENT.","signal":"RESET_SENT"},{"title":"Broker sends RESET","detail":"ACK reconciliation pauses, existing ACK audit entries are cleared for expected groups, and RESET is broadcast with retries every 5 seconds.","signal":"RESET → RESET_ACK"},{"title":"Offsets reset","detail":"Each accepted RESET_ACK resets that consumer group's topic offset to 0. The first accepted ACK claims transition into REPLAYING.","signal":"offset = 0"},{"title":"Snapshot is replayed","detail":"Normal adaptive batch delivery runs against stable local storage while the upstream pipe remains paused.","signal":"REPLAYING"},{"title":"Catch-up is confirmed","detail":"RefreshReplayService requires every expected RESET ACK and every acknowledged consumer offset to reach the storage head.","signal":"consumer offset ≥ head"},{"title":"Broker sends READY","detail":"The state becomes READY_SENT. READY is sent to the live reset-ACK audience and retried every 10 seconds when needed.","signal":"READY → READY_ACK"},{"title":"Normal operation resumes","detail":"After all READY ACKs, the state becomes COMPLETED, reconciliation resumes, and the pipe resumes when no sibling topic refresh remains active.","signal":"COMPLETED"}],"outcome":"Both groups rebuild from the same stable local snapshot before new parent updates are allowed to enter.","safeguards":["State is persisted for broker restart recovery.","Late joiners are added to RESET or READY handling based on active state.","A 10-minute watchdog can abort stalled refreshes.","Pipe resumes only after all topics sharing the refresh ID are terminal."],"sources":["broker/src/main/java/com/messaging/broker/http/RefreshController.java","broker/src/main/java/com/messaging/broker/consumer/RefreshCoordinator.java","broker/src/main/java/com/messaging/broker/consumer/RefreshResetService.java","broker/src/main/java/com/messaging/broker/consumer/RefreshReplayService.java","broker/src/main/java/com/messaging/broker/consumer/RefreshReadyService.java"],"related":[{"label":"Refresh state machine","route":"#/chapter/07-pos-machine-state/refresh-state-machine"},{"label":"Refresh event flow","route":"#/chapter/06-event-kafka-flow/refresh-event-flow"},{"label":"Refresh API","route":"#/chapter/04-api-catalog/refresh-api"}],"searchText":"POS Refresh Flow Reset, replay, ready Refresh freezes upstream pipe changes, resets selected POS groups, replays the local snapshot, and reopens normal operation only after every expected consumer is ready. An operator refreshes prices-v1 while two POS consumer groups are connected. Both groups rebuild from the same stable local snapshot before new parent updates are allowed to enter. Admin starts refresh POST /admin/refresh-topic snapshots registered topic/group consumers, creates a refresh ID, pauses pipe calls, and persists RESET_SENT. RESET_SENT Broker sends RESET ACK reconciliation pauses, existing ACK audit entries are cleared for expected groups, and RESET is broadcast with retries every 5 seconds. RESET → RESET_ACK Offsets reset Each accepted RESET_ACK resets that consumer group's topic offset to 0. The first accepted ACK claims transition into REPLAYING. offset = 0 Snapshot is replayed Normal adaptive batch delivery runs against stable local storage while the upstream pipe remains paused. REPLAYING Catch-up is confirmed RefreshReplayService requires every expected RESET ACK and every acknowledged consumer offset to reach the storage head. consumer offset ≥ head Broker sends READY The state becomes READY_SENT. READY is sent to the live reset-ACK audience and retried every 10 seconds when needed. READY → READY_ACK Normal operation resumes After all READY ACKs, the state becomes COMPLETED, reconciliation resumes, and the pipe resumes when no sibling topic refresh remains active. COMPLETED State is persisted for broker restart recovery. Late joiners are added to RESET or READY handling based on active state. A 10-minute watchdog can abort stalled refreshes. Pipe resumes only after all topics sharing the refresh ID are terminal. broker/src/main/java/com/messaging/broker/http/RefreshController.java broker/src/main/java/com/messaging/broker/consumer/RefreshCoordinator.java broker/src/main/java/com/messaging/broker/consumer/RefreshResetService.java broker/src/main/java/com/messaging/broker/consumer/RefreshReplayService.java broker/src/main/java/com/messaging/broker/consumer/RefreshReadyService.java"},{"slug":"compaction","title":"Compaction Flow","shortTitle":"Compaction","category":"Latest-key retention","summary":"The broker tracks the latest offset for each key, filters stale values during delivery, and later rewrites sealed segments while preserving original offsets.","scenario":"product-42 appears at offsets 100 and 140, then receives a DELETE at 190.","steps":[{"title":"Ingress updates latest-key state","detail":"At offset 140, CompactionIndex marks offset 100 superseded. At 190, the DELETE tombstone becomes the latest state.","signal":"product-42 → 190"},{"title":"Delivery filtering is immediate","detail":"Before physical rewrite, BatchDeliveryService drops superseded offsets. Consumers see the latest surviving state rather than old versions.","signal":"filter 100, 140"},{"title":"Scheduler selects work","detail":"A single-flight scheduled or manual run checks CPU/heap guards and selects topics with enough sealed segments.","signal":"sealed segments only"},{"title":"Planner chooses a window","detail":"CompactionPlanner starts above the stored checkpoint, limits the window size, and always excludes the active segment.","signal":"checkpoint → window"},{"title":"Segments are rewritten","detail":"The streaming rewriter copies surviving records, preserves their original offsets, and removes superseded entries.","signal":"offset gaps preserved"},{"title":"Tombstone retention is applied","detail":"The latest DELETE remains until its retention period expires. Only then may a later compaction remove that tombstone.","signal":"DELETE retained"},{"title":"Replacement is published","detail":"Staging files are forced and atomically renamed, SegmentManager swaps the segment map under a write lock, and checkpoints advance when safe.","signal":"atomic replace"}],"outcome":"Disk eventually contains only the latest retained state, while consumers continue using the original sparse offsets and remain protected before rewrite.","safeguards":["Active segments are excluded unless a manual trigger force-rolls them first.","Streaming rewrite uses a bounded 256 KB buffer.","Atomic staging publication supports recovery.","Compaction is not automatically paused during refresh."],"sources":["broker/src/main/java/com/messaging/broker/compaction/CompactionIndex.java","broker/src/main/java/com/messaging/broker/compaction/CompactionPlanner.java","broker/src/main/java/com/messaging/broker/compaction/CompactionRewriter.java","broker/src/main/java/com/messaging/broker/compaction/CompactionScheduler.java","broker/src/main/java/com/messaging/broker/consumer/BatchDeliveryService.java"],"related":[{"label":"Compaction handling","route":"#/chapter/08-compaction-handling"},{"label":"Event semantics","route":"#/chapter/06-event-kafka-flow/compaction-event-semantics"},{"label":"Compaction API","route":"#/chapter/04-api-catalog/compaction-api"}],"searchText":"Compaction Flow Latest-key retention The broker tracks the latest offset for each key, filters stale values during delivery, and later rewrites sealed segments while preserving original offsets. product-42 appears at offsets 100 and 140, then receives a DELETE at 190. Disk eventually contains only the latest retained state, while consumers continue using the original sparse offsets and remain protected before rewrite. Ingress updates latest-key state At offset 140, CompactionIndex marks offset 100 superseded. At 190, the DELETE tombstone becomes the latest state. product-42 → 190 Delivery filtering is immediate Before physical rewrite, BatchDeliveryService drops superseded offsets. Consumers see the latest surviving state rather than old versions. filter 100, 140 Scheduler selects work A single-flight scheduled or manual run checks CPU/heap guards and selects topics with enough sealed segments. sealed segments only Planner chooses a window CompactionPlanner starts above the stored checkpoint, limits the window size, and always excludes the active segment. checkpoint → window Segments are rewritten The streaming rewriter copies surviving records, preserves their original offsets, and removes superseded entries. offset gaps preserved Tombstone retention is applied The latest DELETE remains until its retention period expires. Only then may a later compaction remove that tombstone. DELETE retained Replacement is published Staging files are forced and atomically renamed, SegmentManager swaps the segment map under a write lock, and checkpoints advance when safe. atomic replace Active segments are excluded unless a manual trigger force-rolls them first. Streaming rewrite uses a bounded 256 KB buffer. Atomic staging publication supports recovery. Compaction is not automatically paused during refresh. broker/src/main/java/com/messaging/broker/compaction/CompactionIndex.java broker/src/main/java/com/messaging/broker/compaction/CompactionPlanner.java broker/src/main/java/com/messaging/broker/compaction/CompactionRewriter.java broker/src/main/java/com/messaging/broker/compaction/CompactionScheduler.java broker/src/main/java/com/messaging/broker/consumer/BatchDeliveryService.java"},{"slug":"pipe-consistency","title":"Pipe Consistency Flow","shortTitle":"Pipe Consistency","category":"Detect-only integrity audit","summary":"The child compares compaction-invariant keyspace digests with a parent, drills into mismatched buckets, and classifies missing, stale, or zombie keys without repairing data.","scenario":"A POS was offline long enough to possibly miss both records and expired DELETE tombstones, then an operator checks prices-v1.","steps":[{"title":"Check is started","detail":"An admin POST or enabled scheduler starts one single-flight check. The target is resolved from the current parent or cloud topology.","signal":"202 async check"},{"title":"Child fixes a watermark","detail":"The service scans its compaction index at the child head. A lagging parent can clamp comparison to an effective watermark.","signal":"child watermark"},{"title":"Both sides build bucket digests","detail":"KeyspaceDigest folds latest key/offset state into 64 XOR buckets. This remains stable even when physical segment compaction differs.","signal":"64 digest buckets"},{"title":"Equal digest ends quickly","detail":"If every digest and count matches, the topic is CONSISTENT or CONSISTENT_UP_TO the clamped watermark.","signal":"fast path"},{"title":"Mismatched buckets are fetched","detail":"The child requests only mismatched bucket entries, bounded by drill-down and entry limits.","signal":"bucket drill-down"},{"title":"Differences are classified","detail":"Parent physical-offset checks and latest-key index state distinguish missed data, stale keys, zombie keys, lagging keys, and child-newer keys.","signal":"classify"},{"title":"Report is published","detail":"The bounded report history and Micrometer gauges expose the verdict and whether a refresh is recommended. No repair is performed.","signal":"detect, never repair"}],"outcome":"Operators receive a per-topic verdict such as CONSISTENT, INCONSISTENT, UNREACHABLE, or INCONCLUSIVE and can decide whether to initiate refresh.","safeguards":["Feature scheduler is fail-closed and disabled by default.","Offline parent returns UNREACHABLE without retry loops.","Heap pressure and concurrent-scan guards skip or reject expensive work.","Drill-down and classification sizes are capped."],"sources":["broker/src/main/java/com/messaging/broker/consistency/PipeConsistencyService.java","broker/src/main/java/com/messaging/broker/consistency/KeyspaceDigest.java","broker/src/main/java/com/messaging/broker/consistency/ParentConsistencyClient.java","broker/src/main/java/com/messaging/broker/http/PipeConsistencyController.java","broker/src/main/java/com/messaging/broker/http/PipeConsistencyAdminController.java"],"related":[{"label":"Feature catalog","route":"#/chapter/03-feature-catalog/pipe-consistency-detection"},{"label":"Consistency API","route":"#/chapter/04-api-catalog/pipe-consistency-api"},{"label":"Runtime configuration","route":"#/chapter/11-runtime-config"}],"searchText":"Pipe Consistency Flow Detect-only integrity audit The child compares compaction-invariant keyspace digests with a parent, drills into mismatched buckets, and classifies missing, stale, or zombie keys without repairing data. A POS was offline long enough to possibly miss both records and expired DELETE tombstones, then an operator checks prices-v1. Operators receive a per-topic verdict such as CONSISTENT, INCONSISTENT, UNREACHABLE, or INCONCLUSIVE and can decide whether to initiate refresh. Check is started An admin POST or enabled scheduler starts one single-flight check. The target is resolved from the current parent or cloud topology. 202 async check Child fixes a watermark The service scans its compaction index at the child head. A lagging parent can clamp comparison to an effective watermark. child watermark Both sides build bucket digests KeyspaceDigest folds latest key/offset state into 64 XOR buckets. This remains stable even when physical segment compaction differs. 64 digest buckets Equal digest ends quickly If every digest and count matches, the topic is CONSISTENT or CONSISTENT_UP_TO the clamped watermark. fast path Mismatched buckets are fetched The child requests only mismatched bucket entries, bounded by drill-down and entry limits. bucket drill-down Differences are classified Parent physical-offset checks and latest-key index state distinguish missed data, stale keys, zombie keys, lagging keys, and child-newer keys. classify Report is published The bounded report history and Micrometer gauges expose the verdict and whether a refresh is recommended. No repair is performed. detect, never repair Feature scheduler is fail-closed and disabled by default. Offline parent returns UNREACHABLE without retry loops. Heap pressure and concurrent-scan guards skip or reject expensive work. Drill-down and classification sizes are capped. broker/src/main/java/com/messaging/broker/consistency/PipeConsistencyService.java broker/src/main/java/com/messaging/broker/consistency/KeyspaceDigest.java broker/src/main/java/com/messaging/broker/consistency/ParentConsistencyClient.java broker/src/main/java/com/messaging/broker/http/PipeConsistencyController.java broker/src/main/java/com/messaging/broker/http/PipeConsistencyAdminController.java"}]};
