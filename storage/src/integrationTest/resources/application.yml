broker:
  storage:
    data-dir: ${java.io.tmpdir}/storage-integration-default
    type: filechannel         # Default; individual specs override via TestPropertyProvider
    segment-size: 1048576     # 1 MB — small segments help trigger rollover in tests

micronaut:
  server:
    port: -1
  application:
    name: storage-integration-test
